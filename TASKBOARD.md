# TASKBOARD.md — Machine-Verifiable Task Tracking (本地工具脚本真相源)
# Last updated: 2026-07-19
# Format: Frozen v2 (MoA签发版) — 4状态模型

## ⚠️ STATE MACHINE (唯一状态流)
```
TODO → IN_PROGRESS → REVIEW → DONE
                         ↓
                      BLOCKED
```

## ⚠️ RULE
- 每次状态变化 → 在【活动任务】区追加一行
- Items NOT in this file = do not exist
- Before every deploy: `scripts/check-taskboard.sh`. Any ❌ = abort
- Every session start: Hermes reads this file first

---

## 活动任务

| TASK_ID | STATUS | OWNER | UPDATED_AT |
|---------|--------|-------|------------|
|| task_P1-C | DONE | Hermes | 2026-07-04 |
|| task_P1-D | DONE | Codex→Hermes | 2026-07-04 |
|| task_P1-E | DONE | Codex→Hermes | 2026-07-04 |
|| task_P1-F | DONE | Codex→Hermes | 2026-07-04 |
|| task_P1-G | DONE | Codex→Hermes | 2026-07-04 |
|| task_P2_reads_all | DONE | Codex→Hermes | 2026-07-04 |
|| task_P2_mutations_low | DONE | Codex→Hermes | 2026-07-04 |
|| task_P2_mutations_core | DONE | Codex→Hermes | 2026-07-04 |
|| task_P2_mutations_settings | DONE | Codex→Hermes | 2026-07-04 |
| task_true_codex_reaudit | DONE | Codex (GPT-5.5) via codex exec | 2026-07-05 |
| task_true_codex_fail_fix | DONE | Codex (GPT-5.5) via codex exec | 2026-07-05 |
| task_true_codex_reaudit_delta | DONE | Codex (GPT-5.5) via codex exec | 2026-07-05 |
| task_true_codex_deploy | DONE | Hermes | 2026-07-05 |
| task_P3_0_spec_sync | DONE | Hermes | 2026-07-05 |
| task_INFRA_codex_sandbox_diagnosis | DONE | Hermes | 2026-07-06 |
| task_P3_1_won_at | DONE | Codex (GPT-5.5) via codex exec → Hermes apply | 2026-07-05 |
| task_P3_1b_alertpanel | DONE | Codex (GPT-5.5) via codex exec | 2026-07-05 |
| task_P3_2_first_contact_trigger | DONE | Codex (GPT-5.5) via codex exec → Hermes apply | 2026-07-05 |
| task_P3_3_quality_api | DONE | Codex (GPT-5.5) via codex exec | 2026-07-05 |
| task_P3_5_dashboard_summary_api | DONE | Codex (GPT-5.5) via codex exec | 2026-07-05 |
| task_P3_6_dashboard_month_filter | DONE | Codex (GPT-5.5) via codex exec | 2026-07-05 |
| task_P3_7_leads_contact_quality_ui | DONE | Codex (GPT-5.5) via codex exec | 2026-07-05 |
| task_P3_8_weekly_review | DONE | Codex (GPT-5.5) via codex exec | 2026-07-05 |
| task_P3_4_deprecate_redirect | DONE | Hermes (manual) | 2026-07-05 |
| task_P3_9_smoke_acceptance | DONE | Hermes (manual safe subset) | 2026-07-05 |
|| task_P0_schema_alias_fix_combo | DONE | Codex (GPT-5.5) via codex exec → Hermes review | 2026-07-06 |
| task_P0_hotfix_audit_trail | DONE | Codex→Hermes | 2026-07-06 |
|| task_kanban_unify | DONE | Codex (GPT-5.5) | 2026-07-06 |
|| task_M1_freeze_sam6 | DONE | Hermes (K3 总控) | 2026-07-19 |
|| task_M1_sam7_preflight | DONE | Hermes | 2026-07-19 |
|| task_M1_sam8_deploy | DONE | Hermes | 2026-07-19 |
|| task_M1_sam9_smoke | DONE | Hermes | 2026-07-19 |
|| task_M1_sam43_api_uat | DONE | Hermes | 2026-07-19 |
| task_L0_auth_me_proxy_fix | DONE | Hermes (OC) | 2026-07-20 |
| PROD-CONTACT-QUALITY-AUTH | DONE | Codex | 2026-08-05 |
| PROD-AUTH-SESSION-HARDENING | DONE | Codex | 2026-08-06 |
| PROD-AUTH-CACHE-HEADERS | DONE | Codex | 2026-08-06 |
| PROD-AUTH-ME-RLS-HOTFIX | DONE | Codex | 2026-08-08 |
| PROD-SUPPLY-CHAIN-ADVISORIES | DONE | Codex | 2026-08-08 |
| PROD-DEPENDABOT-HIGH-ALERTS-OPEN | DONE | Codex | 2026-08-16 |
| PROD-CODEQL-BLOCKING-RULESET-MISSING | DONE | Codex | 2026-08-16 |
| PROD-READINESS-ANON-KEY | DONE | Codex | 2026-08-08 |
| PROD-INVALID-SERVICE-KEY-RECOVERY | DONE | Codex | 2026-08-08 |
| PROD-DEPLOY-LOG-INVOCATION-SCOPE | DONE | Codex | 2026-08-08 |
| PROD-SERVICE-KEY-ROTATION | DONE | Codex | 2026-08-08 |
| PROD-KPI-TARGETS-FK-EMBED | DONE | Codex | 2026-08-08 |
| PROD-AUTH-5XX-LOGIN-PROBE | DONE | Codex | 2026-08-08 |
| PROD-L0-ROOT-CAUSE-PREVENTION | DONE | Codex | 2026-08-09 |
| PROD-L0-FULL-STACK-TEST-GATES | DONE | Codex | 2026-08-09 |
| PROD-DEPLOY-EVIDENCE-PERSISTENCE | DONE | Codex | 2026-08-06 |
| task_SAM51_proxy_service_role_hardening | DONE | Hermes (OC) | 2026-07-20 |
| PROD-LOGIN-LATENCY-SERVER-GRANT | REVIEW | Claude | 2026-08-11 |
| PROD-SESSION-COOKIE-CONTRACT | REVIEW | Claude | 2026-08-11 |
| PROD-AUTH-LOGIN-RATE-LIMIT | REVIEW | Claude | 2026-08-11 |
| PROD-AUTH-ME-CLIENT-DEDUP | REVIEW | Claude | 2026-08-11 |
| PROD-L0-AUDIT-FIX-F07-F15-F25-F04 | REVIEW | Claude | 2026-08-11 |
| PROD-FALSE-GREEN-GATE-RETARGET | REVIEW | Claude | 2026-08-11 |
| PROD-L0-DB-MIGRATIONS-F02-F06-F08-F09-F10 | BLOCKED | Claude | 2026-08-11 |
| PROD-L0-MIGRATION-REPLAY-GATE | REVIEW | Claude | 2026-08-11 |
| PROD-MIGRATION-HISTORY-UNREPLAYABLE | DONE | Codex | 2026-08-15 |
| PROD-DEPLOY-RELEASE-CLAIM-VALIDATION | REVIEW | Claude | 2026-08-11 |
| PROD-CI-CRM-HERMES-FALSE-GREEN | REVIEW | Claude | 2026-08-11 |
| PROD-L0-OPEN-REDIRECT-SESSION-CHAIN | REVIEW | Claude | 2026-08-11 |
| PROD-AUTH-LIMITER-EVICTION-BYPASS | REVIEW | Claude | 2026-08-11 |
| PROD-RELEASE-SCRIPT-FAIL-CLOSED | REVIEW | Claude | 2026-08-11 |
| PROD-KPI-TARGETS-ATOMIC-REPLACE | REVIEW | Claude | 2026-08-11 |
| PROD-QUOTATION-CONVERT-ATOMICITY | REVIEW | Claude | 2026-08-11 |
| PROD-COS-SCRIPT-RELEASE-DRIFT | REVIEW | Claude | 2026-08-11 |
| PROD-AUTH-OLD-TOKEN-REVOCATION | REVIEW | Claude | 2026-08-11 |
| PROD-F09-MONEY-AUTHORIZATION-PHASE2 | TODO | Claude | 2026-08-11 |
| PROD-F02-DEV-SETUP-CREDENTIAL-REMOVAL | REVIEW | Claude | 2026-08-12 |
| PROD-SECRET-SCANNING-ALERTS-OPEN | DONE | Claude | 2026-08-20 |
| PROD-AUTH-ADMIN-RESET-GLOBAL-REVOCATION | REVIEW | Claude | 2026-08-11 |
| PROD-AUTH-LEAKED-PASSWORD-PROTECTION | DONE | Codex | 2026-08-15 |
| PROD-CONTRACT-REVOKE-LIST-DIRECT-WRITE | REVIEW | Claude | 2026-08-13 |
| PROD-PAYMENT-RECORD-NO-IDEMPOTENCY-KEY | REVIEW | Claude | 2026-08-13 |
| PROD-DEPLOY-TASKBOARD-GATE-MISSING | REVIEW | Claude | 2026-08-11 |
| PROD-PROXY-ACTIVITY-THROTTLE-UNBOUNDED | REVIEW | Claude | 2026-08-14 |
| PROD-MIGRATION-HISTORY-IMMUTABILITY-GATE | REVIEW | Claude | 2026-08-11 |
| PROD-CONTRACT-STATUS-PATCH-ROUTE | REVIEW | Claude | 2026-08-11 |
| PROD-ROLLBACK-SECURITY-PRESERVING | REVIEW | Claude | 2026-08-11 |
| PROD-MIGRATION-HISTORY-CONTENT-RECONCILIATION | DONE | Codex | 2026-08-15 |
| PROD-CONTROL-PLANE-BOOTSTRAP | BLOCKED | Claude | 2026-08-11 |
| PROD-CREDENTIAL-RUNBOOK-TWO-PHASE | DONE | Codex | 2026-08-15 |
| PROD-CREDENTIAL-PROVIDER-RECEIPT-PRODUCER | DONE | Codex | 2026-08-15 |
| PROD-MAIN-BRANCH-PROTECTION-UNENFORCED | DONE | Codex | 2026-08-15 |
| PROD-L0-ROUND4-ENTRY-BOUNDARY-AND-MONEY-INTEGRITY | REVIEW | Claude | 2026-08-13 |
| PROD-FIRST-PAYMENT-STATUS-LITERAL-SEQ-ONE | REVIEW | Claude | 2026-08-13 |
| PROD-INSTALLMENT-SCHEDULE-NONCONTIGUOUS | REVIEW | Claude | 2026-08-13 |
| PROD-CONVERSION-RETRY-DOUBLE-COUNTS-AMOUNT | REVIEW | Claude | 2026-08-14 |
| PROD-KPI-PERIOD-DELETE-BYPASSES-ROUTINE | REVIEW | Claude | 2026-08-14 |
| PROD-ROLLBACK-HAS-NO-RETURN-PATH | REVIEW | Claude | 2026-08-13 |
| PROD-QUOTE-CONVERT-SCHEDULE-CENT-TOLERANCE | REVIEW | Claude | 2026-08-13 |
| PROD-PAYMENT-STATE-MODEL-DISAGREEMENT | REVIEW | Claude | 2026-08-13 |
| PROD-ROLLBACK-DB-PHASE-UNCOUPLED | REVIEW | Claude | 2026-08-13 |
| PROD-CHECKOUT-REWRITES-RELEASE-BYTES | REVIEW | Claude | 2026-08-13 |
| PROD-FINGERPRINT-AND-TXCONTROL-COERCION | REVIEW | Claude | 2026-08-13 |
| PROD-STRICT-POSTURE-UNDERVERIFIED | REVIEW | Claude | 2026-08-13 |
| PROD-COMPANIONS-UNBOUND-TO-RELEASE | REVIEW | Claude | 2026-08-13 |
| PROD-DEPLOY-TRUSTS-OPERATOR-MIGRATION-SET | REVIEW | Claude | 2026-08-13 |
| PROD-LEAD-ASSIGNMENT-CAS-DECORATIVE | REVIEW | Claude | 2026-08-14 |
| PROD-NOTIFICATION-FACT-IDEMPOTENCY | DONE | Codex | 2026-08-15 |
| PROD-ROLLBACK-COMPANION-PROMISES-UNCHECKED | REVIEW | Claude | 2026-08-14 |
| PROD-DEPLOY-TASKBOARD-GATE-UNSATISFIABLE | REVIEW | Claude | 2026-08-14 |
| PROD-SYSTEMD-SANDBOX-BLOCKS-NETLINK | DONE | Claude | 2026-08-20 |
| PROD-RUNTIME-SERVICE-KEY-NOT-RELOCATED | DONE | Claude | 2026-08-20 |
| PROD-HEALTH-PROBE-ASSERTS-RETIRED-UNIT | DONE | Claude | 2026-08-20 |
| PROD-UAT-FIXTURE-LEAD-SOURCE-INVALID | DONE | Claude | 2026-08-20 |
| PROD-LOGOUT-REPORTS-UNPERFORMED-REVOCATION | DONE | Claude | 2026-08-20 |
| PROD-UAT-PRODUCER-PARAMETER-GAP | DONE | Claude | 2026-08-20 |
| PROD-UAT-LEAD-WON-EXPECTATION-STALE | DONE | Claude | 2026-08-20 |
| PROD-UAT-KPI-PERIOD-UNINSERTABLE | DONE | Claude | 2026-08-20 |

### 发布里程碑（release scopes）· round-4 C4-4

`scripts/check-taskboard.mjs` 过去只有两种答案：**零未完成行**或**被阻断**，而 canonical deploy wrapper 要的是前者。本板上多数行按设计到不了那里——它们写着 `待部署`，**关闭它们的正是“生产已经跑过这次变更”**。于是 AGENTS.md 称为「物理阻断部署」的那道门禁在结构上不可满足，与 `infra/release/required-jobs.json` 记录的那个缺陷同形：**不可满足的门禁不是严格的门禁，而是一道会被绕过的门禁，被绕过的门禁什么都不保护**。

因此每一行未完成行必须声明它属于哪个里程碑，声明就写在本文件里（Iron Rule：不在板上就不存在）：

| 里程碑 | 含义 | 谁要求它 |
| --- | --- | --- |
| `predeploy_ready` | 部署或受保护的控制面 bootstrap **之前**必须关闭 | `infra/systemd/newme-deploy.sh` 在安装任何东西之前跑 `--require-scope=predeploy_ready` |
| `postdeploy_acceptance` | 不得作为候选 SHA 的自证前置；生产证据和候选 exact-head CI 的板上关闭都由部署后的 TASKBOARD-only closure SHA 记录 | 只有 release-final（`--require-complete`）要求它 |

里程碑**是累积的**：要求 `postdeploy_acceptance` 同时要求 `predeploy_ready`，所以后一个里程碑永远不能越过前一个变绿。候选 SHA 的必需 CI 结论由 canonical wrapper 直接读取 GitHub API 并逐 job 对照候选自己的 `infra/release/required-jobs.json`，不能要求同一个候选 SHA 先把“未来 CI 已绿”写回 TASKBOARD（写回就已换 SHA）。该 manifest 的 `taskboard_closure_evidence` 把这种行机器映射到具体 required job、全部 required jobs 或 main-push/Hermes 后续证据；checker 强制它们留在 `postdeploy_acceptance`。生产完成后只允许一个仅改 `TASKBOARD.md` 的 closure SHA，release-final CI 再绑定 release→closure ancestry、单文件 diff 与全板完成。控制面 bootstrap 不再拥有会自指的第三种 scope；候选 wrapper 的 `bootstrap` 模式会重新执行与 canonical deploy 相同的 exact-SHA CI、taskboard、迁移声明、生产迁移历史与 companion 内容门禁，然后才写机器 gate record。`--require-complete` 等价于最后一个里程碑，仍是 release-final 门禁。

声明每次运行 checker 都**双向**校验（包括那个不要求任何 scope 的 CI job）：**未声明的未完成行 = FAIL 并指名**（否则「离开部署门禁」的办法就是加一行然后什么都不说），**声明了却没有对应未完成行 = FAIL 并指名**（否则陈旧声明会堆到没人分得清哪条是承重的）。块内出现既不是表头也不是三格声明的行同样 FAIL——那正是把真实行藏进块里躲过扫描的路子。

<!-- taskboard-scopes:begin -->
| item | scope | closure condition |
| --- | --- | --- |
| PROD-CHECKOUT-REWRITES-RELEASE-BYTES | postdeploy_acceptance | 候选 exact-head 的 Windows checkout and SPEC gate 由 wrapper 在部署前强制成功；关闭条件是在部署后的 TASKBOARD-only closure SHA 记录该 run 证据 |
| PROD-DEPLOY-TASKBOARD-GATE-UNSATISFIABLE | postdeploy_acceptance | 候选 exact-head 的 Predeploy taskboard readiness 由 wrapper 在部署前强制成功；关闭条件是在部署后的 TASKBOARD-only closure SHA 记录该 run 证据 |
| PROD-CI-CRM-HERMES-FALSE-GREEN | postdeploy_acceptance | 关闭条件是合入 main 后首次 main push 上 crm-ci workflow_run 成功 + Hermes delivery 200，并在 TASKBOARD-only closure SHA 记录证据 |
| PROD-COMPANIONS-UNBOUND-TO-RELEASE | postdeploy_acceptance | 候选 exact-head 的 Migration replay and release contracts 由 wrapper 在部署前强制成功；关闭条件是在 TASKBOARD-only closure SHA 记录该 run 证据 |
| PROD-DEPLOY-TRUSTS-OPERATOR-MIGRATION-SET | postdeploy_acceptance | 候选 exact-head 的全部 required jobs 与紧邻切换前的机器重验由 wrapper 强制成功；关闭条件是在 TASKBOARD-only closure SHA 记录证据 |
| PROD-FINGERPRINT-AND-TXCONTROL-COERCION | postdeploy_acceptance | 候选 exact-head 的 Migration replay and release contracts 由 wrapper 在部署前强制成功；关闭条件是在 TASKBOARD-only closure SHA 记录该 run 证据 |
| PROD-L0-MIGRATION-REPLAY-GATE | postdeploy_acceptance | 候选 exact-head 的 Migration replay and release contracts 由 wrapper 在部署前强制成功；关闭条件是在 TASKBOARD-only closure SHA 记录该 run 证据 |
| PROD-STRICT-POSTURE-UNDERVERIFIED | postdeploy_acceptance | 候选 exact-head 的 Migration replay and release contracts 由 wrapper 在部署前强制成功；关闭条件是在 TASKBOARD-only closure SHA 记录该 run 证据 |
| SAM-6 | postdeploy_acceptance | 候选 exact-head 全部 required jobs 由 wrapper 在部署前强制成功；关闭条件是部署后由总控在 TASKBOARD-only closure SHA 记录复核 |
| hermes-ci | postdeploy_acceptance | 关闭条件是 ci 成功 + crm-ci workflow_run 成功 + Hermes webhook delivery 200，并在 TASKBOARD-only closure SHA 记录三份可审计证据 |
| PROD-CONTROL-PLANE-BOOTSTRAP | postdeploy_acceptance | 关闭条件是候选 wrapper 的 bootstrap coordinator 对同一 release SHA 重新执行全部机器门禁、完成可回滚控制面事务，并留下 snapshot、finalize、健康与零 pending 证据；操作者不得手写 gate record |
| PROD-AUTH-ADMIN-RESET-GLOBAL-REVOCATION | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-AUTH-LIMITER-EVICTION-BYPASS | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-AUTH-LOGIN-RATE-LIMIT | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-AUTH-ME-CLIENT-DEDUP | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-AUTH-OLD-TOKEN-REVOCATION | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-CONTRACT-REVOKE-LIST-DIRECT-WRITE | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-CONTRACT-STATUS-PATCH-ROUTE | postdeploy_acceptance | 关闭条件是部署后再加一次登录态人工 UAT |
| PROD-CONVERSION-RETRY-DOUBLE-COUNTS-AMOUNT | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-COS-SCRIPT-RELEASE-DRIFT | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-DEPLOY-RELEASE-CLAIM-VALIDATION | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-DEPLOY-TASKBOARD-GATE-MISSING | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-F02-DEV-SETUP-CREDENTIAL-REMOVAL | postdeploy_acceptance | 关闭条件是本轮受保护 credential-remediation 与 canonical deploy 完成数据库/PAT/员工凭据轮换、Auth 封禁、会话吊销和全部消费者验证；代码打码与让历史值停止生效是两件事 |
| PROD-F09-MONEY-AUTHORIZATION-PHASE2 | postdeploy_acceptance | 关闭条件是迁移在生产应用后，on_lead_won() 由 active 改 draft 的登录态 UAT |
| PROD-FALSE-GREEN-GATE-RETARGET | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-FIRST-PAYMENT-STATUS-LITERAL-SEQ-ONE | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-INSTALLMENT-SCHEDULE-NONCONTIGUOUS | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-KPI-PERIOD-DELETE-BYPASSES-ROUTINE | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-KPI-TARGETS-ATOMIC-REPLACE | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-L0-AUDIT-FIX-F07-F15-F25-F04 | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-L0-DB-MIGRATIONS-F02-F06-F08-F09-F10 | postdeploy_acceptance | 关闭条件是运维授权并在生产应用这批迁移，即部署自身的数据库相位；另需执行角色对 auth.users 有 SELECT |
| PROD-L0-OPEN-REDIRECT-SESSION-CHAIN | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-L0-ROUND4-ENTRY-BOUNDARY-AND-MONEY-INTEGRITY | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-LEAD-ASSIGNMENT-CAS-DECORATIVE | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-LOGIN-LATENCY-SERVER-GRANT | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-MIGRATION-HISTORY-IMMUTABILITY-GATE | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-PAYMENT-RECORD-NO-IDEMPOTENCY-KEY | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-PAYMENT-STATE-MODEL-DISAGREEMENT | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-PROXY-ACTIVITY-THROTTLE-UNBOUNDED | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-QUOTATION-CONVERT-ATOMICITY | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-QUOTE-CONVERT-SCHEDULE-CENT-TOLERANCE | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-RELEASE-SCRIPT-FAIL-CLOSED | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-ROLLBACK-COMPANION-PROMISES-UNCHECKED | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-ROLLBACK-DB-PHASE-UNCOUPLED | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-ROLLBACK-HAS-NO-RETURN-PATH | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-ROLLBACK-SECURITY-PRESERVING | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| PROD-SESSION-COOKIE-CONTRACT | postdeploy_acceptance | 修复与 PG17/离线证据已写在本条 review 行内；关闭条件是生产已应用本 release 且取得部署后验收证据 |
| SAM-26 | postdeploy_acceptance | 板上自述不拦发布：移动端/视觉人工验收留 M2 观察 |
| SAM-43 | postdeploy_acceptance | 关闭条件是登录态视觉/交互 UAT，不得以 API/CI 证据替代 |
<!-- taskboard-scopes:end -->

> M1 发布链（Linear 为真源）：**RELEASED 2026-07-20**。发布 SHA `49bbb26` → BUILD_ID `MDw2VC9TYmm1SsgcR2Lv-`（evidence 20260719-193837.json，smoke 14/14 + regression 22/22）。SAM-6~12 全链 Done：SAM-28 业务签收（森哥 2026-07-20）+ 技术签收（机器全量验收），SAM-12 发布记录出具。SAM-26 视觉/移动端留人工不拦发布；SAM-45/46 进 M2 backlog。
>
> 热修 #1（2026-07-20）：SAM-48+SAM-47 → SHA `ac416ba` → BUILD_ID `opQHvVVbt_cF_G80h-7is`（CI 29703674152/crm-ci 29703752341 双绿，smoke 14/14，生产 API 验收 8/8：未知设备 400、零总价双端 400、正常链回归 4536 AED 全通、health 无 memory；fixture 清理基线零漂移）。两票 Done。

> `task_P0_hotfix_audit_trail`: `20260706000005_add_leads_archived.sql` adds `leads_archived` as the 20th allowed event type, closing the archive audit gap.
>
> `task_L0_auth_me_proxy_fix` (热修 #2, 2026-07-20): CRM 全员无法登录。根因：`login/page.tsx:82-84` 用 Bearer 头调 `/api/auth/me`（cookies 尚未写入）；`proxy.ts` 的 Bearer fallback 鉴权了 user 但未传播给 supabase client，导致下游 profiles RLS（proxy.ts:77）以未鉴权 client 跑 → 返回 `inactive_account` → 登录页撤销刚创建的 session。修复：把 `/api/auth/me` 加入 `PUBLIC_API_PATHS`（一行）。安全性已复核：route handler `src/app/api/auth/me/route.ts` 用 service_role admin client 自查 profiles（L35-37）+ 主动拒绝未鉴权（L25-26）+ 主动拒绝 inactive（L45-47），proxy 的 RLS 复核对本端点冗余。tsc + eslint 双绿。待部署后用 Bearer curl 验收 200 + isActive:true。

---

## L0 登录延迟 + 安全审计修复 — 2026-08-11（Claude）

> 事实源：代码看 git（工作区 diff），事实看线上。所有行为 REVIEW 而非 DONE：本地门禁已全绿，但**尚未部署到生产**，未取得生产实测证据。部署 + 浏览器实测登录耗时后才可改 DONE。
>
> 本地门禁快照（全绿）：`typecheck` clean；`lint:baseline` PASS（407 errors，无新增）；`npm test` 376 tests / 373 pass / 0 fail / 3 skipped；`check:release` PASS（smoke 14/14）；`check:security` PASS（107 findings，无规则/文件超基线）；`check:workflows` PASS 3/3；`npm run build` exit=0。

| # | File | Operation | Verification | Status | Done Date |
|---|------|-----------|-------------|--------|-----------|
| PROD-AUTH-LEAKED-PASSWORD-PROTECTION | production Supabase Auth configuration, TASKBOARD.md | MODIFY+VERIFY | 2026-08-15 用户明确撤回旧的“保持关闭并接受风险”选择，并授权只把 production project `vfopmpxlhwzpxqegayew` 的 `password_hibp_enabled` 从 `false` 改为 `true`。只读控制面确认项目 `ACTIVE_HEALTHY`、organization `newme` 为 `pro` plan；变更前 Security Advisor 返回 1 条 `auth_leaked_password_protection`。执行前以同一内存凭据对精确 project endpoint 权限预检得到 HTTP 200，随后向官方 `PATCH /v1/projects/{ref}/config/auth` 发送唯一 JSON 字段 `password_hibp_enabled=true`，HTTP 200；PATCH 响应正文直接丢弃，未读取或输出完整 Auth 配置、SMTP、provider 或密钥。变更后再次只读运行 Security Advisor，`auth_leaked_password_protection` 数量为 0；其余 9 条既有告警均为 `authenticated_security_definer_function_executable`。本项关闭不改变 Secret Scanning、Dependabot High、CodeQL ruleset 三条 predeploy blocker。官方 remediation：<https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection> | ✅ | 2026-08-15 |
| PROD-SECRET-SCANNING-ALERTS-OPEN | GitHub Secret Scanning, Supabase control plane, supabase/preflight/f02-credential-cutover.md, TASKBOARD.md | AUDIT+CONTROL-PLANE | 2026-08-15 对公开仓库 `69755354/newme-platform` 执行只读 metadata 核对，确认仍有 2 个 `resolution=null` 的 open alerts：#1 `Supabase Personal Access Token`，历史位置为 `scripts/seed-products.ts`、`ARCHITECTURE_AUDIT.md`、`scripts/fix-lead-customer-name.ts`；#2 `Supabase Secret Key`，历史位置为 `migration-output/private-access-registry.md`、`OC-MIGRATION-BRIEF.md`。查询只读取编号、类型、路径、提交和行号元数据，从未请求或输出 secret 字段。工作树 redaction 和凭据门禁不能证明历史凭据已失效；在 Supabase/账户控制面完成撤销或轮换、正式更新全部消费者、以无密钥健康/连接探针验证、把 GitHub alerts 以 `revoked` 关闭并回读 open=0 之前，本项保持 predeploy blocker，不允许用 `false_positive` 或单纯关闭告警替代供应商失效证据。2026-08-20 在关闭之前先做供应商侧失效实测：alert #1 的泄露 PAT 对 `api.supabase.com` 的 `/v1/projects` 与 `/v1/organizations` 都返回 401 `{"message":"Unauthorized"}`，与一个同形状但从未签发过的 `sbp_` token 的响应逐字相同，而同一台主机的 `/api/v1-json` 在同一次运行里返回 200 —— 说明 401 是鉴权层不认识这个 token，不是主机整体拒绝我们；alert #2 的泄露 secret key 对项目 REST 根与 `/auth/v1/admin/users` 返回 401，而服务器当前在用的 key 对同一端点返回 200（阳性对照，没有它这套 401 证明不了任何事），且两者不相等。探针只读取 HTTP 状态码：凭据值只经 mode-0600 的 curl `--config` 文件传递，从未进入命令行参数、环境变量、shell 历史、日志或本文档。随后以 `resolution=revoked` 关闭两个告警，并回读 live API `open=0`（#1/#2 均 `resolved`/`revoked`，2026-08-19T23:52Z）。遗留清理项（不是暴露，值已死）：`/opt/newme/current/.env.local` 的 `SUPABASE_PAT` 与泄露值逐字相同且同样返回 401，说明这条消费者路径早已是死值，应在下一次凭据维护里删掉 | ✅ | GitHub live API 回读 2026-08-20 |
| PROD-DEPENDABOT-HIGH-ALERTS-OPEN | GitHub Dependabot, package.json, package-lock.json, pnpm-lock.yaml, scripts/check-supply-chain.mjs | AUDIT+MODIFY | Canonical main `56f3a22f1269eb2a23a9e3788e4520967998c191` 的 push CI run `31897791256` 已成功，Repository validation 完成 supply-chain、tests、anonymous smoke 与 build；随后 GitHub Dependabot live API 回读 `open_total=0`、`open_high=0`、high alert numbers 为空。未使用 dismiss 代替修复。 | ✅ | GitHub live metadata 2026-08-16 |
| PROD-CODEQL-BLOCKING-RULESET-MISSING | GitHub Code Scanning and repository rulesets, .github/workflows/ci.yml, infra/release/required-jobs.json, scripts/check-ci-provenance.mjs | AUDIT+MODIFY+CONTROL-PLANE | Canonical main `56f3a22f1269eb2a23a9e3788e4520967998c191` 的 push CI run `31897791256` 已成功；CodeQL analysis `1623801913` 精确绑定该 SHA、`results_count=0`。GitHub ruleset `20891786` 已回读为 `active`、`bypass_actors=[]`、仅 `refs/heads/main`、tool `CodeQL`、`alerts_threshold=errors`、`security_alerts_threshold=high_or_higher`；原五项 strict required checks 与 `enforce_admins=true` 保持不变。 | ✅ | GitHub live metadata 2026-08-16 |
| PROD-CREDENTIAL-RUNBOOK-TWO-PHASE | infra/release/credential-transition.md, infra/systemd/newme-deploy.sh, tests/release/credential-transition.test.mjs | AUDIT+MODIFY | 独立复核确认旧 runbook 首次在未安装 helper/policy/marker 的生产控制面直接调用 `credential-transition`，且错误声称切换后立即删除 inbox/pending/backup 并完成；真实 wrapper 必须先 `credential-trust-bootstrap`，再经盖章 policy 的 `credential-transition → credential-prove-revocation → credential-complete → credential-live-readback → credential-live-consume`，切换阶段只可停在 `awaiting_provider_revocation`。文档已改为精确两阶段顺序、保留/恢复边界及 signed finalizer 语义；契约测试同时钉住命令顺序、awaiting 状态和不得出现旧的一阶段清理声明。仅修运行手册与测试，未触发 GitHub、凭据、数据库、服务或部署写入 | ✅ | 2026-08-15 |
| PROD-CREDENTIAL-PROVIDER-RECEIPT-PRODUCER | scripts/credential-live-attestation.mjs, scripts/credential-live-five-stage-drill.mjs, infra/systemd/newme-deploy.sh, infra/release/credential-live-attestation-policy-v1.json, tests/release/credential-live-attestation.test.mjs, tests/release/credential-transition.test.mjs | AUDIT+MODIFY+CONTROL-PLANE | 正式 coordinator 现于同一 release lock 内先执行受保护 `materialize-provider`：从固定 root-only 路径读取旧身份材料，对 policy 盖章的 replacement object `d67a8b16-3a54-45b1-b457-eb6b7a3e2a9a` 调用精确 Management API object endpoint `reveal=true`，只接受同一 200 provider response 内逐字匹配的 project/object/type/raw key，随后生成 transaction/nonce 分域 HMAC claim、Ed25519 provider-identity receipt 与加密 sealed escrow，再幂等投影固定输入；凭据不进入 argv/env/stdin/stdout 或 release 文件。中断点覆盖 intent 与 sealed escrow 已落盘、claim/receipt/inbox 未投影两种状态；超过15分钟的重启会先用当前 exact-object readback与历史签名 escrow 逐字复核，再安全清理旧事务并生成新 claim，重入不沿用过期证据。2026-08-15 当前字节：live-attestation+transition 53/53；固定 `node:24.18.0-bookworm@sha256:5711…5059`、`--network none`、仓库只读、1 GiB/2 CPU/256 PID 的完整五阶段演练 35/35，PREPARED 过期/重启恢复演练 23/23，均 0 failures；production Management API 已创建并以 `reveal=false` 回读该 non-secret object ID/type，原始 key 未打印或落本地文件。policy 四组非秘密身份已盖章，receipt raw/SPKI pins 仍保持全零，因此 `check-policy` 当前按设计精确拒绝 `policy_receipt_trust_root_unstamped`，必须经首次受保护 bootstrap/status readback/后继 review commit 才能放行 cutover。本项代码与行为 producer 缺口关闭；未执行生产 bootstrap/cutover/revoke | ✅ | 2026-08-15 |
| PROD-LOGIN-LATENCY-SERVER-GRANT | src/app/api/auth/login/route.ts | CREATE | file exists + 含 `grant_type=password` + `isActiveProfile(profile)` 出现在 `applySessionCookies(` 之前（由 tests/security/session-revocation.test.mjs 断言）+ tests/security/auth-login-endpoint.test.mjs 17/17 pass | ⚠️ | 待部署 |
| PROD-LOGIN-LATENCY-SERVER-GRANT | src/app/login/page.tsx | MODIFY | 含 `fetch("/api/auth/login"` 且 **不含** `auth/v1/`、`access_token`、`NEXT_PUBLIC_SUPABASE`、`document.cookie =`（tests/security/sam15-boundaries.test.mjs）—— 3 次浏览器串行往返 → 1 次 | ⚠️ | 待部署 |
| PROD-LOGIN-LATENCY-SERVER-GRANT | src/proxy.ts | MODIFY | `PUBLIC_API_PATHS` 精确等于 `["/api/auth/login","/api/auth/logout","/api/auth/me"]`（解析式断言，非正则；tests/security/session-revocation.test.mjs） | ⚠️ | 待部署 |
| PROD-SESSION-COOKIE-CONTRACT | src/lib/session-cookies.ts | CREATE | file exists + `httpOnly: false` 仅用于 auth-token、`httpOnly: true` 用于 refresh-token、两者 `sameSite: "strict"` + `secure: true`；login 与 session 两个 route 均调 `applySessionCookies(` 且均不直接调 `cookies.set(`（sam15-boundaries.test.mjs 循环断言） | ⚠️ | 待部署 |
| PROD-AUTH-LOGIN-RATE-LIMIT | src/lib/rate-limit.ts | CREATE | file exists + `clientIdentifier` 优先读 `cf-connecting-ip`；验收：每账号 8/15min（大小写不绕过）+ 每 IP 20/5min + 429 带 `Retry-After` + 被限流请求**不转发上游**（auth-login-endpoint.test.mjs） | ⚠️ | 待部署 |
| PROD-AUTH-ME-CLIENT-DEDUP | src/lib/session-identity.ts | CREATE | `readSessionIdentity` 函数体**不含** `lastActive`（鉴权永不读缓存）；useAuthRedirect.ts 只用 `readSessionIdentity()` 且不用 `peekSessionIdentity`；PostHogProviderInner.tsx 不含 `fetch(`—— 挂载时 2 次 `/api/auth/me` → 1 次 | ⚠️ | 待部署 |
| PROD-L0-AUDIT-FIX-F07-F15-F25-F04 | src/app/api/users/[id]/password/route.ts | MODIFY | F-07：自改密码路径无 `updateUserById`（返 400 指向 change-password）；验证端 `signInWithPassword` 位置早于 `updateUserById`；无裸 `createClient(`（tests/security/password-change-session.test.mjs 4/4） | ⚠️ | 待部署 |
| PROD-L0-AUDIT-FIX-F07-F15-F25-F04 | src/app/api/kpi/targets/route.ts, src/app/api/cos/download-url/route.ts, src/app/(dashboard)/settings/page.tsx | MODIFY | F-15 / F-25 / F-07 客户端半边；`npm test` 0 fail + `check:security` 无超基线 | ⚠️ | 待部署 |
| PROD-L0-AUDIT-FIX-F07-F15-F25-F04 | .github/workflows/crm-ci.yml, .github/workflows/test-ci.yml | MODIFY | F-04：`npm run check:workflows` PASS 3/3 | ⚠️ | 待部署 |
| PROD-FALSE-GREEN-GATE-RETARGET | tests/security/{password-change-session,sam15-boundaries,sam15-cookie-only-session,session-revocation}.test.mjs | MODIFY | F-05 子项：原断言把**漏洞实现**和**3 次往返客户端舞步**钉成了必要条件。验收：断言已迁至属性新位置且**未被削弱**（新增：解析式精确 allowlist、gate-before-cookie 顺序、鉴权不读缓存） | ⚠️ | 待部署 |
| PROD-L0-DB-MIGRATIONS-F02-F06-F08-F09-F10 | supabase/migrations/20260811100*.sql, 20260806000000, 20260812000000, 20260813000000 | CREATE | 共 9 个迁移文件已写入但**未应用**。阻塞原因（2026-08-11 更新）：`supabase-prod` MCP 已断开且需交互式 OAuth，本会话无任何线上库通道；此前记录的 `--read-only` 只是同一阻塞的更早形态。已可离线证明的部分（三审后重跑）：`MODE=branch` 在一次性 `postgres:17.10` 上应用 → 幂等重入 → **131 条**行为/目录断言 → rollback 伴随文件 → **30 条 post-rollback 安全断言**全通；`MODE=control` 证明其中 **100 条**在未修复姿态下必失败，且 `100 + 31 = 131` 闭合（无游离断言、无"零断言通过"）。解阻：运维授权 MCP 或由运维本人应用迁移；`20260813000000` 另需执行角色对 `auth.users` 有 SELECT | ❌ | — |

> **PROD-LOGIN-LATENCY-SERVER-GRANT 根因**（用户报告"登录特别慢"）：旧 `src/app/login/page.tsx:67` 由**浏览器直连 GoTrue** 做 password grant —— 离开 Cloudflare 边缘、向 Auth 区付一次冷 TLS 握手，随后再串行 `/api/auth/session` 与 `/api/auth/me`，共 3 次串行往返。已测分层：Node 1.8ms / nginx+TLS 6ms / 过 Cloudflare 60-65ms / 生产→Supabase 45-48ms。现为 1 次浏览器往返（走已建立的边缘连接），grant + profile 鉴权在服务器侧走热连接。
>
> **安全是加强而非交换**：浏览器再也不接触裸 token；inactive profile 根本不会拿到 cookie，其刚签发的 token 在响应返回前已向上游 `/auth/v1/logout` 注销；上游失败文本（可能回引提交的密码）既不转发也不入日志，已由测试断言。
>
> **新增攻面已限定**：把鉴权收到服务端会把所有用户汇入单一 origin IP，把 GoTrue 的 per-IP 爆破保护塌缩成一个桶 —— 因此必须自带 PROD-AUTH-LOGIN-RATE-LIMIT 作为替代边界。**已知作用域限制**：计数器在单 Node 进程内存里；多进程部署必须改为共享存储。
>
> **本地实测（运行中构建）**：415 错误 content-type、403 外域 origin、403 伪造 `X-Forwarded-Host`、503 未配置 fail-closed、405 GET，且**任何被拒请求零 `Set-Cookie`**。
>
> **本次未做（用户明确暂缓，需本人点头）**：仓库转 private、git 历史清除 `deploy-backup-*.json`/`.next.backup/`、origin 防火墙限定 Cloudflare 段、凭证轮换。

---

## L0 独立复审收口（PR #397 maintainer comment）— 2026-08-11（Claude）

> 事实源：代码看 git，生产事实只采用本轮获授权的 Supabase **只读** schema catalog 与 migration-history capture。已采集 schema-only catalog、100 行 migration metadata 及服务端指纹；未采集 statement 正文、密钥或业务行，未对生产执行迁移、数据写入、部署、重启/reload 或控制面修改。
>
> 复审意见按“原始证据”对待而非结论：逐条复现后分为 confirmed / refuted / insufficient-evidence，见本节末尾。

| # | File | Operation | Verification | Status | Done Date |
|---|------|-----------|-------------|--------|-----------|
| PROD-L0-MIGRATION-REPLAY-GATE | scripts/replay-migrations.sh, supabase/replay/production-schema-baseline.sql, supabase/replay/production-schema-baseline.json, supabase/replay/*.sql, .github/workflows/ci.yml | MODIFY+CREATE | 空库历史门禁已改为认证只读采集的 production schema-only baseline（0 业务行）+ exact release manifest 的 25 个 forward-only pending migrations，不再把“在第 2 个旧迁移停住”当成功期望。fresh PostgreSQL 17 的 `MODE=history` 与 `MODE=branch` 均 rc=0：25 个迁移 apply+reapply、342/342 release assertions、2 个 rollback companions、54/54 post-rollback、1 次 recontract 重放两遍、18/18 post-recontract；`MODE=control` rc=0，342 个断言精确分为 283 个 load-bearing FAIL + 59 个 floor PASS，0 unclassified。CI 相关 job 不允许 `continue-on-error`，并由 baseline/history/release contract tests fail-closed；本地实现与数据库行为已验证，仍需 exact-head required CI 关闭发布证据 | ⚠️ | 待 exact-head CI |
| PROD-MIGRATION-HISTORY-UNREPLAYABLE | supabase/replay/production-schema-baseline.sql, supabase/replay/production-schema-baseline.json, scripts/replay-migrations.sh, tests/release/history-schema-baseline-contract.test.mjs | CREATE+AUDIT | **已关闭且未重写任何已应用迁移字节**：通过认证的生产只读 schema catalog 捕获生成 schema-only baseline，watermark=`20260805202917`，覆盖 37 tables / 535 columns / 13 views / 44 functions / 164 constraints / 134 non-constraint indexes / 18 triggers / 264 policies；baseline 明确为 0 业务行，扫描未发现 preflight 或 secret 内容。baseline SQL/metadata/hash 绑定与 tamper、row-DML、credential、capture、reconciliation、manifest 双向负测均 fail-closed。fresh PostgreSQL 17 从空库恢复 baseline 后，exact 25 个 pending migrations apply+reapply，并通过 342/342 release、54/54 post-rollback、18/18 post-recontract 断言；`MODE=history` rc=0 | ✅ | 2026-08-15 |
| PROD-DEPLOY-RELEASE-CLAIM-VALIDATION | scripts/deploy-immutable.sh, infra/systemd/newme-deploy.sh | MODIFY | `validate_release_claims()` 在任何 mkdir/symlink/服务动作之前 `exit 64`：要求 6 个声明变量全部非空、`CI_RUN_ID` 为数字、`CI_RUN_URL` 必须指向同一 run id 的 github.com 路径、`CI_HEAD_SHA == $SHA`、`CI_CONCLUSION == success`、`CI_EVENT ∈ {push, workflow_dispatch}`、`applied_verified` 必带合法 `MIGRATION_IDS` 而 `not_required` 必不带。canonical wrapper 的 GitHub API 校验加入 `event=push` 与 `head_branch=main`（原先任一分支的绿色 `pull_request` run 都被当作 main 证据，而 release-final 作业以 `github.event_name` 为条件 → 更小的门禁集被记成完整门禁）。验收：tests/release/deploy-release-claim-validation.test.mjs 11/11，**直接执行**被抽出的 shell 函数与 wrapper 内联校验块（含 manual_verified 旁路被拒） | ⚠️ | 待部署 |
| PROD-CI-CRM-HERMES-FALSE-GREEN | .github/workflows/crm-ci.yml | MODIFY | `hermes-contract` job 与 Telegram 失败告警均以 `workflow_run` 为条件，而该 workflow 的触发器只有 workflow_dispatch/pull_request/push —— 于是每次 PR 与 main push 都跳过唯一 job 并报 success，失败告警永不可能执行。触发器改为 `workflow_dispatch` + `workflow_run(workflows: [ci], types: [completed])`，`pull_request`/`push` 删除而非保留。验收：`npm run check:workflows` 3/3。**观测边界（本轮无法闭环）**：GitHub 只从默认分支读取 `workflow_run` 触发器定义，且本 job 另有 `head_branch == 'main'` 条件 —— 故本 PR 的 `ci` 成功（run 31457465666）不会、也不应触发 `crm-ci`；本轮 `gh run list --workflow crm-ci.yml` 对 SHA `c59d687` 无任何 run，即为预期结果而非回归。真实证据只能在合入 main 后的首次 main push 上取得（`crm-ci` workflow_run 成功 + Hermes delivery 200），见 line 270 | ⚠️ | 待合入 main 后观测 |
| PROD-L0-OPEN-REDIRECT-SESSION-CHAIN | src/lib/safe-redirect.ts, src/app/login/page.tsx | CREATE+MODIFY | `?redirect=` 未净化即为开放重定向，且 `sb-<ref>-auth-token` 为脚本可读，跳转目标能取得刚建立的会话上下文。验收：tests/unit/l0-auth-hardening.test.mjs 中 `safeRedirectPath` 断言拒绝任意 scheme 绝对 URL（含 `javascript:`/`data:`/`vbscript:`/`file:`）、`//evil`、`///evil`、`/\evil`、`\\evil`、控制字符、非字符串、空白、超长（>512），保留同源 path+query+hash，兜底 `/dashboard` | ⚠️ | 待部署 |
| PROD-AUTH-LIMITER-EVICTION-BYPASS | src/lib/rate-limit.ts | MODIFY | 原实现用 `Map` + `MAX_TRACKED_KEYS=10000` 上限，攻击者只要发足量不同 key 就能把自己的计数条目挤掉从而清零限流。第一轮固定 `SLOT_COUNT=16384` + sha256 分槽消除了淘汰，但独立复审又真实复现一条跨策略碰撞：5 分钟 IP policy 与 15 分钟 account policy 若落入同一 active slot，后者会继承更短 reset，故“碰撞只更严格”不成立。最小修为每个 slot 同时记录 `limit`/`windowMs`；active slot 的 policy 不同即拒绝到当前 slot 到期，绝不按错误的短窗口授予请求，计数仍饱和不回绕。行为负测从公开 API 在有界候选集内找出真实 collision，修复前 **1 条失败**、修复后该文件 **34/34 PASS**，目标 diff-check PASS；既有 40000-key 不淘汰、1MB key、窗口边界和持续洪泛断言仍保留。未提前 DONE，仍待 exact-head CI 与生产验收 | ⚠️ | 待部署 |
| PROD-RELEASE-SCRIPT-FAIL-CLOSED | src/lib/release-script.ts | CREATE | 上一版对空输入 fail-open 返回 `process.cwd()`，且用前缀包含判断而非 `path.relative` 归一。现拒绝空串/纯空白/非字符串/绝对路径/含 `..` 段/逃出仓库根/目录（`statSync().isFile()`）。验收：tests/unit/l0-auth-hardening.test.mjs 断言 `""`、`"   "`、`"."`、`"./"`、`"scripts"`、`"scripts/"`、`/home/ubuntu/newme-platform/scripts/cos-presign.py`、traversal 全部返回 `null`，`scripts/replay-migrations.sh` 正常解析 | ⚠️ | 待部署 |
| PROD-KPI-TARGETS-ATOMIC-REPLACE | src/app/api/kpi/targets/route.ts, supabase/migrations/20260811100500_kpi_targets_atomic_replace.sql | MODIFY+CREATE | 原为两次 PostgREST 调用即两个事务：先 `delete().eq("period")` 再 `insert(rows)`。第二步任意失败（`target_type` CHECK、`NUMERIC(12,2)` 溢出、未知 `assigned_to`、连接中断）都会留下**已提交的删除**与空白周期，且无副本无恢复路径 —— settings UI 一行畸形数据即可清空整月目标。改为单事务 SECURITY DEFINER `replace_kpi_targets(p_period, p_rows, p_set_by)`（`search_path` 固定、仅 `service_role` 可 EXECUTE）。验收：replay 断言 `kpi-fixture-period-seeded` / `kpi-failed-replace-preserves-period`（坏 `target_type` 回滚后既有 2 行仍在）/ `kpi-empty-replace-preserves-period` / `kpi-successful-replace-replaces-period` / `kpi-authenticated-cannot-execute` / `kpi-anon-cannot-execute` | ⚠️ | 待部署 |
| PROD-QUOTATION-CONVERT-ATOMICITY | src/app/api/quotations/[id]/convert/route.ts | MODIFY | 两个缺陷。**并发**：`quote.contract_id` 只是读，写在请求最末，两个并发 POST 都读到 null → 同一报价生成两份合同、两份审批、两个项目、lead 被判定 won 两次。现改为先做条件更新抢占（`.eq("status","accepted").is("contract_id", null)`），以匹配行数为互锁，输者返 409；后续任一步失败则 `releaseClaim()` 把状态放回 `accepted`，避免永久卡在 `contract_created` 而无合同。**确定性失败**：合同号序号原先用调用者 RLS client 计数，`policy_contracts_select_sales` 只让销售看到自己的行，因此 count 漏掉同事当天的合同 → 生成已存在的号 → `contract_no` UNIQUE 触发 23505 → 500；非 admin 只要当天有他人签约就必然失败。现用 `supabaseAdmin` 计数并对 23505 重试（`MAX_CONTRACT_NO_ATTEMPTS=10`）。验收：`npm test` 0 fail + check:security 无超基线 | ⚠️ | 待部署 |
| PROD-COS-SCRIPT-RELEASE-DRIFT | src/app/api/cos/download-url/route.ts, src/app/api/contracts/[id]/upload-url/route.ts, src/app/api/dashboard/ads-roi/import/route.ts | MODIFY | 三处外部脚本调用原先按绝对/固定路径找 `scripts/cos-presign.py`、`scripts/parse-ad-spend.py`，与 immutable release 树漂移（回滚后仍指向新脚本，或指向 release 之外的路径）。改为经 `resolveReleaseScript()` 在**当前运行 release 内**解析，解析失败即 fail-closed；子进程环境只传脚本实际读取的变量，不再整体 spread `process.env` | ⚠️ | 待部署 |
| PROD-AUTH-OLD-TOKEN-REVOCATION | src/app/api/auth/change-password/route.ts, src/app/api/auth/logout/route.ts, src/app/api/auth/me/route.ts | MODIFY | `password_changed_at` 只由请求 token 的 `iat` 判定，改密前签发的 **refresh token 仍能换出 iat=now 的新 access token** 从而通过 proxy 门禁。change-password 现调 `supabaseAdmin.auth.admin.signOut(..., "global")`；logout 原先丢弃 `signOut()` 结果并一律返 `{ok:true}`，现用 `scope:"global"` 并以 502 + `revoked:false` 区分未真正吊销；`/api/auth/me` 的 refresh 路径补上 `password_changed_at` 比对 | ⚠️ | 待部署 |
| PROD-F09-MONEY-AUTHORIZATION-PHASE2 | supabase/migrations/20260812000000_money_actor_identity_and_atomicity.sql, src/app/api/contracts/**, src/app/api/quotations/[id]/convert/route.ts, src/lib/money-rpc.mjs | CREATE+MODIFY | 三审确认的缺陷：例程相信调用方传来的 approver/confirmer/allocator id，而 EXECUTE 已授予全部 `authenticated`。本轮 `money_actor(p_claimed, p_allowed_roles)` 以 **JWT subject 为唯一 actor**（参数不符即 42501）并校验 profile 存在/`is_active`/角色；五个 `trg_guard_*`（**故意 SECURITY INVOKER** —— 判别式 `money_write_is_direct()` 读 `current_user`，改 DEFINER 会让它对所有人放行）拒绝以 `authenticated`/`anon` 到达的直写；新增 `create_contract`/`convert_quotation_to_contract`/`set_contract_status`/`revoke_contract` 单事务例程；`allocate_payment` 绑定分期到该付款自己的合同。五个 route 改为只调例程，SQLSTATE→HTTP 由 `src/lib/money-rpc.mjs` 单点映射（未映射码一律 500 且不回引数据库消息）。验收：`tests/security/money-route-rpc-coupling.test.mjs` 23/23（含实参↔形参核对、UI 转移表与 `set_contract_status` 双向相等、未映射码不泄消息）+ replay 的 `money-*` 断言。**四审 P1-1 / P1-9（本轮新增，两条均先复现后修）**：(1) P1-1 —— `profiles.role` 可为 NULL（`profiles_role_check` 对 NULL 恒真，生产已可持有此行），而角色判别若写成 `not (v_role = any (array[...]))`，在 NULL 下整式求值为 NULL 而非 true，`if` 不进入分支即**放行**（fail-open）。真实 Postgres 17 容器、已应用迁移的库、事务回滚，逐一复现：把 round-2 的成员判别函数体装回 → `sqlstate=00000 accepted=t message_names_the_null_role_boundary=f`；本轮 `money_actor` → `sqlstate=42501 accepted=f message_names_the_null_role_boundary=t`。(2) P1-9 —— 结算权限在四处互相矛盾：route 与 RBAC 文档写 admin/boss/finance，payments 页把 Confirm/Allocate 按钮开给 `operator`，而 `20260812000000_money_actor_identity_and_atomicity.sql:606` 与 `:709` 的 `confirm_payment`/`allocate_payment` role list 实为 `array['admin','boss','finance','operator']` —— 于是"按钮点了像没反应"的 operator 只要直接调 RPC 就能结算真钱。现以数据库为权威收敛到 admin/boss/finance，UI 用 `SETTLEMENT_ROLES` + `canSettle` 对齐（`isPrivileged` **保留**，它是"录入付款/查看全部付款"的正确规则，本轮只把两个结算动作移出它）。复现：把该迁移 585–683 行的 round-2 `confirm_payment` 原样装回已迁移库并以 operator 探测 → `ROUND-2 sqlstate=00000 confirmed=t`；本轮 → `SHIPPED sqlstate=42501 confirmed=f message_names_the_operator_role=t`。验收（可执行，非源码断言）：replay 新增 K7 段共 **18 条**断言，`ASSERT_TOTAL: 221 → 239`（声明处与库内 ledger 自检四处同步）；两个新 fixture 身份（`0a0a…` operator、`0b0b…` role=NULL 且 `is_active=true` —— 刻意让拒绝只能归因于缺角色而非停用）+ 两个专用 lead（`0c0c…`/`0d0d…`：探测须经 `create_contract()` 现造一份 pending_admin 合同，因为 fixture 里的 `c1c1…` 早已被前面章节推进到 `approved`，借用旧 lead 又会撞 `idx_contracts_one_active_per_lead` 变成 23505 空断言；另有两条 setup 非空断言把这种空转判红）；无角色身份被 `confirm_payment`/`allocate_payment`/`void_payment`/`create_contract`/`approve_contract`/`convert_quotation_to_contract`/`set_contract_status`/`revoke_contract` **八条路径**分别以 42501 且消息**指名该边界**拒绝，operator 被结算三例程拒绝、同时仍能 `approve_contract` 把 pending_admin 推到 `pending_ceo`（正控制：证明"被拒"不等于"operator 什么都做不了"）；每组拒绝前后比对 `contracts/payments/confirmed/allocations/approvals/plans` 六项计数签名逐字相同，且读回一律在 `reset role` 之后（否则 RLS 会把读回变成空断言）。三模式实测：`MODE=branch` → 11 migrations / 239 release assertions / 47 post-rollback，`ASSERT_LEDGER total=239 passed=239 failed=0`，18 个 K7 marker 全 `ASSERT_OK`；`MODE=control`（未打本轮迁移的 floor）→ `239 assertions, 190 load-bearing, 49 floor-passing, 0 unclassified SQL errors`，K7 中 14 条在 floor 上如实判红（含 operator 正控制 —— 旧 `approve_contract` 对不存在的合同 id 也返回成功，故该断言额外要求 `v_moved='pending_ceo'` 才不假绿）；`MODE=history` → `APPLIED=2 / TOTAL=113 / STOPPED_AT=20260602010000_crm_mvp_final.sql`（既有钉住点未动）。UI↔DB 耦合另由 `tests/security/money-grant-coupling.test.mjs` 第 4 例守住（`SETTLEMENT_ROLES` 必须等于三个结算例程**最后一次定义**的 role list —— 按文件名序取末次定义，因为那才是按序应用后库里装着的那份；两个结算按钮上方的 guard 必须含 `canSettle` 且不含 `isPrivileged`），两半均经变异验证（分别改 `SETTLEMENT_ROLES` 与把按钮改回 `isPrivileged`，各自判红后已还原，`git status` 确认页面无改动）。**未关闭**：迁移未应用；应用后任何仍以调用者 client 直写资金表的路径会立刻 42501；`on_lead_won()` 由 active 改 draft 需登录态 UAT | ❌ | — |
| PROD-F02-DEV-SETUP-CREDENTIAL-REMOVAL | src/lib/dev-identity.mjs, src/app/api/dev/setup/route.ts, src/app/api/auth/dev-login/route.ts, scripts/check-published-credentials.mjs, supabase/preflight/f02-credential-cutover.md, docs/employee-readiness-20260624.md, docs/onboarding-guide.md, docs/onboarding-guide-en.md, docs/context-pack/flight-recorder-phase0.md, docs/context-pack/11-tanya-feedback-raw.md, migration-output/company-profile.md, crm-v3/ops/HANDOFF-20260701.md, OC-MIGRATION-BRIEF.md, test-matrix.md, test-matrix-runner.mjs, test_matrix.py, .gitignore, tests/security/dev-identity-bootstrap.test.mjs, tests/security/published-credentials.test.mjs | CREATE+MODIFY | 四审 A0。**上一轮把范围写小了，特此更正**：审计只记了 `src/app/api/dev/setup/route.ts` 一处硬编码口令；按同一形状把全仓翻一遍，实际是**八个身份 + 生产数据库口令 + 一条 Supabase PAT 提权配方、十二个文件、二十处**明文发布（其中十五处是逐个文件读出来的，另五处只有把门禁范围改成“恰好等于 git ls-files、不排除任何东西”之后才暴露 —— 范围才是缺陷，正则不是）。 最严重的一处与应用完全无关：`crm-v3/ops/HANDOFF-20260701.md` 与 `crm-v3/v3.1/v3.1 P1P1计划0629.txt`（两处）把**生产 Supabase 数据库口令**贴在一条能跑的命令行里（`supabase migration list --linked --password <值>`）。它不是应用身份，`is_active`、`force_password_change`、`20260813000000` 的 RLS 会话边界、GoTrue 封禁**对它一概无效**，直连 SQL 绕过 §2.2 的全部 policy。因此轮换它是 §3 的第 0 步，排在封禁之前。 另六个身份（boss/operator/admin/sales×3）散在 `docs/employee-readiness-20260624.md`（六行表格 + 一行中文“今天临时密码：”）、`docs/context-pack/flight-recorder-phase0.md`（`| Password | … |` 的 Field/Value 行）、`docs/context-pack/11-tanya-feedback-raw.md`、`migration-output/company-profile.md`、两份 `docs/onboarding-guide*.md`；其中**五个身份共用同一个值**，泄一个等于泄五个。 **本轮只动代码与文档，不碰生产**：(1) 两条 bootstrap route 改由 `src/lib/dev-identity.mjs` 统一解析身份，**没有任何默认值** —— 生产、未显式 opt-in（`ALLOW_DEV_IDENTITY_BOOTSTRAP`，刻意**不用** `NEXT_PUBLIC_*`：那类变量在 build 期被内联进浏览器包，原 `/api/dev/setup` 的 `NEXT_PUBLIC_DEV_MODE` 门禁其实是个发给所有浏览器的构建产物）、未配置、地址不合法、口令短于 16 位，逐一返回**理由码**（不是值）+ 403/503；原 `process.env.DEV_PASSWORD || "<字面量>"` 这种“看着像配置、其实每个没配的环境都在用 git 历史里那个口令”的写法已删除。(2) `/api/dev/setup` 不再对已存在的身份重设口令 —— 那让一个 bootstrap 端点变成了没有鉴权检查的改密端点。(3) 二十处全部就地打码，每处都写明“打码不等于修复”并指向 preflight §7。(4) 新增 `scripts/check-published-credentials.mjs` 作为**必跑门禁**（`git ls-files` 全量范围、九条规则、只报路径与规则名**从不报值** —— 会打印命中的门禁等于把密钥重新发布到每一次 CI 日志里）。 **顺带纠正一处失实文档**：两份 onboarding guide 写着 Tanya/Ayana 能在“团队”页**看到**所有销售的密码。核对 `src/app/(dashboard)/team/page.tsx`（只有 `type="password"` 输入框与 reset 动作）与 `src/app/api/users/route.ts`，页面既不显示也不存明文，已改为“**重置**”。 验收：`tests/security/dev-identity-bootstrap.test.mjs` 13/13 —— 五种拒绝逐一断言、拒绝理由码里不含 `DEV_PASSWORD`/`DEV_EMAIL` 的值、未配置时两条 route **完全没有触达 Supabase**（记录式 double 清点调用为空）、已存在身份的 update 参数里没有 password 键、Supabase 未配置返 503 而不是把 `!` 断言抛成堆栈；`tests/security/published-credentials.test.mjs` 19/19 —— 用**形状**（无一个真实值）重建每一处真实存在过的发布site并要求判红，其中四条是本门禁早期版本真的漏过的缺陷（CJK 标签无 ASCII 词边界、反引号包裹的值被当占位符、只看标签后**第一个 token** 否则 `Password: <值>（首次登录后修改）` 会被当成句子、命令行 `--password <值>` 根本没有标签），另一半要求 `| Check | PASS |`、`| Key | Value |`、路由清单里写着 `password` 的单元格保持干净（第一版就是被这些淹了 39 条噪声）；CI 契约由 `tests/release/ci-full-stack-gates-contract.test.mjs` 守住门禁 + 反向回归两步都在 `validate` job 内，并断言 finding 对象**没有能装值的字段**。 **Batch 0（管理层复审后的第二轮）**：复审指出这道门禁是假绿的。核对成立，而且比复审说的更宽 —— 假绿有三个彼此独立的成因。(a) 门禁的 `SKIP_PREFIX` 把 `.next.backup/` 整个目录排除在扫描之外，而那里躺着 **1634 个被 git 跟踪的构建产物**，其中两份 `.js.map` 的 `sourcesContent` 里仍带着 `/api/dev/setup` 早就删掉的那个明文口令 —— 源码改了，快照没改，生成物是一份更旧的源码的切片。(b) sourcemap 里每个引号都是 JSON 转义的，任何面向源码的正则都匹配不过转义，因此 `.map`/`.json` 现在先解转义、再按真实行号判定。(c) `OC-MIGRATION-BRIEF.md` 是**带行号栏**保存的（每行以自己的行号加一个竖线开头），于是没有任何一行以竖线开头，这个文件在门禁眼里既没有表格行、也没有表头，更没有口令列。(a) 的根因在 `.gitignore`：它写的是 `.next.backup.*`（带点），永远匹配不到目录 `.next.backup/` —— 少一个斜杠，就把一次构建备份提交进了仓库。已修：扫描范围改成**恰好等于 `git ls-files`，不再排除任何东西**（978 个跟踪文件、17.9MB，其中 9 个含 NUL 的按二进制跳过并计数；`logs/pm2-*.log` 这 5.5MB 生产日志也是第一次被扫到），1634 个产物 `git rm --cached` 移出索引并补上 `.gitignore` 的斜杠，“**跟踪了构建产物**”本身成为一条结构性 finding（不读一个字节就能报），规则五条增到九条（新增 `credential-pair`、`credential-in-url`、`credential-property`、`tracked-build-output`），测试 11 条增到 19 条，并给行号栏与 JSON 转义各配**一个变异对照** —— 把被替换掉的那个旧判定原地重写一份，断言它在同一夹具上返回 `[]`；否则哪天夹具失效，测试会静默变成永真。范围一改，又多出四处**非生成物**的发布：`test-matrix-runner.mjs:10-12`、`test-matrix.md:4-6`、`test_matrix.py:29,49-52`、`OC-MIGRATION-BRIEF.md:53-54`。它们带出**第八个身份 `admin@newme.ae`**，并且 tanya/faheem/mohamed 三个身份**各有不止一个已发布的值**，所以轮换要按身份算、不按值算。`test_matrix.py` 另外把一条完整的 Supabase PAT 提权配方写在可执行位置上，**PAT 轮换因此成为一项新增的未授权生产动作**。另有一条只有对着 commit 跑才暴露的事：门禁读的是 `git ls-files`，而新写的 `tests/security/dev-identity-bootstrap.test.mjs` 当时还没被跟踪，所以工作区里那个 OK **根本没扫到它** —— 对着提交后的树再跑，它自己的两处夹具立刻判红。方向是对的（CI 读的是提交，提交才是“已发布”），但**本地一次 OK 只覆盖 git 已经跟踪的东西**。两处夹具按标识符加进 ALLOWLIST 并写明理由，同时新增一条测试：ALLOWLIST 里每个路径都必须仍被 git 跟踪、每条都必须有理由 —— 豁免活得比文件久，是门禁悄悄失效的典型方式。最后一句必须写清楚：`git rm --cached` 只是让它停止被跟踪，**不是抹除**，这 1634 个文件仍然在 git 历史里。 **未关闭（生产动作，均需单独授权）**：§7.2 生产数据库口令轮换（第一优先，验证方式是拿轮换后的凭据跑 §2.5 的 `verify-remote-migration-history.mjs` 要 `OK` —— 只读迁移元数据，任何日志里都不会出现密钥）、§7.1 六个员工口令轮换（经应用重置路径即可连带吊销会话，见 `PROD-AUTH-ADMIN-RESET-GLOBAL-REVOCATION`）、§4 Auth 封禁与会话吊销、Supabase PAT 轮换（`test_matrix.py` 已把提权配方发布出去）、仓库转 private、git 历史清除。**这二十处已发布的值必须一律视为“任何 clone 过本仓、或读过本仓任一次构建产物的人都已知”，把它从工作区删掉与让它停止生效是两件事，本次发布不得把前者说成后者。** | ⚠️ | 待轮换+封禁（生产动作未授权） |
| PROD-AUTH-ADMIN-RESET-GLOBAL-REVOCATION | src/app/api/users/[id]/password/route.ts, src/app/actions/team.ts, supabase/migrations/20260817120000_admin_reset_session_revocation.sql, supabase/replay/00_platform_bootstrap.sql, scripts/gotrue-revocation-drill.sh, tests/security/admin-reset-session-revocation.test.mjs | CREATE+MODIFY | 四审 A3。**本行原先的说法“refresh token 只能由 GoTrue 吊销、数据库层无法关闭”已被实测推翻，特此撤回**：`auth.sessions` 与 `auth.refresh_tokens` 就是普通表，删掉某身份的 session 行会连带带走它的 refresh token。本轮把这件事做成**可验证的后置条件**而不是继承来的副作用：新增 `public.revoke_user_sessions(uuid, text)`（SECURITY DEFINER、pinned search_path、definer 入口断言、`revoke all … from public, anon, authenticated` 后只 `grant execute … to service_role`），它先删两张表的目标行、**再回查确认为 0**，写服务端自有审计行，否则 raise；两条管理员重置路径（route PATCH 与 server action `resetUserPassword`）按 password → `password_changed_at`+`force_password_change` → RPC 顺序调用，且**只有 `verified: true` 才算成功**（未装/无权/返回 null/形状不符 → 502 或 throw，profiles 写失败则根本不调 RPC 并回报“对方仍处于登录状态”）。证据：replay 17 条 `a3-*` 断言在 branch 模式全 `ASSERT_OK`、在 control 模式对未修复地板全部 `ASSERT_FAIL`（`control marker accounting OK: 323 assertions, 265 load-bearing, 0 unclassified SQL errors`）；`tests/security/admin-reset-session-revocation.test.mjs` 13/13，含两条 mutation 测试（删掉 fail-closed 守卫或删掉 RPC 调用后结果必须翻转）；GoTrue 侧行为由 `scripts/gotrue-revocation-drill.sh`（v2.195.0 + supabase/postgres 17.6.1.158，一次性容器、每次运行独立随机口令、身份全为 `@drill.invalid`）实测：admin 改密本身已使 sessions 1→0，重置前的 refresh token 返回 400 `refresh_token_not_found` 且换不出新 access token；单独删 session 行同样使 refresh token 1→0 并杀掉三个并发会话的全部 token；新口令登录 200、旧口令 400。**残留（已在制品中写明）**：已签发的 access token 对 GoTrue 是 403，但对 PostgREST 仍在有效期内可用 —— 由 `20260813000000` 的 `session_token_is_current()` 限制性策略以 `iat` 对比 `profiles.password_changed_at` 覆盖；生产 GoTrue 版本刻意未读取；生产 DELETE 权限无法在离线夹具中证明，故在迁移 apply 时断言。**R2 追加并撤回本行一处说法**：原先写的“profiles 写失败则根本不调 RPC”是缺陷而不是设计 —— 到那一步管理员已经把目标口令换掉了，于是「目标现存会话最确定该被清掉」的那个分支，恰好是唯一不清的分支。两条路径现改为：profiles 更新失败照样调 RPC，先按未撤销（更危险）后按时间戳未记录分别回报（route 502 / 500，action 两条不同 throw）；`/api/auth/change-password` 同理，全局 signOut 移到 profiles 失败回报之前，并按 `sessionsRevoked` 给两种文案。R1 追加：`resetUserPassword` 的调用方角色改由 `getActionAuthContext()`（调用方自己的 RLS 客户端）解析而非 service key，并**移到构造 service-role 客户端之前**。证据：`tests/security/admin-reset-session-revocation.test.mjs` 16/16，含新增变异测试「把 pre-R2 提前返回插回去」—— 插回后两条路径的 RPC 调用数均为 0（证明这个顺序是承重的），以及 forced/revoked 调用方在触达 service-role 客户端前即被拒（calls 为空数组） | ⚠️ | 待部署 |
| PROD-FORCED-PASSWORD-CHANGE-SERVER-ENFORCEMENT | src/lib/forced-password-change.mjs, src/proxy.ts, src/lib/request-auth-context.ts, tests/security/forced-password-change-boundary.test.mjs | CREATE+MODIFY | 四审 A2：`profiles.force_password_change` 此前只是**客户端约定** —— 由 admin 重置路径写入、由 `/api/auth/login` 返回、由浏览器自行跳 `/change-password`；服务端无任何一处读它。忽略跳转（或根本不跑页面、只持 token）的调用方可达全部已认证路由，**包括 service-role 的重置密码路径**。本轮把该标志变成服务端边界：唯一豁免清单放在 `src/lib/forced-password-change.mjs`（3 个 API + 2 个页面：改密、看自己、走人），由两处强制点共同消费 —— 边缘 `src/proxy.ts`（API 返 403 `password_change_required`，页面跳 `/change-password?reason=…`，cookie 与 Bearer 两条 profile 读取均补 `force_password_change` 列）与 `getRequestAuthContext()`（新增 403 码；只有显式 `{ allowForcedPasswordChange: true }` 的调用方能退出，当前**无任何 route 使用**）。**顺带修掉一个既有洞**：`/payments`、`/tasks`、`/tasks/[id]`、`/workbench` 四个 `(dashboard)` 页面从不在 `config.matcher` 里，故边缘检查（本轮的强制改密、以及**更早的 `is_active` 吊销边界**）对它们从未运行；server action 以 POST 打到页面自身路径，未列出的页面同时是未检查的 action 入口。验收（行为测试，非源码断言）：`tests/security/forced-password-change-boundary.test.mjs` 9/9 —— 27 条 service-role route 全量清点后**逐条**以 GET+POST 过 proxy 判 403（豁免清单里只允许 `/api/auth/change-password` 一条重叠，且断言 `/api/users/[id]/password` 仍在清点结果内）、cron/webhook 外部入口带 forced cookie 同样被拒、Bearer 路径断言 REST select **确实含**该列、`(dashboard)` 全部页面必须被 matcher 覆盖（此断言即发现上述四页）、`inactive_account` 优先级不被新码掩盖、豁免页面仍可渲染。变异验证四项（禁用 proxy 门禁 / 禁用 context 门禁 / Bearer select 去掉该列 / matcher 去掉 `/tasks/:path*`）各自判红后还原，CRLF 与字节经 `sha256sum` 复核。本地门禁：`typecheck` clean、`lint:baseline` PASS（406，无新增）、`check:supabase-boundaries` PASS（107 findings 未超基线）、`npm test` 590 tests / 587 pass / 0 fail / 3 skipped。**R1 追加：第三个入口。**本行原先只关闭了 proxy 边缘与 `getRequestAuthContext()` 两处；server action 是第三处，且此前没有任何一处读 `force_password_change` 或 `is_active` —— 每个 action 各自 `createServerSupabase()`、各自读 `profiles.role`。边缘确实顺带覆盖了它们（action POST 到页面自身路径，而 `(dashboard)` 全页面已在 matcher 内），但 matcher 是路由声明而不是授权边界，距离这句话变假只有一个 matcher 条目。新增 `src/lib/action-auth-context.ts` 作为唯一收口（一次 profile 读，`Unauthorized` / `Profile not found` / `inactive_account` / `password_change_required` 四种失败），7 个 action 模块 16 个导出 action 全部改由它解析；唯一豁免是 `getCurrentUser()`（action 形态的 `GET /api/auth/me`，`/change-password` 页面渲染所需）。验收（行为测试）：`tests/security/forced-password-actions-boundary.test.mjs` 8/8 —— 逐个 action 以 forced / revoked / 无会话 / profile 行不存在四种身份实跑真实模块，断言拒绝码正确且日志里除那一次 profiles select 外**什么都没发生**（无 insert/update/rpc、未触达 service-role 客户端、未 revalidatePath）；已清空标志的会话作为负对照必须越过门禁；清单断言 use-server 指令只存在于 `src/app/actions/`、每个导出 action 体内必须触达收口、无 action 自行 `createServerSupabase(`、除 team.ts 外无 action 再读 profiles、收口的 select 必须含这六列。变异两项：把收口的 forced 分支关掉 → forced 测试判红；把 `assignLead` 改回自解析会话 → 四个测试同时判红。本地门禁：`npm test` 850 tests / 847 pass / 0 fail、`typecheck` clean、`lint:baseline` PASS（402，无新增） | ⚠️ | 待部署 |
| PROD-CONTRACT-REVOKE-LIST-DIRECT-WRITE | src/app/(dashboard)/contracts/page.tsx, src/app/actions/contracts.ts, tests/security/contract-revoke-boundary.test.mjs | MODIFY+CREATE | 四审 B1：合同列表页的 Revoke 按钮走 `revokeContract` server action —— 先从 profiles 读角色，再以调用者自己的 client 直写 `update contracts set status='revoking'`；合同详情页走的却是 `POST /api/contracts/[id]/revoke` → `revoke_contract()`。两个入口做的是两件事。隔离 PG17 复现（仅**已提交**迁移 10 个 + `01_floor_schema.sql` + `05_seed_behaviour_fixtures.sql`，以 `authenticated` 并注入 GoTrue 同形 claim，每个子例各用专属 lead/合同以免继承上一例状态）：直写以 **boss** 身份 compat `sqlstate=00000 status→revoking`、strict `42501 未变`；**同一条直写以 sales 身份 compat 下同样 `00000` 且 `status→revoking`** —— admin/boss 规则只活在该 action 的那次 profiles SELECT 里，数据库从未持有它，且直写同时绕过 `revoke_contract()` 的 `for update` 与 `contract_transition_is_allowed()`；`revoke_contract()` 则两模式一致：boss `00000`（`previous_status` active→revoking）、sales `42501`。即该 action 在 strict 下按钮**完全失效**、在 compat 下**授权不足**，同一个根因。修法（最小）：删除 `revokeContract`，列表页改 POST 同一 canonical route，两个入口收敛到同一例程；`approveContract` 本已走 RPC，未动。验收：`tests/security/contract-revoke-boundary.test.mjs` 9/9，检测器先经变异对照（先证它认得被删掉的那条直写、再证它不误伤 5 种合法写法：contracts 只读、leads 状态改写、`contracts.update({sales_id})`、`revoke_contract` RPC、无关 payload 上的 `status:`），然后扫 `src/app/actions` 与 `src/app/(dashboard)` 全量、断言 `revokeContract` 标识符在四个目录中彻底消失（注释经 `code()` 剥离，好让解释性注释仍能保留那条语句原文）、钉住列表页 `res.ok` 分支与 `err.error` 落到 toast、并从迁移正文校验 `guard_contracts_write()` 仍拒绝状态直改、`revoke_contract()` 仍持有 `money_actor(null, array['admin','boss'])`、`for update`、transition 检查与 `previous_status` 回读。**未关闭**：例程随 `20260812000000` 一起等待应用，未应用前两个入口都没有 `revoke_contract` 可调（此依赖非本轮引入，详情页早已如此）；`contracts.sales_id` 的服务角色改派（`src/app/actions/team.ts`）是**另一条**未关闭项，本轮测试刻意不覆盖 | ⚠️ | 待部署 |
| PROD-PAYMENT-RECORD-NO-IDEMPOTENCY-KEY | supabase/migrations/20260813100000_payment_request_key_idempotency.sql, src/lib/payment-idempotency.mjs, src/app/api/payments/route.ts, src/app/(dashboard)/payments/page.tsx, src/app/actions/payments.ts, tests/security/payment-idempotency-boundary.test.mjs | CREATE+MODIFY | 四审 B3：付款页 Record Payment 走 `createPayment` server action，直接 insert `payments`，不带任何幂等键。隔离 PG17 复现（本分支磁盘上 14 个迁移 + `01_floor_schema.sql` + `05_seed_behaviour_fixtures.sql`，以 `authenticated` 注入 GoTrue 同形 claim（缺 `iat` 则 class-28 入口直接失败），sales 身份持有 fixture 合同）：compat 下同一表单提交两次 `sqlstate=00000 rows=2` —— 一次意图两笔钱；strict 下同一条 insert 直接 `22023 a payment must carry request_key`，按钮完全失效。同一枚键重复提交**相同**载荷 `23505 idx_payments_request_key rows=1`；同一枚键改成 99999 金额**同样是 23505**、库内金额仍 4321.00 —— 数据库对「诚实重试」与「键被复用」给出同一个 sqlstate，因此这个判断只能在应用层做，而写错的实现照样能通过任何只读源码的检查。修法（最小）：新增 forward-only 迁移只加 `request_key uuid` 与 `(created_by, request_key)` 部分唯一索引（按 creator 收敛，避免跨用户探测；不含任何 `create or replace function`，以免半份覆盖尚未提交的 round-4 守卫；声明 `NO_ROLLBACK` 并说明理由）；删除 `createPayment`，付款页改 POST `/api/payments`；幂等键在 `openRecordDialog()` **一次意图铸一枚**（铸在提交函数里则每次重试都是新键，等于没修）；路由在 23505 分支回读已存行的全部业务字段与本次请求比对：相同→200 返回首次那一行，不同→409 `IDEMPOTENCY_KEY_REUSED`，读不到→409 `DUPLICATE_REQUEST`。判定逻辑抽进 `src/lib/payment-idempotency.mjs`（沿用 `forced-password-change.mjs` 的 `.mjs`+`.d.mts` 先例），以便被真正执行而不是只被 grep。验收 21/21：检测器先过变异对照（先证它认得被删掉的那条直插、再证它不误伤 5 种合法写法），并断言全 `src/` 仅 canonical route 一处插入 payments、`createPayment` 标识符彻底消失、键在 `openRecordDialog` 铸造且 `handleRecordPayment` 内不含 randomUUID、金额以最小货币单位整数比对（0.1+0.2 视同 0.3，避免把诚实重试判成复用）、`""` 与 null 视同、非有限金额永不相等、只有 request_key 冲突才算键已用尽、录入角色与结算角色仍是两张互不混用的表。**未关闭**：strict 下「每笔付款都必须带键」的守卫在尚未提交的 round-4 迁移里，本轮只让真实 UI 提前合规并关闭重复入账；`created_by` 伪造在 compat 下仍返回 `00000`（strict 为 `42501`），属另一条；付款读模型与 void 状态（B8）本轮刻意未动 | ⚠️ | 待部署 |
| PROD-DEPLOY-TASKBOARD-GATE-MISSING | infra/systemd/newme-deploy.sh, infra/release/required-jobs.json, scripts/verify-remote-migration-history.mjs | MODIFY+CREATE | AGENTS.md 声称 “`scripts/deploy.sh` Step 0 运行 `check-taskboard.sh`，任一 ❌ 即中止部署”，而三审前无任何部署路径调用它。canonical wrapper 现按序硬门禁三件事：(1) 逐 job 读 `/actions/runs/{id}/jobs` 的 `conclusion`（run 级 `success` 会把被 skip 的必需 job 记成绿），要求 `infra/release/required-jobs.json` 每个 job 都 `success` —— 其中 `Release-final taskboard completion` 只可能出现在 `release_final=true` 的 dispatch run 里（`workflow_dispatch` 的 inputs 不由 runs API 暴露，该 job 存在与否是唯一可得的证明，也正是"被接受的 push run 结构上不可能包含 release-final"这一缺陷的修法）；(2) `check-taskboard.mjs --require-scope=predeploy_ready`（newme-deploy.sh:674，第 4 轮 C4-4 前是 `--require-complete`，见 PROD-DEPLOY-TASKBOARD-GATE-UNSATISFIABLE）；(3) `verify-remote-migration-history.mjs`（newme-deploy.sh:623）。验收：tests/release/deploy-release-claim-validation.test.mjs 16/16 + remote-migration-history.test.mjs 12/12，**直接执行**被抽出的 shell 函数与内联校验块。**按设计**：本分支当前必然在 (2) 处 exit —— `predeploy_ready` 里程碑的 10 行未关闭即不得部署。**新增运维前置条件**：root 拥有的 `/etc/newme/migration-db.url`（0400/0600）与 root PATH 上的 `node`，缺失即 exit 65 | ⚠️ | 待部署 |
| PROD-MIGRATION-HISTORY-IMMUTABILITY-GATE | scripts/check-migration-history.mjs, supabase/migration-history-baseline.sha256, scripts/regenerate-history-baseline.sh, tests/release/migration-history-gate.test.mjs | CREATE | 三审否决"重写已应用迁移"后新增的防复发门禁：以 sha256 清单核对 103 个既有迁移文件字节未变，并与 PR base `81956f2ff3bf` 的 git blob 逐一比对；改名、改字节、删除任一即红。新增迁移只能排在历史末尾（forward-only）。基线只能由 `regenerate-history-baseline.sh` 在显式说明理由的 commit 里重生成。验收：`node scripts/check-migration-history.mjs` → `103 listed, 103 verified unchanged` / `8 new` / `manifest vs git: verified against 81956f2ff3bf` / `OK` | ⚠️ | 待部署 |
| PROD-CONTRACT-STATUS-PATCH-ROUTE | src/app/api/contracts/[id]/route.ts, src/app/(dashboard)/contracts/[id]/page.tsx, src/lib/i18n/translations.ts | MODIFY | 合同详情页自诞生起就在 `PATCH /api/contracts/[id]`，而该模块只导出 `GET` —— **该页每个状态按钮一直是 405，状态变更从未生效过**。修法不是补一个写 `body.status` 的 handler：那会把九宫格变成审批链旁路（`approved`/`pending_ceo` 曾在按钮里）。现由 `set_contract_status()` 按转移表决定，越界 400；页面改为按当前状态渲染 `STATUS_TRANSITIONS[status]`，`terminated` 强制填原因。验收：money-route-rpc-coupling.test.mjs 断言 PATCH 导出存在、handler 不把请求状态写进行更新、UI 转移表与例程转移表**双向相等**、审批链状态不在网格里 | ⚠️ | 待部署 + 待 UAT |
| PROD-ROLLBACK-SECURITY-PRESERVING | supabase/migrations/rollback_l0_20260811.sql, supabase/replay/20_assert_post_rollback.sql | MODIFY+CREATE | 三审确认上一版的回滚测试"只证明 SQL 能执行"，且回滚会把安全边界一并撤掉。现回滚只撤业务姿态、不撤安全边界，并由 30 条 post-rollback 断言在 `MODE=branch` 里**执行**验证：回滚后伪造 `audit_logs`/`user_sessions` 插入仍被拒、`meta_tokens` 不回到 `authenticated` 可读、资金 definer 例程的 `anon` EXECUTE 不回来。验收：tests/release/production-rollback-controller.test.mjs + replay 30/30 | ⚠️ | 待部署 |
| PROD-MIGRATION-HISTORY-CONTENT-RECONCILIATION | scripts/verify-remote-migration-history.mjs, scripts/capture-remote-migration-history.mjs, supabase/migration-history-reconciliation.json, supabase/preflight/migration-history-reconciliation.md | MODIFY+CREATE | **已关闭**：认证只读采集得到 100 个远端 history rows，watermark=`20260805202917`；首次采集与 2026-08-14T15:24:11.752Z 的第二次独立读取均得到 canonical digest `82377a9a186dd1c7c8a6d247e4e64ecc1c5641f6bd828745bfdb01f9982aedea`。41 个显式 acceptance 与审计 findings 一一对应：16 `content_not_locally_reproducible`、6 `local_absent_remote_before_newest`、8 `name_mismatch`、7 `no_statements`、1 `non_cli_version`、3 `remote_only`；73/100 rows 可由本地 release 内容重现，audit 为 0 problems。采集只保存版本、名称、计数与服务端指纹，不保存 statement 正文、密钥或业务行；基线篡改、row/content drift、旧编码、未采集、过期/虚假 acceptance、claim/manifest 集合漂移与 capture/reconciliation 双向不一致均 fail-closed | ✅ | 2026-08-15 |
| PROD-CONTROL-PLANE-BOOTSTRAP | scripts/verify-deploy-gate-record.mjs, scripts/install-systemd-assets.sh, infra/systemd/newme-deploy.sh, infra/release/control-plane-bootstrap.md, scripts/control-plane-restore-drill.sh | CREATE+MODIFY | 已确认旧 f37c203 wrapper 无法执行候选门禁且控制面原先不可回退。候选 installer 在任何 mutation 或 pending recovery 前要求 exact-SHA、fresh、root-owned gate record；候选 wrapper 新增 bootstrap coordinator 模式，重新执行 canonical-main、exact-head required CI、predeploy taskboard、派生 migration claim、生产 migration history 与 companion 内容门禁后才由机器生成 record，操作者不得手写 gate labels。控制面、sudoers 与恢复工具进入同一 snapshot/rollback transaction。2026-08-14 在 `node:24-bookworm`、`--network none`、仓库只读挂载、1 GiB/2 CPU/256 PID 限额的一次性容器里实跑 `scripts/control-plane-restore-drill.sh`：**83 checks / 0 failures**；SIGTERM 探针取得 **rc=143**，确认目标仍为中断前完整字节、临时文件 **0**，随后同一 rollback 重入成功并再次重入保持同一控制面摘要；installer pending → finalize → finalize reentry 也全绿。该证据是本地 dirty candidate 的隔离行为证据，**不是** exact-head CI 或生产执行证据；生产 bootstrap 尚未执行，关闭仍需授权动作及 exact-SHA snapshot/finalize/health/zero-pending 证据。 | ❌ | — |
| PROD-MAIN-BRANCH-PROTECTION-UNENFORCED | infra/release/branch-protection.json, tests/release/branch-protection-contract.test.mjs | MODIFY+CONTROL-PLANE | 四审 P1-13 已于 2026-08-15 经 GitHub branch-protection API 应用并独立 GET 回读：`main` 的 `strict=true`，四个 pull_request 可达门禁为 `Repository validation`、`Windows checkout and SPEC gate`、`Narrow task follow-up database contract`、`Migration replay and release contracts`，均绑定 GitHub Actions app id 15368；`enforce_admins=true`、`required_approving_review_count=1`、`dismiss_stale_reviews=true`、`require_last_push_approval=true`、`required_conversation_resolution=true`、`required_linear_history=true`、禁 force push、禁删除。dispatch-only 的预部署/控制面/发布最终门禁不混入 PR 保护；部署前的 canonical main exact-SHA 七项验证（新增隔离的 `CodeQL analysis`）仍由 `required-jobs.json` 与受保护 wrapper 独立执行。仓库只保存声明和回读，不含自动写保护的脚本；契约测试通过。任何后续 live readback 漂移都会重新打开本项并阻断发布 | ✅ | GitHub live readback 2026-08-15 |
| PROD-PROXY-ACTIVITY-THROTTLE-UNBOUNDED | src/lib/activity-throttle.mjs, src/lib/activity-throttle.d.mts, src/proxy.ts, tests/security/proxy-activity-throttle.test.mjs | CREATE+MODIFY | 四审 R8（原「只登记」项，本轮修完）：`src/proxy.ts:81` 的 `const activityThrottle = new Map<string, number>()` 只被 `set()` 过——没有删除、没有过期、没有上限，于是每个经过 proxy 的 user id 各留一条、留到进程结束，只为执行一个 **5 分钟**的窗口；条目在 5 分钟后就再无用处，其余全是死重量（每条几十字节，放大量由「重启之间登录过多少个不同用户」决定，非攻击者可控，故仍非 P0/P1）。**修法与 `src/lib/rate-limit.ts` 同形**（同一理由：缺陷是可增长键表的结构性后果，所以键表整个去掉）：新增 `src/lib/activity-throttle.mjs`，4096 个固定槽位、一次性分配的 `Int32Array tags` + `Float64Array stampedAt`，key 经 sha256 后取 `readUInt32LE(0) & MASK` 定槽、`readInt32LE(4)` 作 tag，O(1)、不保留任何由 key 派生的字符串。**刻意不复用限流器的槽表**：两者回答的是不同问题（「这个客户端的登录预算用完了吗」vs「这个用户刚刚戳过吗」），共表会让一次活跃检查吃掉一次登录额度。**槽位带 tag 是本条的关键**——限流器里碰撞只会更严格（安全方向），这里安全方向相反：`last_active_at` 被当作关于该用户的事实读取（`team/page.tsx`、`api/users/route.ts`、`api/activity/daily-report/route.ts`），所以别人的槽必须被视作「无记录」而放行并接管，**碰撞退化为多一次写、绝不吞掉真实用户的活跃信号**；碰撞率也不可被瞄准，key 是服务端签发、从已验证 JWT 读出的 user UUID。写操作**仍留在 `src/proxy.ts`**：`tests/security/profiles-grant-coupling.test.mjs` 要求 proxy 是 `public.profiles` 唯一的调用方作用域写入点，把 UPDATE 搬进新模块等于为整理内存而破坏一条安全边界。**证据**：新增 `tests/security/proxy-activity-throttle.test.mjs` **6/6**，全部**运行模块**而非正则——窗口边界与被替换的 Map 逐点等价（`T0` 抑制、`T0+1` 抑制、恰好 `T0+WINDOW` 抑制、`T0+WINDOW+1` 放行、且窗口从刚取的戳重新起算）；20 万个不同 key 灌入后槽数不变（4096）且源码内**无** `new Map|new Set|new WeakMap`；5000 个 key 打在 4096 槽上，**首见放行 5000/5000**（若邻居能代答「已戳过」此处必红）；**用 sha256 现算出一对真碰撞 key**（不是假设有碰撞）后依次证明 A 放行、B 放行（碰撞不抑制）、A 再放行（槽被接管）、A 第四次抑制（证明接管来自碰撞而非窗口失效）；时钟回拨——先在 `T0+86400000` 落戳再用校正后的 `T0` 询问，必须放行，否则一次 NTP 校正就能把用户的活跃信号冻结一整天。`npx tsc --noEmit` clean；`profiles-grant-coupling` / `proxy-availability` / `l0-auth-hardening` 与本文件同批 **29/29**。模块写成 `.mjs` + `.d.mts`（与 proxy 已导入的 `auth-profile.mjs`、`forced-password-change.mjs` 同规），这是 `proxy-availability.test.mjs` 能以**真实实现**而非 stub 装载 proxy 的原因。**本轮门禁扫描补修（confirmed）**：`tests/security/forced-password-change-boundary.test.mjs` 用的是**显式 mock 映射**（`Module._load` 只对映射里出现的名字返回替身），R8 给 `src/proxy.ts` 新增的 `@/lib/activity-throttle.mjs` 导入没进这张表，于是该文件 5 个用例全部以 `Cannot find module '@/lib/activity-throttle.mjs'` 失败（`@/…` 在 Next 的 tsconfig paths 之外不可解析）。修法是把**真实的** `shouldRecordActivity` 导入并加进两张映射（不用 stub：该文件量的是拒绝顺序，一个永远同意记录的 stub 就不是发布的那个 proxy），`node --test tests/security/forced-password-change-boundary.test.mjs` **9/9**，全量 `npm test` **924 用例 / 921 通过 / 0 失败 / 3 跳过**。**未关闭**：作用域仍是单个 Node 进程（与限流器相同）；窗口是去重而非安全边界，多进程部署每进程每窗口最多多戳一次 | ⚠️ | 待部署 |
| PROD-L0-ROUND4-ENTRY-BOUNDARY-AND-MONEY-INTEGRITY | supabase/migrations/20260816000000_l0_round4_definer_entry_boundary.sql, supabase/migrations/20260817000000_l0_round4_money_and_business_integrity.sql, supabase/migrations/20260818000000_money_direct_write_contract_phase.sql, infra/release/release-manifest.json, scripts/db-phase-push.mjs, scripts/check-release-manifest.mjs, scripts/phase-tool-drill.sh, tests/release/release-phase-manifest.test.mjs | CREATE+MODIFY | 四审 A1 + B2–B7 + B10 的数据库侧，已提交 `78d13f4364b`。`20260816000000` 把会话断言放到每个 authenticated SECURITY DEFINER 例程的**入口**（停用/封禁/强制改密/过期 token 在第一条语句前即被拒），并从 end-user 角色收回触发器函数的 EXECUTE；`20260817000000` 拒掉会腐蚀账本的写入（负数与重复付款、与合同金额不符的分期、会把报价挂到别人合同上的转换重试、无客户无业务事件的 won lead、被目标编辑删掉或记到错人头上的 KPI 实收）；`20260818000000` 由 `20260815000000` 改名而来，仍排在最后，是 contract 相位与回滚边界（唯一保留回滚伴随文件的一个）。相位工具：`infra/release/release-manifest.json` 为逐文件内容哈希（sha256，CRLF 归一）的精确迁移集，`scripts/db-phase-push.mjs` 只应用一个具名相位，`scripts/check-release-manifest.mjs` 维持清单 == 目录（现 required_for_app 17 / deferred_contract 1 / 目录待应用 18）。`scripts/phase-tool-drill.sh` 九步实测（PG 17.10、两个一次性库、5 次拒绝、18 个迁移分两相位应用）—— **本轮修掉该 drill 自身的两处失真**：中断步骤把"第 12 个迁移 / 11 个已应用"写成常量，`20260813100000` 进入发布集后即已过期（该 drill 在 `78d13f4364b` 未重跑，提交信息也未声称它绿）；证物列 `payments.request_key` 由更早的 `20260813100000` 合法创建，它存在与否证明不了回滚。现改为从清单派生（`BREAK_FILE` 的位次与前一版本号）并以只有该文件创建的 `payments.credited_to` + `payments_amount_positive` 作证物，实测 `12 applied through 20260816000000` → 绿 | ⚠️ | 待部署 |
| PROD-FIRST-PAYMENT-STATUS-LITERAL-SEQ-ONE | supabase/migrations/20260817000000_l0_round4_money_and_business_integrity.sql, supabase/replay/10_assert_release_contracts.sql, tests/security/first-payment-derivation-boundary.test.mjs | MODIFY | 四审 B2 复审残留（独立复审在 `78d13f4364b` 的同一文件里找到、当时**未修**、本轮修）：`contract_first_payment_status()` 用字面量 `seq = 1` 取首期，而校验函数只要求 seq 正整数且不重复 —— 于是编号为 2,3 的分期（`20260817140000` 之前被接受、且生产已可持有此形状的行）在首期已确认并全额分配后仍读 `unpaid`，计划行读 `paid`，此后任何确认或作废都无法把它推回来；存储列与派生逻辑给出同一个错答案，表级对账不变式因此也看不见。改为 `order by seq asc, created_at asc, id asc limit 1`（取**存在的最小 seq**，不是字面 1）。验收：replay `b2-first-payment-status-reads-the-lowest-seq-not-literally-one`（以迁移角色把夹具计划改为 seq 2 —— `20260817140000` 之后 `create_contract()` 已造不出这形状 —— 再确认并分配 40000，断言 `first_payment_status='paid'`；control 模式对未修复地板判红，非空断言）+ `tests/security/first-payment-derivation-boundary.test.mjs` 8/8 | ⚠️ | 待部署 |
| PROD-INSTALLMENT-SCHEDULE-NONCONTIGUOUS | supabase/migrations/20260817140000_l0_round4_installment_sequence_contiguity.sql, src/app/api/contracts/route.ts, supabase/replay/10_assert_release_contracts.sql, tests/security/installment-schedule-contiguity.test.mjs | CREATE+MODIFY | 四审 B4 复审残留（同上，本轮修）：`assert_installment_schedule()` 只查"总额相等 + seq 为正 + 不重复"，`[{seq:1,30000},{seq:3,70000}]` 对一份 100000.00 的合同在**两种 release 模式下都被接受** —— 金额恰好对上、每个 seq 都正且只用一次 —— 产出的合同第二期并不存在，而下游没有任何读者能把它和一份两期合同区分开；单独一条 seq=2 的分期同样被接受，那份合同对每个要"首期"的读者都报告没有首期，即 B2 的隐患从正门进来。本轮把连续性做成校验的一部分：`max(seq) = count(*)` 不成立即 22023，消息给出实际编号（`the installment schedule must be numbered 1..2 with no gaps, but it is numbered 1,3`）。route 侧在 `src/app/api/contracts/route.ts` 同步一份镜像规则，使 400 与数据库的 22023 一致而不是把它变成 500。文件版本号 `20260817140000` 刻意排在 contract 相位 `20260818000000` 之前（expand 相位必须是待应用集的连续前缀，由 `tests/release/expand-contract-rollback-contract.test.mjs` 强制）。验收：replay `b4-a-schedule-with-a-gap-is-refused` / `b4-a-schedule-that-does-not-start-at-one-is-refused`（两条都额外断言 `contracts` 计数为 0，避免"拒绝了但半条写入还在"）+ `tests/security/installment-schedule-contiguity.test.mjs` 8/8 | ⚠️ | 待部署 |
| PROD-CONVERSION-RETRY-DOUBLE-COUNTS-AMOUNT | supabase/migrations/20260817130000_b5_conversion_retry_idempotence.sql, supabase/replay/10_assert_release_contracts.sql, tests/security/quotation-conversion-retry-idempotence.test.mjs, supabase/migrations/20260817170000_b5_retry_reaches_the_same_state.sql, supabase/replay/control-expectations.txt, infra/release/release-manifest.json, supabase/preflight/expand-contract-rollback.md, tests/release/quotation-retry-equivalence.test.mjs, tests/release/sql-text.mjs, tests/release/kpi-period-lock-gate-contract.test.mjs, tests/release/release-claim-derivation.test.mjs | CREATE+MODIFY | 四审 B5 复审残留（同上，本轮修）：`convert_quotation_to_contract()` 的幂等分支认出"这份报价已经转过"并返回既有合同，但**在返回前又跑了一遍记账副作用** —— `customers.total_contract_amount` 因此按重试次数累加，客户额度与看板金额随网络重试而虚增；同一分支对"重试时带了另一套分期"不作声，静默按首次的结果返回。本轮把幂等分支收敛为真正的幂等：既有合同直接返回、不再加金额，且重试携带的分期若与既存合同不一致则以 22023 拒绝（而不是假装成功）。`NO_ROLLBACK` 已声明并写明回退后会恢复的状态。验收：replay `b5-a-conversion-retry-does-not-add-the-amount-again` / `b5-a-retry-asking-for-a-different-schedule-is-refused`（control 模式两条均对未修复地板判红）+ `tests/security/quotation-conversion-retry-idempotence.test.mjs` 5/5。**残留（未关闭，已写入制品注释）**：记账现在是"每个 lead-won 一次"，而 `customers.total_contract_amount` 没有任何对账作业能从 `contracts` 重算 —— 生产库里此前由重试累加出的偏差不会被本迁移修正，需一次性核对（属生产授权动作，本轮不做）。**R4 追加：那个分支学会了拒绝，但没学会完成。**B5 把幂等分支写成"认出重试 + 拒绝一切不是本次重试的东西"，这部分是对的；缺口在**被接受的那条重试**：它一个字都不写就返回 `success: true` —— `quotations.status` 留在 `accepted`、`leads.final_status` 留在 NULL、`contracts.customer_id` 留在 NULL、返回值里没有 `project_id`。于是首次 POST 在"合同行已插入"与"尾部三处写入"之间断掉的调用方，**此后每一次重试都被告知已经转好了**，而那个 lead 永远不算赢、客户永远不挂到合同上、`finalize_lead_won()` 的业务事件永远不发生。**行为证据（PG 17.10 Debian 17.10-1.pgdg13+1，隔离 replay 库，"只抽掉 20260817170000 这一个文件的完整 release"）**：平台 bootstrap + 地板 + 本分支其余 19 个迁移 + `05_seed_behaviour_fixtures.sql` + 同样 19 个再应用一遍（即 runner 的 branch 流水线减一个文件）；在那上面重试返回 `success: true, already_converted: true, quotation_status: 'accepted', project_id: null, finalized: []`，读回三行仍是 `quotations.status=accepted / leads.final_status=null / contracts.customer_id=null`；再把 lead 改成 `'lost'` 后同一次重试答 `sqlstate 00000`（成功）且 lead 仍是 `lost` —— 即它**既没有完成转换，也没有拒绝推翻这个 lead**，它报告了一次没有发生的转换。最小修复 `20260817170000`：`create or replace` 整体重发（PG 的函数体是整体替换），**差异是量出来的而不是声称的** —— 在上述"抽掉一个文件"的库上 `pg_get_functiondef()` 取一次，应用本文件后再取一次，两份已安装函数体的全部差异是：两个新声明、报价读取之后的 `v_status := v_quote.status`、lead 读取上的 `for update`、幂等分支里的四个块，以及该分支返回值里 `quotation_status` 改读 `v_status` 加上新增的 `project_id`；**每一条拒绝、每一个守卫、整条首次转换路径逐字节相同**。修好后的重试：把 `quotations.status` 收敛到 `contract_created`；`leads.final_status` **只从 NULL 修**，任何其它终态一律以 22023 拒绝（与 B5 其它拒绝同形，消息含 "rather than won"）而**不是**推翻它；`contracts.customer_id` 只在为 NULL 时写（与首次转换同一个 `is null` 守卫，故重试不可能盖掉别人的客户）；返回 `project_id`，并以 `finalized` 数组报告自己修了什么；lead 读取加 `for update`，因为这条分支现在会写 leads，两个并发重试不能在读与修之间交错。`installment_plans` 刻意不在重试路径上写 —— B5/B10 的语义是**比对**既存分期并对不一致以 22023 拒绝，在这里写等于允许一次重试改写一份已经被分配过钱的排期。**断言与三模式**：新增 5 条 `r4-a-*`，`ASSERT_TOTAL` 341；branch `== replay OK: 20 migrations, 341 release assertions, 2 rollback companion(s), 54 post-rollback assertions, 1 recontract companion(s), 18 post-recontract assertions ==`；control `control marker accounting OK: 341 assertions, 282 load-bearing, 59 floor-passing, 0 unclassified SQL errors, ledger total=341` + `== control OK ==`；history 债务未变（2 applied，停在 `20260602010000_crm_mvp_final.sql`）。**负控制取强的那一个**：地板上"函数不存在"也会让五条断言判红，那证明不了它们在测什么；于是同样五条在上述"只抽掉一个文件"的库上以 collect 模式跑完，`ASSERT_LEDGER total=341 passed=336 failed=5`、`ASSERT_UNMEASURABLE` 为 **0**，且判红的**恰好**是这五条 `r4-a-*`、别的一条都没动 —— 即它们是为这个缺陷本身判红的。**离线防腐**：`tests/release/quotation-retry-equivalence.test.mjs` 7/7 —— 从**最后一处**定义（按目录派生，不钉文件名）结构化切出两条路径（`if v_quote.contract_id is not null then` 到例程自身缩进的 `end if;`，不靠注释切），比对写入目标集合、副作用例程调用集合与 `return jsonb_build_object` 的**键**（按位置读，否则值里的 `'contract_created'` 会被当成键），唯一豁免是 `installment_plans` 且理由与本行同文写在代码里；**它自带负控制** —— 同一套比对跑在 `20260817130000`（还在树里的那份缺陷定义）上，必须报出恰好缺 `project_id`、恰好 `contracts/leads/quotations` 三张表没被修，否则上面那些比对就是两版都满足的形状匹配、什么也没在测。**顺带修掉两处会读成假绿的写法**：`tests/release/kpi-period-lock-gate-contract.test.mjs` 的"周期号在全集里独一无二"用的是原文包含判断，于是 R3 把实测周期写进文件头这件事让它把那个迁移判成第二个写手 —— 文档不是写手，改为经新增的 `tests/release/sql-text.mjs` 剥注释后只扫可执行 SQL，并**把剥离器本身钉住**（保留字面量、`$$` 体、`'it -- is not a comment'`、`e'a''b'`；剥掉 `--` 与嵌套 `/* */`；且真实 R3 文件必须"原文含该周期、剥完不含"）；`tests/release/release-claim-derivation.test.mjs` 里有三处硬编码的 `18` —— 在一个专门用来钉"计数必须派生"的文件里第二次声明 release 的规模，改为从清单读。**声明面同步**：清单已 stamp（`required_for_app 19 / deferred_contract 1 / pending 20 / hand-run 5`，OK）；`expand-contract-rollback.md` 的 expand 栅栏、三处计数、状态表两组行、§6.1 query 3 的"最新版本"全部推进到 `20260817170000`；`10_assert_release_contracts.sql` 里那句陈旧的"all 331 assertions"散文改为不带计数的说法（它已经腐坏过一次，第二份计数副本就是会腐坏的那一份）。`node --test tests/release/*.test.mjs` **337/337**。**残留（未关闭）**：本迁移让**今后**的重试把三行修回去，但生产库里已经半转换、且**没人再重试**的那些行不会被它修好，需一次性核对（同属生产授权动作，本轮不做） | ⚠️ | 待部署 |
| PROD-KPI-PERIOD-DELETE-BYPASSES-ROUTINE | supabase/migrations/20260817150000_kpi_period_clear_owns_the_delete.sql, supabase/migrations/20260817160000_kpi_period_lock_covers_money_writers.sql, src/app/api/kpi/targets/route.ts, src/types/database.ts, supabase/replay/10_assert_release_contracts.sql, supabase/replay/19_concurrency_kpi_period.sh, scripts/check-release-manifest.mjs, supabase/preflight/expand-contract-rollback.md, tests/security/kpi-actuals-boundary.test.mjs, tests/release/kpi-period-lock-gate-contract.test.mjs | CREATE+MODIFY | 四审 B7 复审残留（同上，本轮修）：`20260817000000` §14 只关了 **SAVE** 路径（`replace_kpi_targets()` 结转 actual_amount，丢弃仍持实收的配对即 22023），而 `src/app/api/kpi/targets/route.ts` 的 DELETE 仍以 service-role 客户端直打表：`delete().eq("period", period)`。PG 17.10 + 地板 + 全部分支迁移 + 夹具，两种模式实测同一条语句的三重后果 ——（1）删掉 2 行并带走 700.00 已收金额，无任何守卫（`actual sum 700.00 -> 0`），而没有任何作业能从 `payments` 重算回来；（2）不取周期锁：在一个未提交的 service-role DELETE 进行中，第二连接对同一 `hashtextextended('public.kpi_targets:'||period,0)` 键的 `pg_try_advisory_xact_lock` 返回 TRUE、`pg_locks` 上该键零条 advisory 锁，即 `replace_kpi_targets()` 声称的串行化根本不覆盖删除路径；（3）绕过 RLS，于是 route 自己的角色表就是全部授权 —— 当时是 admin/boss/operator，而生产 DELETE 策略（`20260701000000_non_core_tables_rls_fix.sql:227`）是 admin/boss，operator 的同一条语句被数据库拒绝、被 route 接受（与 B1 同形）。本轮新增 `public.clear_kpi_targets(p_period text, p_actor uuid) returns bigint`（SECURITY DEFINER、pinned search_path、首条语句 `assert_current_session_at_entry()`、取与保存路径**同一把** advisory 锁、锁内读计数后若有非零 actual_amount 即 22023 并给出与保存路径同形的消息、写 `KPI_PERIOD_CLEARED` 审计行且 `actor_id` 留空而把 claimed actor 放进 details、`revoke all … from public, anon, authenticated` 后只 `grant execute … to service_role`），route 改为 `rpc("clear_kpi_targets")` 并把角色表收敛到 admin/boss。验收：replay `b7-clearing-a-period-that-holds-collections-is-refused`（断言 2 行仍在、实收仍为 777.00）/ `b7-clearing-a-period-that-holds-nothing-is-still-allowed`（正控制：否则"拒绝一切"的例程也能满足上一条）/ `b7-the-period-clear-routine-is-service-role-only`（走 `pg_proc` + `has_function_privilege` 目录判定，并刻意写成"函数不存在时判红"而非空过）+ `tests/security/kpi-actuals-boundary.test.mjs` 12/12。**残留（未关闭）**：没有任何作业能从 `payments` 重算 `kpi_targets.actual_amount`，历史上被直接 DELETE 清掉的实收无法自动恢复；`supabase/replay/01_floor_schema.sql:728` 的地板 `kpi_targets` 上没有任何 RLS 策略，故"策略级"断言在 replay 中会空过 —— 上述第三条断言因此改用目录授权判定。**R3 追加：那把周期锁只覆盖了改目标的两个例程，没覆盖搬钱的两个。**`20260817000000` §14 与本行的 `20260817150000` 都把 `hashtextextended('public.kpi_targets:'||p_period,0)` 描述为"串行化同一周期的一次保存与一次清空"—— 这句话是准确的，也正是缺口所在：真正把钱搬进/搬出 `kpi_targets.actual_amount` 的 `confirm_payment()` 与 `void_payment()` 写同一周期的行，且**一把锁都不取**。三条后果同键、同形（改目标的一方持锁，搬钱的一方直接走过去）：（1）**实收丢失** —— `replace_kpi_targets()` 在锁内把周期实收读进 `v_actuals`（B7 结转），随后 delete + insert 回写快照；在快照与 delete 之间提交的 `confirm_payment()` 其金额被 delete 带走、被 pre-confirm 的旧值覆盖，两个事务都报成功，而没有任何作业能从 `payments` 重算回来；（2）**B7 孤儿守卫被绕过** —— 那条"拒绝丢弃仍持实收的配对"是拿 `v_actuals` 判的，对锁后提交的 confirm 天然过时，于是保存被接受、目标行消失、它持有的实收随之消失，正是守卫注释里"这笔钱没有正确的去处"那条路径的无声版本；（3）**清空可以宣称周期是空的** —— `clear_kpi_targets()` 在锁内数非零实收后 delete 并写 `KPI_PERIOD_CLEARED` 审计行，落在数与删之间的 confirm 被删掉，而那条服务端自有审计行记录的是"干净地清掉了一个空周期"。最小修复 `20260817160000`：两个例程按 `create or replace` 整体重发（PG 的函数体是整体替换），**除了在各自 `kpi_targets` 写之前取同一把、同一键表达式的锁以外，两个函数体一个字都没改**；周期由 `to_char(v_payment.payment_date,'YYYY-MM')` 得出，与两处写用的是同一个值；锁序在文件头逐例程论证（四个例程一律行锁在前、advisory 锁在最后、持 advisory 锁后不再取新行锁，`void_payment()` 持锁后那条 contracts UPDATE 打的是它开头就已锁住的同一行），故不引入死锁；**刻意不加 `set local lock_timeout`**（它会一并作用于这些例程既有的 `for update` 等待，把当前能成功的等待变成报错，那是另一件事）；`create or replace` 保留 ACL，文件末尾自检断言它既没放宽授权、也确实存在 ≥4 个写 `kpi_targets` 的函数且全部持这把锁。**行为证据（PG 17.10 Debian 17.10-1.pgdg13+1，隔离 replay 库，两个方向都测，`supabase/replay/19_concurrency_kpi_period.sh`，屏障取自数据库自身锁状态、全程只有 `sleep 0.2` 轮询）**：负控制的变异体不是手写的，而是从**已安装的定义**里 `pg_get_functiondef` 出来再 `grep -v -F` 掉那一行锁（断言恰好少一行），跑完按字节还原并逐字节复核（EXIT trap 与正常路径都还原）；`EXPECT=lost` —— 收款方向搬钱会话等在 `transactionid`（行锁，不是这把键，证明变异确实生效），`confirm_payment` 报 success 而 `actual_amount=0.00`、账面 `4321.00`；退款方向保存把 `4321.00` 结转下去后 `void_payment` 报 success 而 `actual_amount=4321.00`、账面 `0.00`；两个方向都是"操作成功且 KPI 与账面整笔不一致"。`EXPECT=serialized`（本迁移在位）—— 两个方向搬钱会话都等在 `advisory`，随后收款 `actual_amount=4321.00 = 账面 4321.00`、退款 `actual_amount=0.00 = 账面 0.00`；**没观察到等待、或等错了锁类型，gate 判红**，所以一次绿灯不可能是两个会话根本没重叠、或变异没生效的那种绿。runner 三模式：branch `replay OK: 19 migrations, 336 release assertions, 2 rollback companion(s), 54 post-rollback assertions, 1 recontract companion(s), 18 post-recontract assertions`；control `control OK`；history 债务未变（2 applied，停在 `20260602010000_crm_mvp_final.sql`）。gate 只在 branch 模式跑两个方向且**控制方向先跑**（它要改再还原已安装的定义），未修复地板没有 B7 结转、在那里保存本来就会清零，冒充不了控制，runner 里写明了这个理由。**声明面同步**：`expand-contract-rollback.md` 的第十二条 posture 谓词改为**由函数体推导**（`pg_get_functiondef(p.oid) ~* '(update|delete[[:space:]]+from|insert[[:space:]]+into)[[:space:]]+public[.]kpi_targets'`，要求 `count(*) >= 4` 且全部 `pg_advisory_xact_lock(` + 该键表达式），两个 phase 各一份、与迁移自检里那份逐字节相同并由测试绑死 —— 名单式的检查关不住"谁又加了一个写 `kpi_targets` 的函数"，这正是 R3 本身的形状；`scripts/check-release-manifest.mjs` 的 posture 校验因此改为**先去掉单引号字面量再扫禁用关键字**（谓词要携带含 update/insert/delete 的模式），引号数不成对即拒绝，`$` 引用刻意不解包（里面的关键字照样判红），新增反例：字面量之后的关键字、未闭合字面量、`$` 引用三种都必须仍被拒。离线门禁：`tests/release/kpi-period-lock-gate-contract.test.mjs` 11/11（含"两个方向都接 `|| fail`、控制先跑、EXPECT 无默认值、屏障不是 sleep、verdict 比的是 actual 与账面而不是源码、变异体的推导与还原、gate 的周期号在 replay 与 migrations 全集里独一无二、以及**没有两个并发 gate 共用 done-marker 键**"）、`tests/security/kpi-actuals-boundary.test.mjs` 13/13（改为读每个例程的**最后一处**定义而非钉死某个文件，并断言每个写 `kpi_targets` 的最后定义都在首次写之前取这把锁；以 ROUND4 的旧体做非空过探针：takes lock=false / writes kpi_targets=true）、`tests/release/mode-controlled-guards.test.mjs` 18/18、`tests/release/release-phase-manifest.test.mjs` 20/20、`tests/release/*` 329/329、`node scripts/check-release-manifest.mjs` OK（required_for_app 18 / deferred_contract 1 / pending 19 / hand-run 5）。**顺带修掉两处真缺陷**：`18_concurrency_mode_setter.sh` 的 done-marker 键与 `17_concurrency_mode_flip.sh` 撞了（17_ 以 `MARK_LO + 1` 占了 648/649，18_ 声明的是 649），改为 650；`tests/release/replay-concurrency-gate-contract.test.mjs` 原先禁的是 branch 块里出现 `EXPECT=lost` 这个词，而本 gate 的控制方向按设计就叫 `lost` 且就在 branch 跑，断言改为按 gate 变量收窄。**残留（未关闭，与上同）**：仍然没有任何作业能从 `payments` 重算 `kpi_targets.actual_amount`；本迁移只保证并发下不再丢，历史上已丢的不会回来 | ⚠️ | 待部署 |
| PROD-ROLLBACK-HAS-NO-RETURN-PATH | supabase/migrations/recontract_money_direct_write_contract_phase.sql, supabase/migrations/rollback_l0_20260811.sql, supabase/replay/30_assert_post_recontract.sql, scripts/replay-migrations.sh, scripts/check-migration-history.mjs, scripts/check-db-static.mjs, infra/release/release-manifest.json, supabase/preflight/expand-contract-rollback.md, tests/release/expand-contract-rollback-contract.test.mjs, tests/release/control-marker-accounting.test.mjs, tests/release/migration-history-gate.test.mjs | CREATE+MODIFY | 四审 B9：回滚伴随文件把 `direct_write_mode` 退回 `compat`，但**没有任何已验证的返回路径** —— `20260818000000` 一旦应用即被记录进 `supabase_migrations.schema_migrations`，`supabase db push` 与 `scripts/db-phase-push.mjs` 都跳过已记录版本，回滚后待应用集为空；再加一个编号迁移也无用（首次部署即被记录，第二次仍是同一死路），所以返回路径必须是**手工运行的构件**，与回滚伴随文件同一形状（名字不匹配 `^[0-9]{14}_`，CLI 永不应用，可重复运行）。`recontract_money_direct_write_contract_phase.sql` 因此只改一张表的一行，并在机制缺失时**拒绝声明姿态**：`money_release_mode` 不存在、`money_direct_write_mode()`/`money_direct_write_is_blocked()` 缺失、或四个受模式门控的守卫触发器有任一缺失/被 `disable`，即以 42P01 拒绝（把列写成 `strict` 而守卫不在，是一个背后什么都没有的姿态声明）；写入后不只读列、还经守卫真正调用的 `money_direct_write_mode()` 回读，并留 `MONEY_CONTRACT_PHASE_REENTERED` 审计行记录来源模式。**本轮同时把这类缺口做成通用闸门**：每个被回滚的迁移都必须有 `-- RECONTRACTS:` 双生文件，或由回滚伴随文件以 `-- NO_RECONTRACT: <理由>` 说明为何不需要 —— 闸门首次运行即抓到第二处单向门：`rollback_l0_20260811.sql` 会 drop `replace_kpi_targets(text, jsonb, uuid)`（service_role 专用、上一版 release 不调用），而 `20260811100500` 同样已被记录，重新部署候选版后 `POST /api/kpi/targets` 会以 42883 失败；其返回路径是重跑该前向迁移本身（`create unique index if not exists` + `create or replace function` + revoke/grant，天然幂等），故以 `NO_RECONTRACT` 声明并写入 runbook §5.1，而不是再造一份会漂移的函数体副本。清单侧修掉一处假绿：两个 `write-guards-are-*installed` 姿态谓词只查 `pg_trigger` 存在性，被 `disable` 的触发器照样计入 —— 现同时要求 `tgenabled = 'O'`，使 `--verify-only` 与构件的拒绝条件问同一个问题。静态闸门把 `recontract_` 与 `rollback_` 同列为手工伴随文件（`check-db-static.mjs` 原先会把 `recontract` 当成时间戳前缀，第二个此类文件即误报重复）。验收：PG 17.10 一次性库 `MODE=branch` 全绿 —— `18 migrations, 331 release assertions, 2 rollback companion(s), 50 post-rollback assertions, 1 recontract companion(s), 13 post-recontract assertions`；构件被**连续应用两次**后 `30_assert_post_recontract.sql` 13/13（含 `previous_mode='compat'` 的审计行证明它确实从回滚态执行、两次运行两条审计行一行状态，以及三条行为断言：`20_assert_post_rollback.sql` 在 `compat` 下断言**成功**的同一批直写在此处必须重新以 42501 被拒）；`npm run test` 714 passed / 0 failed；`node --test tests/release/expand-contract-rollback-contract.test.mjs` 15/15。**本轮补记一处 CI 判红与其成因**：`8263cdabc8d` 的 `ci` / Repository validation / Database type source gate 判红 —— `scripts/check-database-types.mjs` 把 `supabase/migrations` 下**每个** `.sql` 都纳入指纹，手工运行的伴随文件也算，于是新增 `recontract_money_direct_write_contract_phase.sql` 与对 `rollback_l0_20260811.sql` 的一处**纯注释**修改都会让 `src/types/database.ts` 首行的 `sha256=` 过期，而生成的类型本身一个字节都没变。已以 `node scripts/check-database-types.mjs --stamp` 重盖（单行 diff，复跑 exit 0）。刻意**未**把闸门收窄成排除伴随文件：重盖是准确的（`recontract_*` 不含任何 DDL，由本行自己的测试强制；rollback 的改动是注释），而单方面放宽一个安全相邻闸门属于联合审计的范围决定 | ⚠️ | 待部署 |
| PROD-QUOTE-CONVERT-SCHEDULE-CENT-TOLERANCE | src/app/(dashboard)/quotes/quote-detail-dialog.tsx | MODIFY | 四审 B10 的 UI 侧（数据库侧已随 `20260817000000` 落地，replay `b10-a-conversion-schedule-must-equal-the-quotation-exactly`）：对话框用 `abs(remainder) <= 0.01` 判定分期表与报价总额相符，而 `convert_quotation_to_contract()` 自 `20260817000000` 起改用两位小数取整后的**相等**判定 —— 于是差一分钱的分期表在界面上显示为匹配、转换按钮可点，提交后由数据库以 22023 拒绝，操作员看到的是一个自己无法据以修正的错误。现改为 `scheduleRemainder === 0`（`round2` 之后），按钮启用条件与两处标签配色/文案共用同一判定，使界面所说的匹配就是例程接受的匹配。验收：`npx tsc --noEmit` 通过；`npm run lint:baseline` 404 错误无新增 | ⚠️ | 待部署 |
| PROD-PAYMENT-STATE-MODEL-DISAGREEMENT | src/lib/payment-state.mjs, src/types/payments.ts, src/app/api/payments/list/route.ts, src/app/(dashboard)/payments/page.tsx, src/app/actions/payments.ts, src/lib/i18n/translations.ts, tests/security/payment-state-model.test.mjs, tests/security/money-grant-coupling.test.mjs | CREATE+MODIFY | 四审 B8：三方对「一笔付款是什么」的说法互不相同。数据库：`confirmed` 与 `voided_at` 是两个独立列，分配落在 `payment_allocations`，而 `installment_plans.allocated_amount` / `projects.paid_amount` / `kpi_targets.actual_amount` / `contracts.first_payment_status` 每一个派生总额都从 `p.confirmed = true and p.voided_at is null` 重算。列表路由：`select("*, contracts(contract_no, party_a_name)")` 后 `as any[]`，**完全没有分配聚合**。页面：自带一份 `interface Payment`，声明了一个非可选的 `allocated_amount: number` —— 而 `payments` 表根本没有这一列（它在 `installment_plans` 上），任何 select 都供不出来，于是 Total Allocated KPI 恒为 AED 0.00、每行的分配信息永不渲染；`as any[]` 正好抹掉了路由与页面唯一必须一致的那个位置的行类型，TypeScript 看不见。**本轮同时纠正复审条目本身的一处不准确**：void 字段并未"没有返回"——`*` 会带回 `voided_at`/`voided_by`/`void_reason`，把它们丢掉的是页面那份不含这些字段的声明。后果仍然成立且更严重：`void_payment()` 同时把 `confirmed` 置回 false，故一笔已作废付款在页面上显示为 **Pending**、同时被计入 Total Recorded 与 Total Pending（同一笔钱数两次），并被提供一个 `confirm_payment()` 以 `22023 a voided payment cannot be confirmed` 拒绝的 Confirm 按钮 —— 数据库 fail-closed，坏的是读模型与 UI。另外三处：（1）`void_payment()` 与 `POST /api/payments/[id]/void` 都已存在，页面却触达不到任何一个，于是 UI 上唯一的"冲正"手段就是三审已从所有角色收回的 DELETE；（2）`allocate_payment()` 先 `delete from public.payment_allocations where payment_id = p_payment_id` 再插入提交集 —— 一次提交是**替换**而非追加，而对话框以空输入打开，因此"给第二期分配"会静默释放第一期；它还把 `previouslyAllocated` 与一个跟付款金额比较的运行总额并排显示，仿佛两者相加；（3）对话框显示 `plan.paid_amount`，那是只由 `20260605000000` 里按 `payments.installment_plan_id` 触发的前分配期触发器写入的遗留列，而 `allocate_payment()`/`void_payment()` 重算的是 `allocated_amount` —— 即它展示了一个自己怎么点都不会动的数字。**缓存侧**：void 路由 revalidate 了三条路径，而 `confirmPayment`/`allocatePayment` 两个 server action 什么都没做，于是确认一笔付款在库里推动了 `projects.paid_amount`、`kpi_targets.actual_amount` 与 `contracts.first_payment_status`，而访问过的 `/contracts`、`/dashboard` 仍渲染写入前的数字。修法：`src/lib/payment-state.mjs` 是唯一的状态模型（沿用 `payment-idempotency.mjs` 的先例写成**可执行函数**而非只能被 grep 的常量，因为"把作废的钱算成待确认现金"正是任何拼写检查都会放过的那一类错误），三态由 `voided_at` 优先判定，所有金额以 fils 整数比较（numeric(12,2) 经 JSON 变成浮点，`0.1+0.2` 会让 `allocated >= amount` 在最后一分钱上变成抛硬币，与 B10 的两位取整相等判定同源），`hasAllocationData()` 区分「真实的 0」与「读模型没给」，`paymentAllows{Confirm,Allocate,Void}()` 各自镜像一个例程的行前置条件（**不含角色**：角色规则留在页面的 `SETTLEMENT_ROLES`，两条规则写成一条正是三审 P1-9）；`src/types/payments.ts` 是路由与页面共用的唯一声明；列表路由改为显式列名并**必须是单个字符串字面量**（`[...].join(", ")` 的 select 参数类型是 `string`，postgrest-js 的类型级解析器随之失败、行类型退化成 `{ error: true } & String` —— 这正是 `as any[]` 当初进来的原因），`allocated_amount` 在此从 `payment_allocations` 以 fils 求和后给出、并在**同一次读**里附带每期明细（`plan_id → 金额`，按 plan 累加而非赋值，因为 `payment_allocations` 上没有 `(payment_id, plan_id)` 唯一约束，同一期两行是可表示的、后一行不能覆盖前一行），使列表上的合计与对话框里的预填不可能是两个答案，也省掉浏览器对资金表的第二次查询（`npm run check:security` 的 `client-side-supabase-read:payments/page.tsx` 因此仍是基线 1，未被抬高），`force-dynamic` + `private, no-store`；页面四张 KPI 卡全部改读 `totals`（作废金额与笔数作为 Recorded 的副行单独报告，聚合缺失时显示 `allocationUnknown` 而不是把 0 当答案）、新增第四个 voided 页签、三态徽章、作废原因与 `voided_at` 上屏、三个操作按钮一律 `canSettle && paymentAllows*`、新增作废对话框（原因必填，与路由的 400 及例程的 22023 一致）、分配对话框改为**预填**当前分配（取自列表路由随行返回的每期明细）并以 `allocationDraftStatus()` 取代 `getAllocatedTotal()`、计划行改显 `allocated_amount`；两个 server action 补 `revalidateSettlementPaths()`（`/contracts`、`/payments`、`/dashboard`）；en+zh 各新增 15 个 `payments.*` 键。**顺带澄清（非缺陷）**：列表路由此前缺少 `isSales` 过滤并不是越权读 —— `policy_payments_select_sales` 与 `policy_payment_allocations_select_sales` 已把行限定在调用者自己的合同上，RLS 仍是边界；本轮显式加上的 `.in("contract_id", …)` 是纵深防御，并顺手修掉一个真实陷阱：销售名下零合同时空的 `.in()` 列表会被 PostgREST 当作"全部行"而非"零行"，故提前返回空响应。验收：`tests/security/payment-state-model.test.mjs` 30/30（判定函数**被执行**：作废付款不计入 recorded/pending、`voided` 压过 `confirmed`、分配上限就是付款金额、`0.1+0.2` 视同 `0.30`、缺失聚合既不当 0 求和也不反算成全额未分配；行守卫与 `20260817000000` 里**最后一次**定义的三个例程的实际 raise 逐条对齐，并断言 `void_payment()` 不含"须先确认"以免开始提供例程会拒的按钮）+ `money-grant-coupling` 的 handler 环扩到 `openVoidDialog(payment)` 5/5 + `payment-idempotency-boundary` 21/21 + `npx tsc --noEmit` 通过；源码类断言另做变异对照：对 `HEAD` 的三份旧文件跑同一批正则，14 条中 13 条判红（第 14 条因此从 `select("*")` 收紧为任意通配 select）。**残留（未关闭）**：`installment_plans.paid_amount`（遗留触发器）与 `.allocated_amount`（当前模型）是两个互相竞争的字段，只有后者被维护，本轮只停止在 UI 上展示前者，未收敛数据模型；付款分配仍在待登录态 UAT 清单上；本轮未触碰生产。**R5 追加（同一笔钱的第二个谓词，以及一个没有逐出口的缓存）**：（1）**缓存** —— `src/lib/api-cache.ts` 是进程内 `Map`，30 秒 TTL，**一个逐出导出都没有**，而 `revalidatePath()` 触不到模块状态。10 条路由 import 它，其中 5 条是资金读（`analytics/summary`、`contracts/list`、`dashboard/summary`、`pipeline/list`、`settings/data`）：一笔付款被确认/作废/重分配之后，这 5 条在剩下的 TTL 里继续供写前数字，而 `/api/payments/list`（force-dynamic）供的是新数字 —— 两个页面、两个答案、一份账本；`/contracts` 列表的首付徽章又驱动 Remind 按钮，于是一个过期的 "unpaid" 会去催一笔已入账的钱。修法：这 5 条移除缓存 + `force-dynamic` + 统一走 `applyPrivateNoStore()`（六份各自的 `NO_STORE` 局部常量合并成一份，那正是角色作用域的资金响应丢掉 `Vary` 的由来）；另 5 条非资金读（`alerts`、`dashboard/team-ownership`、`tasks/list`、`team/list`、`workbench`）**保留**缓存，边界由测试从“这条路由到底查了什么表/列”**推导**而不是名单 —— 名单正是七条路由能漂走的原因，`dashboard/payment-tracker` 通过 `contracts` 嵌入读 `payments`，对只认 `from("payments")` 的检查完全隐形。（2）**谓词** —— 数据库每一个派生总额都算 `confirmed = true and voided_at is null`，而七条路由只算 `confirmed`；`dashboard/payment-tracker` 甚至没 select `voided_at`，于是它的 `collected`、`outstanding` 与每个销售的 `collection_rate` 把已作废的钱当现金 —— **作废一笔付款会抬高销售的回款率**。修法：JS 侧只一份拼写 `countsAsCash()`（B8 已建的 `src/lib/payment-state.mjs`，不新增模块），SQL 侧只一份 `.eq("confirmed", true).is("voided_at", null)`；现测量：`analytics/summary` 2+1、`dashboard/payment-tracker` 2、`dashboard/summary` 1+1、`weekly-trends` 1（SQL）、`pipeline/list` 1、`dashboard/page.tsx` 1。（3）**第二个读模型** —— `/api/payments` 的 GET 是与 `/api/payments/list` 不同行形的另一份付款读（`select("*")` + 自己的 `confirmed` 过滤），本轮删除，该路由只剩 POST（断言导出方法集恰为 `["POST"]`）。（4）`api-cache.ts` → `api-cache.mjs` + `api-cache.d.mts`：门禁要**执行**这个 Map 的过期行为（源码正则关不住“陈旧窗口是真的”），而 `.ts` 不能被 `node --test` 直接 import。（5）**i18n**：徽章三态，新增 `leadDetail.paymentNotConfirmed`（en/zh）并与 `pendingConfirm` 刻意不同字 —— `v_lead_trace.confirmed = false` 同时覆盖“待确认”与“已作废”，把两者都叫 pending 正是告诉操作员“已退的钱还在路上”的那句话。**行为证据（PG 17.10 Debian 17.10-1.pgdg13+1，隔离 replay 库，`supabase/replay/21_payment_predicate_divergence.sh`，两个方向都跑且复现方向先跑）**：这条发现只在“一行能同时 confirmed 且 voided”时才是缺陷，而它能 —— `guard_payments_write()` 无条件拒绝直改 void 列，却把“已确认不可变”那组检查放在 `money_direct_write_is_blocked()`（= 直写 **且** strict）之后，而作废后 `old.confirmed` 已经是 false。实测：经例程 stage 7654.00（insert → `confirm_payment` → `void_payment`，后两步都要求回 `"success"`），行为 false/voided、**两个谓词都是 0.00**（不先堆出分歧，否则后面量的就是自己的夹具）；无出口的循环：`allocate_payment` 答 22023 'payment must be confirmed before allocation'、`confirm_payment` 答 22023 'a voided payment cannot be confirmed'，**各两次逐字一致且行不动**（被拒的分配也没留下 `payment_allocations` 行、没动 `allocated_amount`）；`update payments set voided_at = null` **两种模式都是 42501**（反转是单向的，所以矛盾行只能用“重新确认”搭出来）；中断/并发：那条直写在自己事务内生效、持恰好 **1** 把 advisory share lock，且重取 `money_release_mode_lock_key()` 不增 `pg_locks` 行（**身份而非计数** —— 这正是它与 17_/18_ 量到的那把排他锁同一把的可观测形式），`rollback` 后一点不留。结果：**compat 下同一条 `update payments set confirmed = true`（合约自己的销售、过 RLS、`set local role authenticated`）被接受** → row=true/voided、ledger=0.00、loose=7654.00，差额恰好等于那笔被作废的 7654.00，已提交且对一个从没持过该写的新连接可见；**strict 下同一条被 42501 拒绝**（'payment confirmation, amount and linkage change through confirm_payment() and allocate_payment()'）、行不动、两个谓词都 0.00。收尾在判定之前：还原 mode、删掉那笔付款、回测 `first_payment_status`/`allocated_amount`/无 `kpi_targets`/无 `projects` 行，退出状态 `strict|0|unpaid|0.00` 与入场一致。一次干净容器的全量 `MODE=branch`：`replay OK: 20 migrations, 341 release assertions, 2 rollback companion(s), 54 post-rollback assertions, 1 recontract companion(s), 18 post-recontract assertions`。**离线门禁**：`tests/release/payment-predicate-gate-contract.test.mjs` 12/12（两方向都接 `|| fail`、复现先跑、`EXPECT` 无默认值且只收 compat\|strict、verdict 比的是两个谓词而不是源码、数字从另一条连接读回、拒绝逐字且重入、单向反转在选模式前就测、锁身份而非计数、**gate 自己一行 `sleep` 都没有**、无例程就判红而不空过、收尾在判定之前、周期与付款 id 在 replay+migrations 全集里独一无二、以及把 JS 那一半绑到一个**真执行**谓词的测试上）；`tests/security/api-cache-money-boundary.test.mjs` 11/11（资金误读集合由查询推导；缓存的陈旧窗口、过期分支与 `cacheSize()` 都**执行**）；`payment-state-model` 30/30；`i18n-uat-keys` 7/7；`payment-route-behavior` 6/6；`forced-password-actions-boundary` 8/8。**本轮自测发现并修掉的三处真回归（都是 R5 自己弄出来的）**：（a）`payment-state-model.test.mjs` 钉的是列表路由源码里的 `no-store` 字面量，而 R5 把它换成了共享 helper，于是断言改为**跟着委派走**（route 必须 import 并在两个出口调用，helper 自己必须发 `private, no-store, max-age=0, must-revalidate`）—— 钉字面量正是六份局部 `NO_STORE` 能各自写短一点的原因；（b）两个用 CJS loader **真跑**路由/action 体的行为测试因新 import 解析不到而整体报错：`payment-route-behavior` 缺 `@/lib/request-auth-context` 桩（桩的两个头值**从真模块源码读出**，不再写第二份，读不到就判红），`forced-password-actions-boundary` 缺 `@/lib/payment-state.mjs`（改为把 `src/lib/*.mjs` 全量**真模块**注入 mock 表，因为该测试是动态枚举 actions 目录的，手维护纯 helper 名单会让边界因无关原因判红）；（c）lint：`contracts/page.tsx` 里 `Date.now()` 在 render 内（`react-hooks/purity`）是**既有**发现，但我改了相邻行使其代码框指纹变化而被报成 new —— 不重新盖章，而是从同一次时钟读数 `today` 派生 +7d 边界（`new Date(<string>)` 是纯解析，日期结果不变，也顺带黄了两次读钟跨午夜的边缘情形），以及测试里 `const module` 改名（`@next/next/no-assign-module-variable`）。**全量门禁**：`npm test` **897 tests / 894 pass / 0 fail / 3 skipped**（exit 0）；`npx tsc --noEmit` 通过；`node scripts/check-lint-baseline.mjs` —— 399 current errors, no new errors over baseline。**残留（本行新增，未关闭）**：`v_lead_trace` 没有 `voided_at` 列，且**仓库里没有任何迁移创建这个视图** —— 它是生产独有对象，属迁移历史完整性缺口（C4 家族，本轮只登记）；7 个文件 59 个缺失 i18n key（R5 范围外）；`LeadSalesProcess.tsx` 22 处可证死的 `t()` fallback（棘轮 `<= 22`）；`src/app/api/auth/me/route.ts` 自带一份局部 `applyPrivateNoStore`（被既有测试钉住，本轮未合并）；`allocate_payment()` 的 voided 分支在实践中不可达（非空数组检查与未确认检查都在它之前，本轮实测）；本轮未触碰生产 | ⚠️ | 待部署 |
| PROD-ROLLBACK-DB-PHASE-UNCOUPLED | scripts/check-release-phase.mjs, scripts/check-release-manifest.mjs, infra/release/release-manifest.json, infra/systemd/newme-production-rollback.sh, infra/systemd/newme-deploy.sh, scripts/phase-tool-drill.sh, supabase/preflight/expand-contract-rollback.md, tests/release/database-phase-coupling.test.mjs | CREATE+MODIFY | 四审 C8：「数据库相位回滚未与生产应用回滚耦合 —— 应用回滚不验证也不切换 DB 模式；contract 历史可以说已应用而模式还是 compat。要求的收口：一个持久的相位状态机。回滚必须在切回 f37 之前验证 compat，候选完成必须要求 strict，re-contract 必须是一次新的前向迁移。」缺陷成立且比措辞更急：state 4（contract 已应用）下把 `current` 符号链接移回上一版 release，那一版每一笔直连资金写入都会被 `strict` 以 42501 拒绝 —— 一次**由回滚自己制造**的线上故障，而回滚脚本从头到尾没问过数据库一句话。修法是把「现在哪些 release 可以跑」变成数据库的属性 + 每个 release 自己的声明：`public.money_direct_write_mode()` 三态（`absent` 函数不存在 / `compat` 接受上一版的直写 / `strict` 拒绝），每个 release 在自己树内的 `infra/release/release-manifest.json` 写 `runs_under.database_phases`；**上一版 release 没有这个声明**，而它的诚实读法不是"什么都行"而是前机制契约 —— `absent` + `compat`、**永不含 strict**，这一条默认值就是让 f37 回滚被拒的全部来源（默认成全集则本门禁存在的那次回滚会默认通过）。两个判定都 fail-closed：`--for-switch --release-dir DIR` 由 `newme-production-rollback execute` 在**触碰任何东西之前**问（连 live 资产快照都还没做），拒绝即 `exit 70`；`--for-completion` 由 `newme-deploy finalize <sha> pass` 在改写证据文件之前问，只有 `strict` 放行 —— 因为完成宣称的是"整个 release 已上线"，而模式为 `compat` 时 contract 那一半根本没生效，无论迁移历史怎么记。一个 resolver 两个 judge 全为纯函数（`resolveDeclaredPhases`/`judgeSwitch`/`judgeCompletion`），`readLiveMode` 是唯一带副作用的部分：`begin read only` 里**先**查 `pg_catalog.pg_proc`（`pronargs = 0`）**正面确立缺失**、再调函数 —— 把 `undefined_function` 当 `absent` 会把被收回的 EXECUTE、打错的名字或 search_path 问题一并读成 state 1，而那是唯一能让前机制 release 在 strict 下通过的答案；连接失败只打 `error.code ?? error.name`（pg 的连接错误会把 URL 抄进 message）。清单侧 rule 7 由 `scripts/check-release-manifest.mjs` import 部署路径**同一个** resolver（两道门禁因此不可能各说一套）：缺声明、`required_for_app` 非空却声明 `absent`、缺 `strict`、`deferred_contract` 非空却缺 `compat`，一律判红。**持久状态机**：观察到的模式写进回滚那份 10 行未决记录的 `db_phase=`，由 `status` 以 `rollback_db_phase=` 报告 —— 只读该记录、**不开任何数据库连接**，故监控探针永远不需要迁移凭据 —— 并以 `NEWME_DB_PHASE=` 落 journald。两条恢复路径**故意不设门禁**：expand 相位把模式留在 `compat`，`recover_preswitch_deploy` 与 `restore_original_transaction` 因此照常通过，只有 contract 之后的切换被拒；一道相位门禁不该挡住"部署失败要退回去"。**故意偏离 C8 措辞一处**：C8 要求 re-contract 是新的前向迁移，本仓库的返回路径是手工运行的 `recontract_money_direct_write_contract_phase.sql`（理由见 PROD-ROLLBACK-HAS-NO-RETURN-PATH：编号迁移一旦被记录就永不再被应用，新前向迁移在第二次回滚之后仍是同一条死路），runbook §2 的 state 6 与"被连续应用两次仍成立"的验收即为此，已在文档里写明这是偏离而非漏做。 **离线验收**：`tests/release/database-phase-coupling.test.mjs` **17/17**（含"缺失必须被正面确立"、"拒绝路径不得回引 `error.message`"、runbook 必须写明 exit 70 与 `rollback_db_phase`）；`node --test tests/release/*.test.mjs` **259/259**；`npm test` **781（778 通过 / 3 跳过 / 0 失败）**；`check:taskboard` 18 PASS / 0 FAIL、`check:release-manifest`（required_for_app 17 / deferred_contract 1 / 目录待应用 18）、`check:migration-history` 均 OK。 **真库验收（不是 stub）**：`scripts/phase-tool-drill.sh` 新增第 11 步（CI job `migration-replay`）在 PG 17.10 上把 §2 状态表逐格**测出来**：state 4 → 完成放行、切到本 release 放行、切到**无声明**的前机制 release 被拒且消息指名 `rollback_money_direct_write_contract_phase.sql` 与 `target declares : absent, compat (undeclared, no manifest in the release)`；手工跑回滚伴随文件（state 4 → 5）后模式读回 `compat`、同一次切换转为放行、完成被拒并指名 `db-phase-push.mjs --phase deferred_contract`，**而此刻 `supabase_migrations.schema_migrations` 里 contract 版本的行数为 1** —— C8 的"历史说已应用、模式却是 compat"两半在同一处被同时测量；再跑 recontract（state 5 → 6）两个判定各自回到 strict 的答案。state 1 在**第三个从未被任何相位碰过的库**上测（切本 release 拒、切前机制 release 放行）。三条负控制必须判红：装一个 present-but-unusable 的 `money_direct_write_mode()`（raise 42501）**不得**被读成 `absent`；任何拒绝**不得**在 stdout 打印模式（调用方会把那一行记成一次可以做的切换）；group-readable 的 URL 文件必须被拒。实测尾行 `== phase drill OK: 11 steps, 3 databases, 4 + 7 refusals + 3 negative controls, 18 migrations applied in two phases and reproduced through the gate, release↔phase coupling measured in absent, compat and strict ==`。drill 自己 exit 0 与第 11 步在不在无关，故由 `database-phase-coupling.test.mjs` 最后一条测试从**外部**钉住该步骤存在、钉住那几条断言文本、并要求 CI 里这一步不带 `continue-on-error`。 **未关闭**：两个 shell 调用方（`newme-production-rollback.sh` 的 switch 门禁、`newme-deploy.sh` finalize 的 completion 门禁）只有**源码级**断言，**从未在生产主机上执行过** —— 它们随控制面 bootstrap 一起生效（见 PROD-CONTROL-PLANE-BOOTSTRAP）；`/etc/newme/migration-db.url` 未落地前回滚连模式都读不到（按设计 fail-closed，exit 70，而"读不到就不切"正是本条要的行为）；Windows 开发机上 POSIX 归属/权限断言恒真，URL 文件 mode 那条负控制只在 Linux 容器与 CI 里成立 | ⚠️ | 待部署 |

| PROD-CHECKOUT-REWRITES-RELEASE-BYTES | .gitattributes, tests/release/checkout-byte-fidelity.test.mjs | CREATE | 五审 C4-7：「修 CRLF 假红，但不得删或跳过 Windows 测试。」**失败行为先测出来再修**：在 `166101e2ad6` 上开一个真 `git worktree`，`core.autocrlf` 保持 Windows 默认 `true`（该设置同时写在系统 gitconfig 与本仓库 `file:.../.git/config` 两处，仓库内无 `.gitattributes`）——checkout 把 **126/126** `supabase/migrations/*.sql`、**29/29** `scripts/*.sh`、**27/27** `scripts/*.mjs`、**5/5** `infra/systemd/*.sh` 全部写成 CRLF，而 `git status` **一行都不报**（clean/smudge 不对称：`text` + `eol=lf` 下 smudge 是 no-op、clean 把 CRLF 归一回 LF，于是磁盘上的字节错了而索引比对是干净的）；`node --test tests/release/*.test.mjs` **259 个测试 250 通过 9 失败，散在 6 个文件**；`scripts/verify-remote-migration-history.mjs` 对 **120/120** 个文件以 `local_content_line_endings` 拒绝。九条失败全部与生产无关：都是断言的正则带 `\n` 或 `$`、比对的是本 release 要发的脚本本身——**一次假指控，且重跑永远清不掉**。两种把它改绿的错法是「把每条正则放宽」和「在 Windows 上不跑这些测试」，C4-7 明确禁止后者，所以修法落在 checkout 上：**最小修 = 一个新文件** `.gitattributes`，`* text=auto eol=lf`，加 4 条 `-text` 豁免与 3 条 `binary`。豁免用 `-text` 而不是 `eol=crlf`：这 4 个 blob 在规则存在之前就以混合行尾提交（`build.log`、`docs/meta-ads-auth-instructions.txt`、`src/app/api/leads/\[id\]/quality/route.ts`、`tests/integration/api-validation-static.test.mjs`），`text=auto` 单独会在下一次写入时归一它们、`eol=lf` 又会把 CRLF 原样发回工作树，于是**每个工作树永远报它们已修改**——同一个假红换了地方；`-text` 让字节原样保留且报告干净。**同源同提交的差分**：同一个 worktree 同一个 commit 加上属性、`core.autocrlf` 仍强制为 `true` → **0 CRLF、259/259 通过**；同 commit 的 LF 对照 worktree **0 失败**；全量差分 79 vs 70 失败，只差的那一组正是这 9 条。**门禁自己的 CRLF 拒绝原样保留**：`.gitattributes` 只管 git checkout，以 archive/copy/rsync 到主机的树没有属性可施，对那棵树这条拒绝是字节等价宣称与被改写文件之间唯一的东西——删掉它是把假红换成假绿。**回归证明（决定性）**：`text=auto` 会不会让工作树永久脏，用测量而不是推理回答——在一次性 worktree 的 detached HEAD 上做探针提交 `7e9e140d685`（**未移动任何分支**），在环境 `core.autocrlf=true` 下全新 checkout → **`git status` 0 行、1009 个 `w/lf`、0 CRLF**。主树就地重新归一：`777 w/crlf + 51 w/mixed + 183 w/lf` → **1007 w/lf**，5 个 CRLF 噪音 migration 以 `sha256sum -c` 逐个 **OK**、未纳入暂存，未跟踪的 HANDOFF 存活；残留 829 条 ` M` 是 stat 缓存的化妆问题（`git diff --numstat` = 0 行、`git diff --cached --numstat` = 0、`git hash-object` == 索引 blob），且已被上面那次全新 checkout 证明**不会随提交传播**。**离线验收**：`tests/release/checkout-byte-fidelity.test.mjs` **6/6**，逐条都带负控制——(1) 必须声明 `* text=auto eol=lf`、任何路径不得 `eol=crlf`、4 个豁免必须是 `-text`；(2) 386 个字节敏感文件（`supabase`/`scripts`/`infra`/`tests`/`.github`，下限 300 防家族被改名后测了个空）逐个 `git check-attr` 解析必须是 `text=auto` + `eol=lf`，豁免集反向断言必须 `text=unset`、且必须 `< 10` 个（窄性上限），二进制取 git 自己的 `i/-text` 分类而不是后缀表，负控制 `build.log` 必须解析出 `text: unset`；(3) 索引里除这 4 个外不得有任何 `i/crlf`/`i/mixed` blob，且 4 个都必须 `attr/-text`；(4) **端到端**：`git -c core.autocrlf=true checkout-index -z --stdin --prefix=<tmp>/` 之后按 `blob <len>\0` 自算 SHA1 与索引 blob id 逐个比对，负控制把一个样本 migration 的 `\n` 改成 `\r\n` 后 id 必须不同、原字节必须相同；(5) 内容门禁对 CRLF 树仍以 `local_content_line_endings` 拒绝、对同一棵 LF 树不以该原因拒绝；(6) **没有测试被删或被跳过**——6 个 CRLF 敏感文件必须仍在且仍注册测试，跳过清单必须**恰好等于** `DECLARED_SKIPS`（`tests/security/supply-chain-gate.test.mjs` 3 条既有遗留跳过），且**首次测试注册之后任何一行代码不得读取宿主平台**（位置规则而非模式匹配：测试体里一句 `if (process.platform === "win32") return;` 与 `.skip` 同样抑制覆盖却匹配不到任何 skip 模式；比对前先剥掉 `//` 注释与 `^\s*\*` 行，否则本文件对该规则的说明会绊倒自己；6 处合法的 `win32` 都在文件头做路径/解释器翻译，均在首次注册之前）。**两侧都做了变异测试**：删掉 `.gitattributes` → 6 条里 4 条失败（两条独立于属性的正确地仍然通过）；在 `tests/unit/stage-guard.test.mjs` 追加一条平台早退的测试 → 第 6 条失败并指名 `a test consults the host platform after registration begins`；追加一条只在注释里提到 `process.platform` 的测试 → 6/6 通过（剥注释后不误报）；两次变异后该文件 `git diff --numstat` 均为 **0 行**。**未关闭**：Linux CI 上的 exact-head 复核（Windows 开发机无法证伪 POSIX 侧行为）；4 个混合行尾 blob 未做内容归一（那是对本轮并不触碰的文件做内容改动，故留待单独一批）；此前 `pinned-commit-verification-recipe` 里那步「手工把验证 worktree 归一为 LF」在本提交之后不再需要，属文档跟进 | ⚠️ | 待 exact-head CI |
| PROD-FINGERPRINT-AND-TXCONTROL-COERCION | scripts/verify-remote-migration-history.mjs, scripts/db-phase-push.mjs, scripts/statements-fingerprint-parity.mjs, scripts/phase-tool-drill.sh, tests/release/release-phase-manifest.test.mjs, tests/release/remote-migration-history-reconciliation.test.mjs | MODIFY | 五审 C4-6：「statement fingerprint 对 NULL、空元素、非一维和异常下界 fail closed；phase tool 拒绝 ROLLBACK、ABORT、CHAIN、SAVEPOINT、RELEASE、PREPARE TRANSACTION 等破坏外层事务的语句，并补真实 PG mutation tests。」两条缺陷成立，且**都是"强制转换让摘要说谎"这同一个形状**。**(1) 指纹域 —— 失败行为先在真 PG 17.10 上测出来**：`coalesce(s.statement,'')`（SQL 侧）与 `String(x ?? "")`（JS 侧）把"CLI 什么都没记"映射成"CLI 记了一条空语句"，于是**四类值 count 与 digest 全都相同**：`{ok,NULL}` ≡ `{ok,''}`（同为 `cb5b94d08b987f9c…`）、`statements` 为 NULL 的列 ≡ `'{}'`（同为 `9a271f2a916b0b6e…`）、一维 `{a,b}` ≡ 二维 2×1（同为 `a4b041cd2d567a0e…`）、`'[0:1]='` 与 `'[5:6]='` ≡ 下界 1；另测得二维 2×2 时 `array_length(a,1)=2` 而 `cardinality=4`（`array_length` 只数第一维、`unnest` 却拉平，于是表头根本不描述被哈希的载荷）。**一个能被没人应用过的行满足的摘要，正是本门禁存在的理由的反面**。修法是**收窄定义域而不是补 coalesce**：`STATEMENTS_WELL_FORMED_SQL`（`is not null` + `coalesce(array_ndims,1)=1` + `coalesce(array_lower(...,1),1)=1` + 无 NULL/空元素）单点导出，**同时插值进 digest 与 count**（count 若算在域外，就会去描述 digest 拒绝描述的值，同一行两个字段互相矛盾）；空数组仍在域内，因为 `array_ndims`/`array_lower` 对空数组为 NULL 而 `cardinality` 为 0，那个 coalesce 恰好只放行 `'{}'`；改用 `cardinality` 而非 `array_length`。JS 侧改为**抛** `UnfingerprintableStatements`（`code=unfingerprintable_statements`）而不是返回 null —— **两个 null 相等，返回 null 就是一次假绿，抛出的错误无法被比较成通过**；`rowContent` 捕获它并落到既有的 `content_unmeasured` 拒绝，读回路径另加 `statement_count === null || statements_sha256 === null` 一条。**没有任何已固化产物被作废**：两版对**每一个都接受的值**给出逐字节相同的 digest（编码没变，是定义域变窄了），仓库内 fixture 为 `capture: null, rows: [], accepted: []`，且 120 个迁移文件实测 **0 个零语句文件、0 个空/非字符串元素**。**(2) phase tool** ：`isTransactionControl()` 只回答"能不能跳过"，它答不的一律被执行 —— 其中 `rollback` 不只是丢弃迁移，它**结束事务块**，之后的 history 行自己提交，得到"版本记为已应用、它建的东西一个不在"，而**已记录的版本永不再被应用**，于是该相位永远"已应用"且对象永远缺失。形状取**白名单**（破坏外层事务的写法随每个 PG 版本增长，故首关键字落在事务词表里的语句一律拒绝，除非精确等于两种可跳过形式），`breaksOuterTransaction()` 只返回关键字、绝不返回语句正文；拒绝在**整相位预检**里（任一文件有一条，前面的文件一个都不应用），并在**真正会发送它的那一行**再检一次（`transaction_control_in_apply`）。**离线验收**：25/25 敌对写法被拒且逐条报出所分类的关键字（含用户点名的 6 种 + `set transaction`/`set constraints`/`set session characteristics`/`discard`/带隔离级别的 `BEGIN`/被注释遮蔽的形式/`rollback prepared`/`commit prepared`/`and chain` 三种），12/12 可跳过形式放行，9/9 普通语句（含 `select 'rollback';`、`create table savepoints`、`insert into release_notes`、plpgsql 函数体里的 `begin…end`）不受影响；对**磁盘上 126 个 .sql 的全部语句**实测 **0 次误拒**且跳过路径非空（`total > 1000` 与 `skipped > 0` 双下限，防"拒绝一切"或"什么都没测"）；另以源码位置钉住预检在 apply 循环**之前**、纵深检查在其**之内**。**真库 mutation 验收（drill 第 12 步，第 4 个一次性库，PG 17.10）**：先把危害本身测出来 —— 事务中 `rollback` 之后，前面建的表消失而后面的语句**自己提交**（`true|true`）；再把 8 条语句逐一注入**复制到 `$WORK` 的真 release 树**（仓库内文件一个不动，仍守 C7），每次以 `check-release-manifest.mjs --stamp` 重算清单（否则拒绝的原因会是哈希不符，与本步无关），要求 exit 1 + 消息指名关键字 + `schema_migrations` **0 行**（连表都不该被 `--init-history` 建出来）；正控制是同一棵树同一个库同一条命令去掉注入后**应用 17 个迁移**（没有它，一棵坏掉的副本给出 8 次一样的拒绝会读成通过）；**最强的一条是把守卫自己变异**：在副本里把 `breaksOuterTransaction` 退回 C4-6 之前的行为并重注 `rollback;`，实测工具 **exit 0**、`20260817150000` 被记为已应用、而只有该文件创建的 `public.clear_kpi_targets` **不存在**——即那条永远清不掉的部署死锁被当场造出；它自己再带一条控制（同一个被变异的工具、去掉注入 → 见证对象在位），故损害归因于被注入的语句而非变异本身；见证对象由 `grep -l` 断言恰好属于一个编号迁移，将来被挪走会**响亮地失败**而不是静默测空。drill 尾行 `== phase drill OK: 12 steps, 4 databases, 4 + 15 refusals + 7 negative controls, 18 migrations applied in two phases and reproduced through the gate, release↔phase coupling measured in absent, compat and strict, 8 transaction-control mutations refused with nothing applied and the superseded classifier measured recording a rolled-back migration ==`，且该步由 `release-phase-manifest.test.mjs` 从**外部**钉住（8 条注入、`--stamp`、四条断言文本、`MUTANT` 必须在 `$WORK` 下、CI 该步不得 `continue-on-error`）。**双侧指纹平价（真 PG 17.10）**：23 个对抗向量（其中 **6 个要求两侧都拒绝**，2 个下界向量属"服务端拒绝即全部防线"）+ 120 个迁移 + 2 对边界 = 143 行逐行相等，并新增 **5 对必须相异的碰撞对，同一次查询里用被废弃的表达式求值、实测 5/5 在旧式下碰撞而现在 0 碰撞**（F-05：没有负控制的测量等于没测量）；`--self-test` 143/143 全部报为差异。**顺带修掉两处会读成假绿的写法**：旧测试断言 `statementsFingerprint([]) === statementsFingerprint(null)`（正是被修掉的那个碰撞）与 `["a"] != ["a",""]`（只要求不等，而正确答案是后者根本不可指纹化），已改为对 9 类域外值逐一断言抛出并核对 `reason`。**离线验收**：`node --test tests/release/*.test.mjs` **268/268**（新增 3 条，原 265）；`check:taskboard` 18 PASS / 0 FAIL / 46 UNFINISHED、`check:migration-history`（126 .sql、hand-run companions 5、manifest vs git 81956f2ff3bf）、`check:release-manifest`（required 17 / deferred 1 / pending 18）全 OK。**未关闭**：Linux exact-head CI 复核（Windows 开发机跑不了 psql，本轮真库证据全部取自 `postgres:17.10` 容器） | ⚠️ | 待 exact-head CI |
| PROD-STRICT-POSTURE-UNDERVERIFIED | infra/release/release-manifest.json, supabase/migrations/recontract_money_direct_write_contract_phase.sql, supabase/migrations/rollback_l0_20260811.sql, supabase/replay/20_assert_post_rollback.sql, supabase/replay/30_assert_post_recontract.sql, tests/release/mode-controlled-guards.test.mjs, tests/release/expand-contract-rollback-contract.test.mjs, supabase/preflight/expand-contract-rollback.md | CREATE+MODIFY | 五审 C4-2：「strict posture 核验全部 7 个 mode-controlled guards 及 rollback 影响的 KPI RPC。」两条缺陷都成立。**(1) strict posture 是 compat 的真子集，且点名的 guard 集合两个方向都错**：strict 只有 3 条谓词（compat 7 条），两边点名的四个触发器把 `trg_guard_contract_transition` **算了进来**（它从不读发布模式，因此那一项永不可能失败），又**漏掉** `trg_guard_installment_plans_write`、`trg_guard_contract_approvals_write`、`trg_guard_payment_allocations_write`（自 `20260814000000` 重写后三者都读模式）。于是 `count(*) = 4` 在三个真 guard 被 drop 之后**仍然成立**——先把失败行为测出来：PG 17.10 上三条 `drop trigger` 之后 `db-phase-push.mjs --verify-only --phase deferred_contract` 仍 `rc=0 ok=3 failed=0`。**实测清单（同一库）**：调用 `money_direct_write_is_blocked()` 的 enabled 触发器共 **6** 个（`contracts`、`payments`、`installment_plans`、`contract_approvals`、`payment_allocations`、`quotations`），背后 **5** 个不同函数（`guard_definer_only_write` 支撑其中两个）；`trg_guard_contract_transition` 是第 7 个 guard 触发器且**不受模式控制**。复审说的「7 个」对应源码里 7 处 `if not public.money_direct_write_is_blocked() then`（`20260814000000` 4 处 + `20260817000000` 3 处），在库里收敛为 5 函数 / 6 触发器——数字对齐写在测试与 runbook 里，避免下一个人再按 7 去凑。**(2) 回滚把 KPI 保存路径做成死路**：`rollback_l0_20260811.sql` 无条件 drop `public.replace_kpi_targets(text, jsonb, uuid)`，而该签名的现行定义来自 `20260817000000` §14（带 A1 会话边界 + B7 结转）；**已记录的迁移永不重放**、recontract companion 按章程只碰一张表一行，于是回滚后数据库停在 strict 且**没有任何前向产物能把 KPI 保存路径装回来**，而旧 `--verify-only` 对此报 3/3 OK。**同一个错四元组被四份产物重复**：manifest 的两个 posture、recontract 的 `v_guards`、`30_assert_post_recontract.sql`，以及**测试自己**——`expand-contract-rollback-contract.test.mjs` 的 `assert.equal(manifestGuards.length, 4)` 是在**执行**这个缺陷而不是检查它（两份错清单彼此一致不是证据），runbook 散文里另有两处。**修法：把查表换成集合相等，不再维护任何整数**。谓词以声明的 (触发器, 表) 六元组 `full join` 实测集合，断言 `count(*) > 0 and count(*) = count(d.tgname) and count(*) = count(live.tgname)`——漏一个 guard 会红，**多出一个未声明的**受模式控制的 guard 也会红。用**对**而不是名字（`guard_definer_only_write` 一函数两名，`trg_require_current_session` 证明一名可挂二十表）；实测集合从**函数体是否调用闸门**导出（`pg_get_functiondef(p.oid) like '%money_direct_write_is_blocked%'`），带 `g.tgenabled = 'O'`（仅 disable 也算坏）与 `p.prokind = 'f'`（`pg_get_functiondef` 对聚合会抛）。compat 7→**11**、strict 3→**11**（同样十一条，仅模式断言取反）；新增两条 KPI 例程的在位（`to_regprocedure` 钉签名，缺失得 NULL 匹配不上任何行，`count(*) = 2` 即 fail-closed）、server-only 授权、以及两者**共享同一把 period 锁**。`rollback_l0_20260811.sql` 的 drop 改为有条件：以 `pg_get_functiondef(...) like '%assert_current_session_at_entry%'` 判别当前定义是否 round-4（grep：`20260811100500` **0** 处、`20260817000000` **9** 处），是则 notice 并 `return`，只在还是 round-1 定义时才 drop——即「能删的那一个对象」变成字面为真。**真库验收（`postgres:17.10`，一个模板库 + 每次损伤各自克隆，故为独立测量而非累积）**：负控制 strict、compat 各 `rc=0 ok=11 failed=0`（strict 原为 3）；**26 次独立损伤全部被拒**且逐条点名责任谓词——strict 侧 13（三条 guard drop、`clear_kpi_targets` 授权给 authenticated、两条 KPI 例程各自 drop、`create_contract` drop、闸门函数变 definer、模式函数变 invoker、`revoke_user_sessions` 授权给 anon、`trg_guard_contracts_write` **仅 disable**、transition guard drop、**新出现**一个未声明的 `trg_guard_leads_write`），compat 侧 13（同形状 + 模式行**提前**翻 strict → `release-mode-row-is-compat` + `mode-function-reports-compat`，以及三种 period-lock 损伤：去锁、错键 `hashtextextended('public.kpi_targets', 0)`、会话锁替代事务锁 → 均落在 `kpi-routines-share-the-period-lock`）。**往返（同一库）**：strict 11/11 → 跑全部 rollback companion 后停在 `compat` 且 `replace_kpi=true round4_def=true`（notice：`NOT dropping it`）→ `--verify-only --phase required_for_app` 正确地在 **precondition 阶段**拒绝（库已记录 contract 相位，姿态根本不该被求值）→ recontract 连跑两次回到 strict 11/11（幂等）；三个受模式控制的 guard 被 drop 时 recontract **exit 3** 并以 `42P01` 点名，多一个 `trg_guard_leads_write on public.leads` 时同样 exit 3。**新测试 `tests/release/mode-controlled-guards.test.mjs`（17 条）从 SQL 源码导出真相**：解析 `create function` / `create trigger` / `drop trigger`，按源码位置排序、后定义者胜，因此「某个 guard 何时开始或停止读模式」由迁移自己回答；断言四份声明**都等于**导出集合、都排除 transition guard 却仍按名单独检查它、两个方向的写法都在（`full join`、两个 `count(*) =`、`tgenabled = 'O'`、`prokind = 'f'`）、两个相位都验 KPI 例程、companion 缺例程时必须拒绝、rollback 里**没有顶层 drop** 且唯一那条 drop 位于判别式之后且中间有 `return;`、两个 replay 文件的 `pg_temp.assert(` 调用点数减一等于各自声明的 ASSERT_TOTAL（并扫 `loop … end loop` 保证没有被循环放大的断言）——**第五份拷贝被明确禁止**。**变异测试 7 条逐条红**且报出预期失败名（manifest 少一对、recontract 把 transition guard 算进六个、rollback 恢复无条件 drop、ASSERT_TOTAL 变陈旧、丢掉反向检查、post-rollback 退回「函数已被 drop」的旧见证、post-recontract 不再从闸门调用导出），控制与还原均 17/17，`cmp` 逐字节确认还原。ASSERT_TOTAL 50→**53**、13→**17**。**runbook**：§5.1 拒绝清单重写为六对 + transition guard + 未声明 guard + 两条 KPI 例程及其 server-only 姿态，并**删掉「重跑 `20260811100500_kpi_targets_atomic_replace.sql`」这条会把函数降级、抹掉 B7 的指示**，改为明写「不要重跑」及理由（回滚到 `20260817000000` 之下是另一个需审计的步骤，本轮的拒绝就是预期结果）；§6.3 的「同样三条谓词」改为十一条。**离线验收**：`node --test "tests/release/*.test.mjs"` **285/285**；`check-release-manifest.mjs` OK（required 17 / deferred 1 / pending 18）；容器内四道门禁全绿——`MODE=branch`（18 迁移 / 331 release 断言 / 2 rollback companion / 53 post-rollback / 1 recontract / 17 post-recontract）、`MODE=control` OK、`MODE=history`（APPLIED=2 / TOTAL=120，历史欠账不变）、`phase-tool-drill.sh`（12 步 4 库；`POSTURE_COUNT` 从清单读取，故自动从 3/7 跟到 11，无需改脚本）。**未关闭**：Linux exact-head CI 复核（Windows 开发机跑不了 psql） | ⚠️ | 待 exact-head CI |
| PROD-COMPANIONS-UNBOUND-TO-RELEASE | infra/release/release-manifest.json, scripts/check-release-manifest.mjs, scripts/db-phase-push.mjs, scripts/replay-migrations.sh, scripts/verify-deploy-gate-record.mjs, scripts/phase-tool-drill.sh, infra/systemd/newme-deploy.sh, infra/release/control-plane-bootstrap.md, supabase/preflight/expand-contract-rollback.md, .github/workflows/ci.yml, package.json, tests/release/companion-binding.test.mjs, tests/release/control-plane-bootstrap-contract.test.mjs, tests/release/release-phase-manifest.test.mjs, tests/release/database-phase-coupling.test.mjs | CREATE+MODIFY | 五审 C4-5：「rollback、recontract、KPI companions 纳入 manifest SHA256 与 candidate SHA 和 CI 绑定；partial reentry drill 移除故障后必须重跑至完整成功。」两条都成立。**(1) 手跑 companion 的内容在任何地方都没有绑定**：本 release 有 **5** 个（`recontract_money_direct_write_contract_phase.sql`、`rollback_crm_v3.sql`、`rollback_l0_20260811.sql`、`rollback_money_direct_write_contract_phase.sql`、`rollback_p0_10.sql`），它们是**运维以 service role 手工对生产执行**的 SQL，而三道内容门禁**结构上都到不了**：文件名不匹配 `^[0-9]{14}_` 故 CLI 永不应用、`supabase_migrations.schema_migrations` 永无对应行，于是 `check-migration-history.mjs` 按设计把它们排除在既应用历史之外（这条**不是缺陷**：把它们写进历史等于宣称生产跑过），它唯一的 companion 检查是 `if (!COMPANION_NAME.test(name)) fail(...)`——而那张名单本身就是用同一条正则筛出来的，**是个恒真式**；`verify-remote-migration-history.mjs` 比对的是服务端记录的 statements 与文件，没有记录行就没有可比之物。`replay-migrations.sh` 只执行「覆盖了本分支新迁移」的 companion（`-- ROLLS_BACK:` → `BRANCH_MIGRATIONS`），而 `rollback_crm_v3.sql` 与 `rollback_p0_10.sql` 在本 release 覆盖 **0** 个，故 `BRANCH_ROLLBACKS` 从不选中它们——**没有任何 job 执行过它们**。**失败行为先在真 PG 17.10 上测出来**：往 `rollback_money_direct_write_contract_phase.sql` 追加 `grant select on public.contracts to anon;`、往 `rollback_p0_10.sql` 追加 `grant execute on function public.create_contract(jsonb) to authenticated;`，`MODE=branch` 均 **rc=0**、全部 post-rollback 断言照常通过。**(2) 中断的相位只测到「回滚干净」，没测到「重跑成功」**：drill 第 9 步注入故障后只断言 12 个已应用 / 该文件整体回滚 / 0 行记录，随后就换库了——「可重入」是这套两相位部署的**核心宣称**，而它当时是推理出来的。**修法：清单第 8 条规则 + 单点判定函数**。`release-manifest.json` 新增 `companions` 数组（`file`/`kind`/`sha256`，按文件名序，已 `--stamp`），`auditCompanions({manifest, files, hashes})` 从 `auditManifest` 拆出单独导出，因为 `--verify-companions` 与全量门禁**不许对「声明集合」有两种理解**；集合相等**双向**（磁盘上有而未声明 → 指名拒绝；声明了而磁盘上没有 → 指名拒绝），`kind` 由文件名前缀导出而非自由填写（harness 按前缀决定执行顺序，标错等于一次不会被兑现的执行顺序宣称），并逐个比对 CRLF 归一化 sha256。**三个坑都是实测出来的、不是设想的**：① `readTreeFiles(dir)` 一个读者三个调用方（本门禁、`db-phase-push.mjs`、测试 fixture）——任何一方把 companion 过滤掉，第 8 条规则就**反转**成「清单列了磁盘上没有的 companion」并拒绝每一次 push，与 C4-2 那四份互相不一致的名单同形，故只允许一份实现；② companion 文件名以字母开头、字母排在数字之后，故 `file.slice(0,14) > production_stamp` 对**每一个** companion 都为真——摘要行若不先用 `CLI_MIGRATION` 过滤会把 `pending in tree` 报成 **23** 而不是 18；③ `--stamp` 的替换以 `"file"` 为锚，companion 在 `"file"` 与 `"sha256"` 之间多一个 `"kind"`，旧模式**匹配不上**，而 `stamp()` 在匹配不上时 **exit 1 并指名文件**（不是「restamped 0 file(s)」），故必须放宽——放宽是**承重的**，测试同时断言不含 `kind` 容差的模式对 5 个 companion **一个都匹配不上**、对 phase 条目仍匹配（证明没有贪婪跨条目）。**绑定到 candidate SHA 与 CI**：`replay-migrations.sh` 在计划摘要之后、任何 SQL 之前新增 `== companion content ==`（**每个 MODE 都跑**，这是唯一能覆盖「谁都不执行的那两个」的形式）；CI `migration-replay`（required job）新增 `Hand-run companion content binding` 步（`npm run check:release-companions`，**不带** `continue-on-error`，F-05）；`newme-deploy.sh` 在远端历史门禁之后于 `$WORKTREE`（root 所有、canonical main 的候选树）内跑同一条命令并写 `gate=release-companions-verified`，`verify-deploy-gate-record.mjs` 的 `REQUIRED_GATES` 同步（既拒缺失也拒未知门禁），`control-plane-bootstrap.md` 手工引导块与三处「four gates」→「five gates」同步，三边由 `control-plane-bootstrap-contract.test.mjs` 钉住相等（21/21）。**真库验收（`postgres:17.10`，每次损伤各自一次性库）**：干净树 `MODE=branch` **rc=0**，`== companion content ==` 打出 5 行 `companion OK` 及各自哈希前 12 位，随后 18 迁移 / 331 release 断言 / 2 rollback companion / 53 post-rollback / 1 recontract / 17 post-recontract；**三次损伤全部 rc=1 且 `applied-any=0`**（拒绝发生在任何 SQL 之前，不是「跑到一半发现」）并逐条指名——`rollback_money_direct_write_contract_phase.sql has changed since the manifest was stamped`、`rollback_p0_10.sql has changed ...`（**即那个谁都不执行的文件，这是本条最关键的一次测量**）、`rollback_c45_smuggled.sql is a hand-run companion ... but the manifest does not name it`；三次损伤后 `md5sum` 与备份逐字节相同（仓库内文件一个不动，改动只发生在容器里的可写副本，守 C7）。**partial re-entry（drill 新增第 10、11 步，同一个 `$INTERRUPT_DB`，什么都不重置）**：第 9 步注入 `assert_installment_schedule(jsonb, numeric, text) returns text` 影子函数使真定义（`20260817000000` 的 `returns integer`）以 **42P13** 在文件中途失败 → 12 个已应用、该文件整体回滚、0 行记录；第 10 步 `drop function` 移除故障后**重跑同一相位** → `already applied : 12` / `to apply : 5` / `applied` **5** 次 / `history verified` **17** 次（`history verified` 遍历全部 `phaseEntries`，故跨两次尝试都被核验）/ `posture OK` 11 次 / `OK`，`schema_migrations` **17** 行，并以三个探针断言被回滚的对象确已回来（`payments.credited_to` 列、`payments_amount_positive` 约束、`assert_installment_schedule` 的 `prorettype = 'integer'`）；第 11 步在**这个恢复过来的库**上继续跑 contract 相位 → `posture OK` 11 次（含 `release-mode-row-is-strict`）、`money_release_mode.direct_write_mode = 'strict'`、`--verify-only` OK——即「中断的 expand 相位只是延误，不是必须重建的数据库」由测量而非推理成立。drill 尾行 `== phase drill OK: 14 steps, 4 databases, ... an interrupted phase resumed to complete success and carried on through the contract phase to the strict posture, ... ==`，全部数字从清单读出（`manifest_count expand\|contract\|posture\|strict`、`manifest_before count\|version`），故加一个迁移或一条谓词都不必改脚本。**新测试 `tests/release/companion-binding.test.mjs`（13 条）逐条带变异**：已提交清单 `auditCompanions` 无问题且等于磁盘集合；「谁都不执行的 companion 仍被声明」——**从 `-- ROLLS_BACK:`/`-- RECONTRACTS:` 与 pending 集合导出**而不是写死那两个文件名（下一个 release 改了覆盖关系，这条测的仍是那个 release）；未声明 / 声明了但文件不在 / 哈希漂移 / `kind` 与文件名不符 / 无 sha256 / 重复 / 乱序 / `companions` 缺失或不是数组（三种写法）逐条要求拒绝；companion 被塞进 `required_for_app` 时必须报「hand-run companion ... 从不由 CLI 或相位工具应用」而不是「不是合法文件名」（后者读起来像拼写错误）；两个程序的 `COMPANION_NAME` 字面量**以源码行相等**钉住（`check-migration-history.mjs` 是无导出的顶层程序，这是唯一允许的一份拷贝）；端到端 CLI 在临时目录（不写仓库任何文件）上复现追加 `grant` → exit 1 + `companion BAD` 行 + `refusing: 1 problem(s)`，再加一个未声明文件 → 仍 exit 1；并钉住 CI 步、npm 脚本、harness「先验证后执行」的源码位置与 wrapper 的 `$WORKTREE` 调用。**runbook**：§4 前置条件新增「`check:release-companions` 在该 head 为绿且 5 行 `companion OK` 在 job log 里」并说明为何前两条门禁看不见这些脚本；§5 新增「执行前先验证内容」块（附上那次 rc=0 的测量结果与 `gate=release-companions-verified`）；§5.1 第 2 步把 `--verify-companions` 放在 `psql` 之前并明写「第一条拒绝就不要跑第二条」。**离线验收**：`node --test "tests/release/*.test.mjs"` **298/298**（新增 13）；`check:release-manifest` OK（required 17 / deferred 1 / pending 18 / hand-run companions 5）；`--verify-companions` rc=0；`check:workflows` 3 PASS；`bash -n` 三个 shell 脚本干净。**未关闭**：Linux exact-head CI 复核（Windows 开发机跑不了 psql，本条全部真库证据取自 `postgres:17.10` 容器） | ⚠️ | 待 exact-head CI |
| PROD-DEPLOY-TRUSTS-OPERATOR-MIGRATION-SET | scripts/check-release-manifest.mjs, scripts/verify-remote-migration-history.mjs, infra/systemd/newme-deploy.sh, scripts/deploy-immutable.sh, scripts/verify-deploy-gate-record.mjs, infra/release/control-plane-bootstrap.md, tests/release/release-claim-derivation.test.mjs, tests/release/deploy-release-claim-validation.test.mjs | CREATE+MODIFY | 五审 C4-1：「canonical deploy 从 exact manifest 派生 required_for_app 全集，拒绝操作员真子集；切流前 required 全 applied 且内容匹配、deferred 未提前 applied，并执行 `check-release-phase --for-switch`。」两条都成立。**(1) 被验证的迁移集合就是操作员在命令行上打的那一串**：`newme-deploy.sh` 取 `MIGRATION_IDS=${4:-}` 原样传给 `verify-remote-migration-history.mjs --require-applied`，而该门禁**只重测交给它的 id**——于是**声明同时也是范围**，没有任何一处从 release 派生范围。**失败行为先用该门禁自己的纯判定测出来（本 release 真清单：required 17 + deferred 1）**：① `applied_verified 20260806000000` → **0 findings**，而此时 17 个 required 里有 **16 个根本没应用**，即 app 会被切到一个缺少本 release 全部授权迁移的库上；② required 全部已应用**且** deferred contract 相位 `20260818000000` 已提前应用 → 同样 **0 findings**，而该相位关掉的正是仍在线的旧 release 还在走的直写路径，提前应用就是 `supabase/preflight/expand-contract-rollback.md` §2 专门为之而写的那次故障；③ wrapper 自己的参数正则 `^[0-9A-Za-z_.-]+(,[0-9A-Za-z_.-]+)*$` **接受单个 id**，路径上没有任何一层说不。**修法是派生，而不是接受**：`auditReleaseClaim({manifest,status,claimed})` 返回 `{problems, required, deferred}`，**两个方向的集合相等**（真子集拒绝并逐一点名缺失的 16 个；超集也拒绝，因为本 release 唯一多出来的 id 恰是那个「此刻不许应用」的相位，故它得到一条**指名 runbook §5.1 的更长消息**而不是泛化的「不在 required 里」）；每个相位的版本经本地 `versions(phase)` 校验 `file` 匹配 `CLI_MIGRATION`、`version === file` 前 14 位、`sha256` 为 64 hex，任一不成立即返回 `null`，于是**清单坏掉时 problems 非空且两个集合为空**——绝不产生一个「部分派生集」交给 `--require-applied`，那会让它去静默它本该发现的东西。`--verify-claim` **先跑全量 `auditManifest`**（含规则 1：`required ∪ deferred == pending`），因为派生的前提是清单描述这棵树：一个躺在 `supabase/migrations/` 里而没被写进 `required_for_app` 的必需迁移，会让派生集成为真相的真子集——同一个缺陷下移一层；失败时报的是「the manifest at this SHA does not describe supabase/migrations/, so the required set cannot be derived from it」而不是一句哈希抱怨。**两层都派生，因为两层都可被单独调用**：wrapper 在 `$WORKTREE`（root 所有、canonical main）内派生并把**派生结果**（`REQUIRED_IDS`/`DEFERRED_IDS`，各自再过 `^[0-9]{14}(,[0-9]{14})*$`，空则拒绝）喂给历史门禁与下游 env，`deploy-immutable.sh` 在 `git archive "$SHA"` 出来的 `$STAGE` 内**再派生一次**（`scripts/deploy.sh` 只到得了这一层；且 `check-release-manifest.mjs` 由自身文件位置算 `ROOT`/`MANIFEST_PATH`/`MIGRATIONS_DIR`，调 `$STAGE` 的那份即把两侧都绑到 SHA，而 `$ROOT` 的工作副本并不是被部署的树），位置在 `npm ci` **之前**——一个错的声明不该先花掉一次构建。**范围检查刻意不放进 `validate_release_claims()`**：`tests/release/deploy-release-claim-validation.test.mjs` 把该函数逐字提出来、在只定义了 `SHA=` 的 `set -Eeuo pipefail` 下执行，且那一刻 SHA 的树还不存在，故该函数只判**形状**、范围在 `git archive` 之后判，两处各自的测试都写明了这条分工。**(2) 「不许提前应用」这个方向此前无人测量**：`--require-unapplied` 新增于 `structuralFindings` 2b 段——`deferred_applied_early` 用 `remoteAll` 而非 `remoteByVersion`（任何以该版本记录的行都意味着它跑过，问题是它是否跑过、不是谁记的），消息直接给出补救（`rollback_money_direct_write_contract_phase.sql`，runbook §5.1）；`claim_contradiction`（同一版本被同时要求 applied 与 unapplied 是拒绝，不是二选一）、`claim_unparseable`、`deferred_not_in_release` 三条一并 fail closed。**切流门禁**：`deploy-immutable.sh` 在 `mv "$STAGE" "$RELEASE"` 之后、`write_deploy_state switch_pending` 之前（即 `mv -Tf "$CURRENT_NEXT" "$CURRENT"` 之前）跑 `$RELEASE/scripts/check-release-phase.mjs --for-switch --release-dir "$RELEASE" --url-file "$MIGRATION_DB_URL_FILE" --modules-dir "$RELEASE/node_modules"`——门禁与声明两侧都出自 `$RELEASE`；连接串只以**路径**交出（`NEWME_MIGRATION_DB_URL_FILE` 默认 `/etc/newme/migration-db.url`，从不进参数、env 或日志）；`pg` 可解析是因为 `npm ci` 不带 `--omit=dev`；release 里没有该门禁是**拒绝**而不是跳过；stdout 不含 `NEWME_DB_PHASE=` 也是拒绝（空输出不得读成「已核验的 mode」）。门禁记录随之 5 → **6**（`gate=release-claim-derived`），`verify-deploy-gate-record.mjs` 的 `REQUIRED_GATES`、wrapper 写的行、`control-plane-bootstrap.md` 手工引导块与三处「five gates」由 `control-plane-bootstrap-contract.test.mjs` 钉住相等——**一个不知道这道门禁的 wrapper 因此装不了控制面**。**修后复测（同一纯判定、同一清单）**：真子集 0 → **16 findings**（且每一条都指名缺失版本）、提前应用 contract 相位 0 → **1 finding** 且带补救、正确的切流前状态仍 **0 findings**、矛盾声明被拒；`--verify-claim` 端到端：精确 17 id → rc 0 并打出两个派生集合与 `17 required before the switch, 1 deferred until after it`，单 id → rc 1 + `missing …` + `refusing: 1 problem(s)`，**拒绝时也照样打出派生集合**（那是操作员的下一条命令）；临时目录里塞一个未分类迁移 / 改一个 required 迁移一个字节 → 两次都 rc 1 且报「不能派生」。**新测试 `tests/release/release-claim-derivation.test.mjs`（19 条）**：派生集合、id 三种写法（版本/文件名/`.sql`）与 7 类非法写法、**逐一丢掉任意一个 required 都必须红**（不只测那个单 id 的例子）、deferred 混入 applied_verified、未 required/重复/不可解析/空声明、`not_required` 两个方向、6 种未知 status、7 类坏清单必须**同时**给出 problems 与空集合、`compareHistories`/`auditHistory` 两条方向的 findings、`--verify-claim` 端到端两例、wrapper 与 `deploy-immutable.sh` 的源码位置（派生在历史门禁与 `npm ci` 之前、`--require-applied "$REQUIRED_IDS"` 且**不存在** `--require-applied "$MIGRATION_IDS"`、相位门禁在 `switch_pending` 之前）、六门禁三边相等。**离线验收**：`node --test "tests/release/*.test.mjs"` **317/317**（新增 19，原 298）。**未关闭**：Linux exact-head CI 复核；两处派生的真机行为在部署主机上未测（本条全部证据取自纯判定、CLI 与源码位置） | ⚠️ | 待 exact-head CI |
| PROD-LEAD-ASSIGNMENT-CAS-DECORATIVE | supabase/migrations/20260817180000_leads_updated_at_is_server_owned.sql, supabase/migrations/20260817190000_lead_reassignment_activity_type.sql, supabase/migrations/20260817200000_lead_reassignment_notification_related_id.sql, supabase/migrations/20260817210000_quote_number_and_lead_unassignment_integrity.sql, src/lib/lead-transfer-batch.mjs, src/app/actions/settings.ts, src/app/api/dashboard/sales-load/rebalance/route.ts, src/app/api/leads/[id]/assignment/route.ts, supabase/replay/22_lead_reassignment_writes.sh, supabase/replay/23_lead_assignment_cas.sh, supabase/replay/25_quote_unassignment_integrity.sh | CREATE+MODIFY | 生产 schema-only baseline **反证了旧叙述**：`public.leads` 已有 enabled `trg_set_updated_at BEFORE UPDATE`，且 `notifications.related_id` 为 `text`，不是 UUID。故 `20260817180000` 现在只在缺少有效 server-clock trigger 的 floor 创建 fallback；真实 production-shaped replay 中它严格 no-op，既有 trigger/function/ACL fingerprint `eb1df7bdd6a68fe763d500470f5237a1` 不变且 `zz_leads_stamp_updated_at` 不存在。production-text cast control 以 1/1/1/1/1 提交，原“production 42804”风险标为 REFUTED；UUID floor control 两次稳定复现 42804 且五类副作用均为 0，修复后 text/UUID 两型均 1/1/1/1/1。应用直写已改走强制非空 token、request-bound idempotency 与原子审计的 `reassign_lead_atomic` / `unassign_lead_atomic`；PG17 并发、陈旧 token、重放、rollback 与 exact audit 门禁通过。当前完整链为 25 pending / 342 release / 54 post-rollback / 18 post-recontract；尚未部署，故保留 REVIEW | ⚠️ | 待部署与 exact-head CI |
| PROD-NOTIFICATION-FACT-IDEMPOTENCY | src/app/api/notify/route.ts, src/lib/notification-events.ts, src/lib/notification-dispatch.ts, src/lib/notifications.ts, src/lib/notify.ts, supabase/migrations/20260817220000_notification_event_idempotency.sql, supabase/replay/26_notification_event_idempotency.sh, tests/security/notification-integrity-boundary.test.mjs, tests/security/notification-idempotency-race.test.mjs | CREATE+MODIFY | confirmed 的已认证通知伪造/广播、持久化失败假成功、read-before-insert 并发重复与四元组误抑制已关闭：公共 route 只接受可证明的事件，服务端重读已提交业务事实并派生正文/收件人，写失败 fail-closed；`payment_received` 拒绝 voided payment，KPI 通知只允许管理角色且 actor 必须等于 `set_by`，公共 `lead_assigned` 在 resolver/DB 前 400（该事件仅由原子移交路径产生）。nullable `event_key` 保留人工可重复提醒，非空 key 由 service-role-only 原子 RPC 以 `(user_id,event_key)` 唯一化。最终定向 Node 25/25、typecheck、相关 ESLint 通过；PostgreSQL 17.10 双会话 concurrency、rollback、reentry、nullable-repeat 与 ACL 门禁 rc=0；未写生产 | ✅ | 2026-08-15 |
| PROD-ROLLBACK-COMPANION-PROMISES-UNCHECKED | supabase/migrations/rollback_money_direct_write_contract_phase.sql, supabase/migrations/rollback_l0_20260811.sql, supabase/replay/24_rollback_companion_guards.sh, scripts/replay-migrations.sh, infra/release/release-manifest.json | CREATE+MODIFY | 五审 R7：「rollback companions 必须守住 in-flight 的 KPI 工作与哈希绑定」。四条 claim 里 **3 条 confirmed、1 条 refuted**。**(1) 资金 companion 的读后写只读到列，没读到函数** — `rollback_money_direct_write_contract_phase.sql` 更新 `money_release_mode` 后 `select direct_write_mode ... where id = 'only'`，读的正是它刚写的那一行，于是**唯一能让它失败的方式是 UPDATE 自己失败**。六道守卫调用的是 `public.money_direct_write_mode()`：函数被改成返回常量（或被 drop 后重建错）时，列检查照过，而上一版应用的直写仍被拒——**一次报告成功、却把应用留在坏掉状态的回滚**。recontract companion 早就经函数读回，rollback 没有，而这正是「无人被告知」的那个方向。**修法**：列检查之后加 `if public.money_direct_write_mode() <> 'compat' then raise`。**(2)「其余一切照旧关闭」只写在注释里** — 头部那七个 bullet（`money_actor()` 仍绑 JWT 主体、class-28 会话边界仍在每张表、DELETE 仍未授出、状态机/lead 归属/分期不变式/放款规则仍在例程里、F-02/06/08/09/10 未动）是**关于本文件留下的那个数据库的断言**，而本文件一条都不查。若会话边界、状态机或 KPI 例程在它运行时缺失，把 mode 退回 `compat` 就不是「回到现状」，而是回到**上一版从未跑过的姿态**。**修法**：取锁后逐项 fail-closed —— `money_direct_write_mode()` / `money_direct_write_is_blocked()` / `money_actor(uuid, text[])` / `assert_current_session()` 存在性；**逐表指名**（不是计数）列出所有缺少 enabled `trg_require_current_session` 的 public 基表，外加「一张都没匹配上」的零表守卫；`contracts` 上的 `trg_guard_contract_transition` 必须是 `tgtype = 19` 且 `tgattr::text = ''`（**无条件** `before update`，不是 `before update of status`——`compat` 下会话直写 `contracts.status`，列清单式触发器正是在这个姿态下变成洞）；`replace_kpi_targets(text, jsonb, uuid)` 与 `clear_kpi_targets(text, uuid)` 必须存在、`prosecdef`、且 EXECUTE **只**给 service_role。缺任何一项 → `42P01` 并**指名**缺的是哪一个。**(3) 手工改姿态没有留痕** — 新增 `MONEY_CONTRACT_PHASE_ROLLED_BACK` audit 行，`details` 带 `previous_mode`（UPDATE **之前**读取）、`mode`、`artifact`、`reversed_by`，行形与 recontract 一致，故一次 rollback/roll-forward 循环的两个方向能在一张表上读完；`actor_id` 留 null，理由与 `revoke_user_sessions()`/`clear_kpi_targets()` 相同（运维路径上没有终端用户身份，而该列外键指向 `auth.users`）。**(4)「companions 没有哈希/版本绑定」→ refuted（已由 C4-5 关闭）**：`infra/release/release-manifest.json` 顶层 `companions` 数组逐个带 `kind` + `sha256`，`scripts/check-release-manifest.mjs` 第 8 条规则按文件名序、**双向**集合相等校验（磁盘有而未声明 / 声明了磁盘没有，各自指名拒绝），`--verify-companions` 与全量门禁共用同一个 `auditCompanions()`。本轮不重复实现，只在此登记为 refuted。**(5) `rollback_l0_20260811.sql` 的 KPI drop 既不取锁也无 `lock_timeout`（confirmed，本轮新测出的第二个缺陷）** — `kpi_targets` 的每一个写者（`replace_kpi_targets`、`clear_kpi_targets`、`confirm_payment`、`void_payment`）都在 `pg_advisory_xact_lock(hashtextextended('public.kpi_targets:' || period, 0))` 上按 period 串行；本文件不写 `kpi_targets`，于是**什么都不取**，把保存路径从一个 mid-transaction 的保存底下抽走。且运维手跑时它已持有上面各段的 DDL 锁，无 `lock_timeout` 意味着可以无限期挂住整个回滚。**修法**：`set_config('lock_timeout', coalesce(nullif(current_setting('lock_timeout'), '0'), '15s'), true)`，再按 `select distinct period from public.kpi_targets order by 1`（排序，故两个并发运维取锁顺序一致、排队而不死锁）逐个取 period 锁，最后 drop 并 `raise notice '... with % period lock(s) held'`。**诚实的边界写进文件**：period 是从 `kpi_targets` 枚举的，正在创建**全新 period 第一批行**的保存持有这段扫描看不到的键——那次调用仍会跑完（已解析的函数活过自己的 drop），锁买到的是**所有已存在的 period**，也就是 KPI 界面能编辑的每一个。round-4 定义判别器（函数体含 `assert_current_session_at_entry` 就拒绝 drop）原样保留。**证据（真 PG 17.10 / `r6replay`，新增 `supabase/replay/24_rollback_companion_guards.sh`，两个方向，已接进 `scripts/replay-migrations.sh` 的 lead-CAS 门禁之后、rollback-companions 段之前，每个 MODE 都跑）**：`EXPECT=companion_guards` **rc=0** —— control 以**旧的两语句形状**在回滚事务里复现缺陷（`old_shape_reports=compat` 而 `guards_see=strict`，即那次「报告成功、守卫仍在拒」的回滚被**测出来**而不是推理出来）；negative 1 把 mode 函数钉成 `select 'strict'::text` → 拒绝并指名 `the mode column says compat but public.money_direct_write_mode() says strict`，mode 仍 strict、**无 audit 行**；negative 2 在 `payments` 上 `disable trigger trg_require_current_session` → 拒绝并指名该表；negative 3 `grant execute on clear_kpi_targets(text, uuid) to authenticated` → 拒绝并指名该例程的 server-only definer 姿态；positive 经函数读回 `compat`、守卫下岗、**恰好一行** audit 且 `previous_mode=strict`；reentry 第二次运行对 mode 幂等、单独记一行 `previous_mode=compat`；收尾用**本仓库发布的** recontract companion 把姿态还回 strict。`EXPECT=kpi_drop` **rc=0** —— DO 块由 `awk` **从 `rollback_l0_20260811.sql` 抽取**（不重打一遍），并 fail-closed 要求抽取物含 period 锁、`lock_timeout` 与 drop（否则门禁会在量错的文本上绿）；A 会话（`application_name='replay_kpi_writer'`）`begin` 后调保存路径、持有 period `2026-08` 的 advisory 键 `-6688192008465168489` 不提交；control「**裸 drop 在 A 的 KPI 写仍在飞的情况下 0s 内移除了保存路径**」；guarded 在 `pg_locks` 里被看见 `not granted` 地**等**这个键；随后**以 lock timeout 退出且函数仍在**（被拒的回滚不留任何东西）；A 提交后同一段块成功 drop 并打出 `period lock(s) held`；reentry 走 `already absent` 干净退出；最后把 round-4 定义**逐字**还回并断言标记回归。**四个坑都是实测出来的、不是设想的**：① `money_direct_write_is_blocked()` 在运维会话里**永远返回 false**（其体在 `not money_write_is_direct()` 时即为 false，而运维路径的 `current_user` 从不是 `authenticated`），所以我先加的那条断言是**不可能失败的假绿**——已从 companion 与门禁中删除，改为「存在性 + 经函数读回 mode」，并把「谓词本身」留给 `supabase/replay/20_assert_post_rollback.sql`（它从一个该谓词有意义的会话里评估）；② PL/pgSQL 的 `text[] || '字面量'` 会解析成 `array_cat(anyarray, anyarray)` 并以 `malformed array literal` 失败——每一次 append 都 `::text`，否则**一次指名的拒绝会在最需要它的时刻变成解析错误**；③ `money_actor` 的真实身份是 `(p_claimed uuid, p_allowed_roles text[])` 而不是 `()`，`to_regprocedure` 必须带全参数类型（同名不同签名不是同一个）；④ 门禁的 pre-round-4 替身**必须 drop 后重建**（`create or replace` 改不了返回类型，实函数返回 `SETOF kpi_targets`），因此替身保持同一签名与返回类型，且门禁必须**捕获并逐字节还原 ACL**（`{postgres=X/postgres,service_role=X/postgres}`）——否则重建出的对象带默认 `PUBLIC EXECUTE`，门禁自己会把 KPI 保存路径开放给每个 authenticated 调用者留在库里；另外负数 advisory 键在 `pg_locks` 里用 `(classid::bigint << 32) + objid` 重组会 **bigint 溢出**，改为按位分解 `(key >> 32) & 4294967295` / `key & 4294967295` 并钉 `objsubid = 1`。**姿态复位已验证**：两个方向跑完后 `direct_write_mode = strict`、`replace_kpi_targets` 仍是 round-4 定义（`prosecdef=t`、体含 `assert_current_session_at_entry`）、其 ACL 与门禁开跑前逐字节相同、`pg_locks` 中 advisory 锁 **0** 个。**刻意不查**：rollback 方向不检查那六道 mode-gated 守卫——本文件的目的就是让它们下岗，要求它们在场等于要求本文件存在的理由不成立；recontract companion 才是必须发现它们 enabled 的那个方向。**本轮补测出的第五个缺陷（门禁自己留下了脚印，confirmed，已修）**：把 24_ 接进 `scripts/replay-migrations.sh` 之后**第一次跑完整 `MODE=branch`**，`supabase/replay/30_assert_post_recontract.sql:83` 以 `ASSERT_FAIL recontract-actually-executed-from-the-rolled-back-state` 中止（`BRANCH_EXIT=1`）。测出的原因不是 recontract 坏了，而是**门禁自己的写入被下游数进了**：24_ 的 positive/reentry 提交两次 rollback、收尾又提交一次 recontract，于是同一个库里 `MONEY_CONTRACT_PHASE_ROLLED_BACK|3|{strict,compat,strict}`、`MONEY_CONTRACT_PHASE_REENTERED|3|{compat,compat,strict}`，而 30_ 要求 REENTERED **恰好 2 行**且**恰好 1 行** `previous_mode='compat'`。`scripts/replay-migrations.sh:897-898` 明写「Each direction restores what it mutated」——audit 行正是它没还的那部分。**修法**：24_ 在探针开跑前记下这两个 action 的现有 id（不是时间戳、不是「删最后三行」），收尾在 recontract 之后按 id 删掉**只属于它自己**的行，并断言删除数 `= 3`、两 action 计数回到基线、`public.audit_logs` 全表行数回到基线（第三条守的是「companion 换了 action 名」这种改动）；失败路径**不删**（门禁失败时 harness 就停，那些行是证据）。**复测（同一容器 PG 17.10，库 drop/create 后重跑）**：`MODE=branch` **rc=0**，24_ 打出 `audit trail restored: 3 row(s) this gate wrote removed by id, public.audit_logs back to 2 row(s)`，全程 23 迁移 / 341 release 断言 / 2 rollback companion / 54 post-rollback / 1 recontract / **18 post-recontract**；`MODE=control` **rc=0**；`MODE=history` **rc=0**（欠账不变，APPLIED=2 / TOTAL=125）。`check:release-manifest` OK，`check:release-companions` 5 个 companion 全部 hash 匹配（本条要求的 `--stamp` 已完成）。**未关闭**：Linux exact-head CI 复核 | ⚠️ | 待部署 |
| PROD-DEPLOY-TASKBOARD-GATE-UNSATISFIABLE | scripts/check-taskboard.mjs, TASKBOARD.md, package.json, infra/systemd/newme-deploy.sh, infra/release/control-plane-bootstrap.md, tests/release/taskboard-completion-gate.test.mjs, tests/release/ci-full-stack-gates-contract.test.mjs | MODIFY | 已确认整块 taskboard 作为 predeploy 前置会要求部署结果先于部署，结构上不可满足。现仅保留 `predeploy_ready` 与 `postdeploy_acceptance` 两个累积 scope：未声明或畸形声明 fail closed 到 predeploy；production history reconciliation 与 unreplayable baseline 属 predeploy；控制面 bootstrap 的生产验收记录属 postdeploy，但 bootstrap coordinator 本身重新运行全部 predeploy 机器门禁，不依赖自指的第三 scope。release SHA 的 exact-head CI 只要求 predeploy；生产证据完成后仅允许 TASKBOARD.md 的 closure SHA，并由 release-final CI 与 `check-release-closure.mjs` 验证 release→closure ancestry 和单文件 diff。生产与 exact-head CI 尚未执行。 | ⚠️ | 待 exact-head CI |
| PROD-RUNTIME-SERVICE-KEY-NOT-RELOCATED | scripts/credential-transition.mjs, infra/systemd/newme-deploy.sh, infra/systemd/newme-production-rollback.sh, infra/release/credential-transition.md, tests/release/credential-transition.test.mjs, tests/release/production-rollback-controller.test.mjs | MODIFY | PR #397 把服务端凭据的存放位置改成了固定运行时存储：`scripts/deploy-immutable.sh` 从候选 `.env.local` 里剥掉 `SUPABASE_SERVICE_ROLE_KEY`，再用 `--require-runtime-service-key --require-no-release-service-key --network` 校验。生产从未搬迁过——键仍只在 live release 的 `.env.local` 里，`/etc/newme/newme-runtime.env` 只有 `CABLE_COSTING_CONFIG`、`NEWME_READINESS_TOKEN`、`NEXT_PUBLIC_SITE_URL` 三项——于是**任何**应用部署都必然死在自己的 config validation 上（2026-08-20 部署 a5cce2c 实测：`runtime SUPABASE_SERVICE_ROLE_KEY is missing or malformed`，资产从 `/var/backups/newme-systemd-assets/20260820T014857Z.52iafD` 干净还原，生产留在 f37c203 且 service active / health 200 / 四个事务全 none）。而安装器只在存储里**已经**有赋值时才加 `--require-runtime-service-key`，所以没有任何安装路径会去播种它 —— 这是 #397 漏掉的迁移，不是配置错误。轮换流程（`credential-transition`）不能用来补：它要求供应商侧新签发的密钥、一次 `credential_remediation=true` 的专用 CI、以及签名的两阶段收口；这里没有新密钥，也就没有可证明的供应商事实。本轮新增一条**搬迁而非轮换**的受控路径 `newme-deploy credential-adopt <sha>`（+ `credential-adopt-recover`）：把 live release 已在使用的值写进固定存储，不新建、不退役、不声称任何供应商事实；轮换输入在场、存储里已有赋值、release 里赋值数不为 1、或任何未收口事务在场时一律拒绝；候选文件 `O_EXCL` 写成 root:root 0600 并先过带 `--network` 的校验器（阳性对照：搬过去的值必须真的能以服务端身份认证），再 `rename` 提交并 fsync 目录；重启后必须同时过 `is-active` 与 readiness，不过则自动还原上一份并重启，两个方向都失败则保留日志与备份并拒绝——绝不报告自己没验证过的状态。值不进 argv / 环境变量 / stdin / 任何记录，日志只存整文件 sha256。`newme-production-rollback status` 增加 `credential_adopt_transaction=` 字段 | ✅ | 12 条新合约测试（tests/release/credential-transition.test.mjs，全套 45 passed）：搬迁后存储里出现该值且原有设置一字不丢、日志里搜不到凭据、两个 sha256 与实际文件逐字相符；轮换输入在场 / 轮换事务在场 / 发布事务在场 / 存储已有键 / release 缺键 / release 重复键 / release 值格式非法 —— 七种都拒绝且不留任何暂存；校验器拒绝时存储零改动、无暂存残留；重启失败自动回滚到无键存储并写 `rolled_back`；两次重启都失败则 `recovery_failed` 且保留备份；在 `after_switched_record` 与 `after_pending` 注入崩溃后，恢复分别给出 `complete` 与 `interrupted_before_switch`；存储被手改成第三种内容则 `runtime_digest_unrecognized` 拒绝；无日志的孤儿备份拒绝且不删除。另有合约测试断言 wrapper 的阻断集合与轮换等价、事后重跑 `--require-runtime-service-key --network`、且**不含** `credential_live_exec` / CI 校验 / taskboard 门禁（搬迁不得冒充补救） |
| PROD-HEALTH-PROBE-ASSERTS-RETIRED-UNIT | infra/observability/health-check.sh, tests/unit/health-check.test.mjs, docs/observability-final.md | MODIFY | 发布后的 composite health 判据要求 `hermes-bridge` / `hermes-dashboard` / `hermes-worker` 三个 unit 都 active，而 `hermes-worker` 已经**不可能**启动：2026-06-16 的 openclaw 迁移把它的程序目录 `/root/.openclaw` 整个归档掉了（快照仍在机器上：`/home/ubuntu/.hermes/_snapshots/openclaw-migration/old-worker/worker/worker-server.js`），unit 却留在 `/etc/systemd/system`；老进程带着已被删除的工作目录活了两个月，直到 2026-08-20 08:13 因 node 版本收口重启才真正尝试启动，此后每 5 秒失败一次 `status=200/CHDIR`（journal 里 1591 次），没有任何进程监听它的 22886，后继 unit `openclaw-gateway` 处于 disabled/inactive。于是**任何**应用部署都会在切换后的 composite health 上被这条永久假阳性打回：2026-08-20 部署 5e18c4d 实测 —— config validation 与 credential 依赖全 PASS（`credential-adopt` 已生效）、3001 的 14 条 smoke 全过、login/dependency 两个子探针 OK，唯独 `[HERMES_DOWN] hermes-worker inactive` 让 health 子探针 exit=1，部署从 `/var/backups/newme-systemd-assets/20260820T022629Z.6uDkTj` 干净还原，生产留在 f37c203。修法不是把它从监控清单里删掉：探针改为**只把显式 `mask` 当作运维声明**（`masked` / `masked-runtime` 跳过存活判据并打印 `retired service skipped:`），`disabled` 与 unit 缺失照旧告警，清单里仍然列全三个名字，所以退役必须在主机上声明、不能靠悄悄改判据。主机侧同步做了退役：unit 文件归档到 `/var/backups/newme-retired-units/hermes-worker.service.20260820T023831Z`（sha256 7b22ade32e6193b8b904656c20402d34e1d07f1c59a31a4784247db976dc9ab1）后 `mask`，`is-enabled=masked` / `is-active=inactive`，另两个 unit 仍 active | ✅ | tests/unit/health-check.test.mjs 7 passed（Linux 上跑，macOS 因 `wc -l` 带前导空格有一条与本改动无关的既有失败）：masked 的 unit 被跳过且输出里没有 `HERMES_DOWN`；**阴性对照**——同一个 unit 不 masked 而 inactive 时照旧 `[HERMES_DOWN] hermes-worker inactive` 且 rc=1，没有这条就分不清“认退役”和“干脆不检查了”；`disabled` 且 inactive 同样告警；另有断言判据里仍然列全三个 unit。主机上跑了两个方向的对照：新判据 rc=0 且打印 `retired service skipped: hermes-worker (masked)`，同一时刻老判据（`/opt/hermes-scripts/observability/health-check.sh`）仍然 rc=1 |
| PROD-SYSTEMD-SANDBOX-BLOCKS-NETLINK | newme-platform.service, infra/systemd/newme-platform.service, infra/systemd/newme-deploy.sh, infra/systemd/newme-production-rollback.sh, tests/release/systemd-process-contract.test.mjs, tests/release/postdeploy-producer-contract.test.mjs, tests/release/production-rollback-controller.test.mjs | MODIFY | 2026-08-20 生产不可用约 21 分钟，三个缺陷都在候选发布自己的控制面文件里。(1) 加固后的 unit 把 `RestrictAddressFamilies` 限到 `AF_UNIX AF_INET AF_INET6`，而 Next 16 的 `next start` 在 listen handler 里调 `os.networkInterfaces()`，libuv 用 AF_NETLINK socket 实现它 —— 沙箱把拒绝变成 EAFNOSUPPORT(97)，进程在 readiness 之前就崩，服务 crash-loop。三个对照证明因果：屏蔽 → `uv_interface_addresses ... 97`；同一 unit 加 AF_NETLINK → `ifaces lo,eth0,tailscale0`；不限制 → 同上。Next 16 的 `-H/--hostname` 没有绑定环境变量，配置层无法绕开，故必须放开这一个族（仍不含 AF_PACKET/AF_RAW）。(2) `recover_preswitch_deploy()` 是 `systemd_asset_transaction=pre_switch` 唯一被认可的出口，却没有向受保护的 asset rollback helper 声明 `NEWME_VERSIONED_ASSET_RECOVERY=1`，于是每次都死在 helper 的 exit 75 上（现场表现为 exit 68 与“protected pointer retained”）；`execute` 的 deploy_recovery 分支同样会死，而且是在 `switch_release_links` 之后死。本轮两处都按 helper 自己的条件（记录里的 `backup=` 必须逐字匹配、进程必须持 fd 9 上的 release 锁）声明恢复模式，普通 rollback 的护栏一字未动；`restore_original_transaction()` 传的是本次事务的 live 快照，按构造永不可能匹配记录，故保留为源码中写明的已知缺口，不为它削弱 helper。(3) `materialize_ci_gate_audit_record()` 结尾 `unset CI_GATE_AUDIT_BASE64`（有意为之，证据已落到 root 拥有的 0600 记录），而 `require_ci_gate_still_fresh()` 只读该变量，导致每次非 bootstrap 部署都以“GitHub Actions evidence expired”失败 —— 把操作者指向 CI，而证据其实合规（run 32315848990：7 个必需 job 全 success，最旧完成时间距今 3497 秒，上限 86400）。现改为变量为空时回读 canonical 记录路径，并保持写入方的同等信任（regular file、非符号链接、root:root、0600、非空且 ≤64 KiB），sha256 绑定仍是真正的认证 | ✅ | 六臂对照（在 Linux 主机以 root 跑抽出的判据函数）：base64 在场 → 0；无 base64 + root:root 0600 记录 → 0（修复前物理不可能）；记录属非 root → 65；记录是符号链接 → 65（O_NOFOLLOW）；记录内容被改一字节 → 65（sha256）；不给记录路径 → 65。AF_NETLINK 三臂对照见上。合约测试：systemd-process / production-rollback-controller / postdeploy-producer 全绿。生产侧临时缓解（unmanaged drop-in `10-netlink-outage-recovery.conf`）在本发布上线后删除并复验 unit 独立成立 |
| PROD-UAT-FIXTURE-LEAD-SOURCE-INVALID | scripts/run-postdeploy-acceptance.mjs, tests/release/postdeploy-producer-contract.test.mjs | MODIFY | 生产上的 postdeploy acceptance 从来没有可能通过：`sudo newme-deploy accept` 以 `uat_cleanup_incomplete` 拒绝，而持久 journal 记的真实原因是 `failure.primary_code = fixture_seed_failed`。把 seed 的六条语句对生产重放后拿到确切错误：`new row for relation "leads" violates check constraint "leads_source_check"` —— `public.leads.source` 是销售侧拥有的封闭取值集（ins / fb / show_room / whatsapp / website / offline / referral / other / unknown_import / unknown，见 `supabase/migrations/20260714000001_normalize_lead_sources.sql`，生产 schema baseline 逐字相同），而 fixture 写的是描述性的 `postdeploy_uat`，这个值从未被该约束接受，所以在**任何**带此约束的库上都不可能成功。现在写入的是导出常量 `FIXTURE_LEAD_SOURCE = "other"`，并由合约测试同时对着「拥有该取值集的迁移」与「生产 baseline」解析出的集合做绑定，两边不能再各自漂移。fixture 行靠 `customer_name` / `notes` 里的 `marker` 识别，`source` 本来不承担识别职责；`contract_approvals.notes` 里的 `jsonb_build_object('source','postdeploy_uat')` 有意保留 —— 那是无约束的 JSON 列。这个缺陷之所以吃掉一整个 deploy/accept 周期，是因为 `seedFixtures` 用裸 `catch` 把数据库错误吞了；现在它先打印 PostgreSQL 自己的标识（code、constraint、table、column、routine、message）再拒绝，并且**永不**打印 `detail` —— 约束冲突的 detail 里嵌着那一行数据 | ✅ | 生产 journal + 语句重放 2026-08-20 |
| PROD-LOGOUT-REPORTS-UNPERFORMED-REVOCATION | src/app/api/auth/logout/route.ts, scripts/run-postdeploy-acceptance.mjs | MODIFY | `POST /api/auth/logout` 一直回 `{ok: true, revoked: true}`，而实际什么都没吊销。生产实测：acceptance 登入一个会话、调用该端点、拿到上面那个回答，再拿**同一个** cookie 打 `/api/auth/me` 得到 HTTP 200，于是发布被 `uat_sales_post_logout_credential_active` 拒绝。原因是服务端客户端携带凭据的方式与 auth-js 的检查口径不一致：`createServerSupabase` 把 access token 作为全局 `Authorization` 头注入并以 `persistSession: false` 构造客户端，从不调用 `setSession()`，所以客户端的会话存储是空的；auth-js 的 `_signOut()` 只在从**存储的**会话里读到 `accessToken` 时才发那次网络吊销，于是它不发请求就返回 `{error: null}`。`revoked: true` 的真实含义因此只是「这次响应清掉了 cookie」，refresh token 在 UI 报告成功登出之后仍能在它整个 30 天寿命内继续换发 access token —— 而该路由的注释恰好声称已经堵上了这个洞。现在改为调用本代码库在管理员重置密码与用户自助改密上已经在用、且已被验证的 `revoke_user_sessions`：删掉该身份的全部 `auth.sessions` 与 `auth.refresh_tokens` 行、回读确认一行不剩、做不到就 raise、并审计结果。只有它自己的 `verified` 后置条件回来为真才报告已吊销，否则回 502 且 `revoked: false`；所有路径（含失败）仍然清 cookie；不带活凭据的调用现在回 `{ok: true, revoked: false, reason: "no_active_session"}`，不再谎称吊销过 | ✅ | 生产 UAT 判据 2026-08-20 |
| PROD-UAT-PRODUCER-PARAMETER-GAP | scripts/run-postdeploy-acceptance.mjs, tests/release/postdeploy-producer-contract.test.mjs | MODIFY | 上一条修好取值之后 `accept` 换了个错误拒绝：`code=42P18 ... could not determine data type of parameter $8`。我的第一个假设（刚改过的 leads insert）**被探针证伪**：把该语句以未加类型、全加类型、修复前源码三个臂对生产 PREPARE，三臂全部干净通过（`begin; … rollback;`，走 `/etc/newme/migration-db.url`，读 `pg_prepared_statements.parameter_types`）。真正的原因在 contracts insert：它绑了 15 个值却只引用 14 个占位符，`actorIds.admin` 作为 `$8` 没有被任何列使用。PostgreSQL 从占位符**出现的位置**推断它的类型，一个传了却从不被引用的参数无类型可推，于是**整条语句**在 Parse 阶段就失败、一行都写不进去 —— 这类缺陷不产生任何局部症状，只表现为整句消失。修法是删掉那个值并重排编号（改用 `$8` 承载 marker）。已把判据从「这一句对不对」提升为「每一句都不许有空洞」：合约测试扫描 producer 里每一个 `db.query(`，配对 SQL 字面量与参数数组，要求引用到的占位符集合恰好是 1..N 且 N 等于绑定个数，并要求非字面量 SQL 参数为 0（否则扫描器会静默漏掉一句）| ✅ | 生产 PREPARE 探针 + 合约测试 2026-08-20 |
| PROD-UAT-LEAD-WON-EXPECTATION-STALE | scripts/run-postdeploy-acceptance.mjs, tests/release/postdeploy-producer-contract.test.mjs | MODIFY | 参数空洞修好、夹具第一次真的种进生产之后，`accept` 换成 `flow_lead_to_contract_readback_failed` 拒绝，而且只给这一个代码 —— 四个条件被压成一个判据，日志里看不出哪一个不成立。从生产库 `pg_proc` 回读 `public.on_lead_won()` 的实际函数体：它把合同以 `status='draft'`、`approval_status='none'` 建出来，再单独插一行 pending 的 `admin_review` 审批。这是 `supabase/migrations/20260812000000_money_actor_identity_and_atomicity.sql` §12 **有意**做的行为变更并在文件里写明：此前把合同直接建成 `active`，等于改一个 lead 字段就绕过了本文件在别处强制的整条审批链；20260817000000 把同一份函数体带了下来。而流程判据要的是 `pending_admin` —— 这条路径上任何代码都从未写过这个值（`pending_admin` 是 `set_contract_status()` 把 draft **提交审批之后**才有的状态），所以在任何带该迁移的库上这条断言都不可能通过。现在期望值变成导出常量并由合约测试对着「拥有该触发器的那个迁移」解析出的字面量绑定；同一个测试还回读迁移前的 production baseline（那里写的是 `active`），所以一个照抄 baseline 的常量会在这里失败 —— 常量跟的是迁移，不是旧 schema。拒绝改为逐条列出未满足的期望（字段、期望值、实测值），值走封闭取值集过滤：像 status/stage 这种枚举照打，其他一律打成 `<not-a-taxonomy-value>` —— 这些行同时带着客户姓名和自由文本，会把业务数据写进部署日志的诊断等于不能用。另外 `admin_review_pending` 这条断言此前回读的是合同行本身，也就是说「行为变更的另一半」从来没被那条声称在验它的断言验过；现在它真的读 `contract_approvals` | ✅ | 生产 pg_proc 回读 + 合约测试 2026-08-20 |
| PROD-UAT-KPI-PERIOD-UNINSERTABLE | scripts/run-postdeploy-acceptance.mjs, tests/release/postdeploy-producer-contract.test.mjs | MODIFY | lead→合同流程修好之后 `accept` 走到最后一个流程 `kpi_period_replace` 才拒绝，代码是 `flow_kpi_replace_http_failed`。对生产库做只读探针后确认这个流程**从来没有成功过一次**：`kpi_targets_period_check` 只接受 `^[0-9]{4}-[0-9]{2}$`，而生产者规划的周期是 `uat-<uuid>`；`kpi_targets_target_type_check` 只接受 `signing` 与 `collection`，而请求发的是 `revenue`。两条 CHECK 里任一条都足以让 `replace_kpi_targets()` 抛错、路由回 500，所以这不是环境漂移，是判据自诞生起就与 schema 不一致 —— 只因为它排在最后，前面每个缺陷都得先修掉才看得见它。第三个缺陷藏在后面：请求写的 notes 是一句字面量，而回读与清理的 delete 都按 `notes = fixture.marker` 认这一行，所以即使插进去也会先判失败、再把这行留在生产库里。现在周期改为保留的远期十年（29xx-MM），既满足 CHECK 又不可能撞上任何真实报表周期（原有的 `count(*)=0` 重试仍然负责并发隔离）；`target_type` 与 notes 改为常量与 marker。合约测试不复述生产者发什么，而是从 `supabase/replay/production-schema-baseline.sql` 把两条 CHECK 解析出来再判：400 次抽样加区间两端都必须被 schema 的正则接受，旧的 `uat-<uuid>` 与真实周期（2026-08 等）都必须被拒 —— 没有这两个阴性对照，这个测试在坏掉的生产者上也会通过（已实测：把常量改回去，两个测试挂）。同时把这一类瞎子拒绝一并堵掉：任何 non-2xx 现在都打印方法、路径、状态码与路由自己的 error 常量，error 文本走白名单（只允许纯字母与空格的路由字面量，其余打成 `<redacted-error-body>`），以免把行值、id 或邮箱写进部署日志 | ✅ | 生产只读探针 + 合约测试 2026-08-20 |

> **复审逐条结论（confirmed / refuted / insufficient）**：F-09 资金授权 leg-2 会造成停摆 → **confirmed**（已删除该 leg）；F-02 删除不可逆且毁证据 → **confirmed**（改停用 + 互锁 + 可回滚）；F-06 profile-email/改密接管链 → **confirmed**（列级 grant + 改密只用 `user.email`）；F-08 审计/会话可伪造插入 → **confirmed**（三表 `with check (false)` + 移除 proxy 的 PAGE_VISIT 调用者写入）；F-10 Meta token 明文可读 → **confirmed**（drop policy + revoke `anon`/`authenticated`，`service_role` 保留）；限流器可被冲刷 → **confirmed**；开放重定向 + 脚本可读 token → **confirmed**；旧 token 吊销缺口 → **confirmed**；KPI delete-then-insert 数据丢失 → **confirmed**；COS 脚本 release 漂移 → **confirmed**；Hermes CI 不可达 → **confirmed**；迁移未被 CI 演练 → **confirmed**（且更严重：整个历史不可重放）；canonical deploy 接受不完整/错误 event 的声明 → **partially refuted**：`infra/systemd/newme-deploy.sh` 确实查了 GitHub API（复审说“完全不查”不成立），但它漏了 `event`/`head_branch`，且 `scripts/deploy.sh` → `deploy-immutable.sh` 这条路径当时确实零校验 —— 两处均已修。
>
> **仍缺生产后置证据**：当前只读 catalog/history capture 不证明候选迁移已经应用，也不证明应用运行时、四角色 UAT、告警演练、Sentry/Hermes、性能与延迟验收已通过；这些只能在 exact-head CI、canonical deploy 与 postdeploy acceptance 后关闭。

> **三审（2026-08-11）逐条处置**：迁移历史重写 → **已撤销**（逐字节还原 + 新增不可变门禁）；replay job false-green → **已修**（三模式全门禁，history 以期望文件钉死）；资金例程信任调用方 id → **已修**（`money_actor` 取 JWT subject + 五个 INVOKER 触发器）；报价转换/合同创建多事务 → **已修**（单事务 definer 例程）；审批链读最早 pending 行 → **已修**（步骤由合同状态推导）；UI 调用缺失的 PATCH 路由 → **已修**（新增 PATCH + 转移表双向相等断言）；付款分配未绑定合同 → **已修**（跨合同 42501）；KPI 缺周期串行化 → **已修**；直连 JWT 吊销边界 → **部分**（数据库侧已做，refresh token 只能由 GoTrue 吊销）；canonical deploy 只看 run 不看 job → **已修**（逐 job + taskboard + 远端迁移历史）；回滚只证明 SQL 可执行 → **已修**（30 条 post-rollback 安全断言）；Hermes 投递 `continue-on-error` → **已修**；F-02 声称凭据已死 → **已撤回该说法**，F-02 保持未关闭。
>
> **仍需生产授权且本轮未执行的动作**：通过 canonical `newme-deploy` 分期应用 exact manifest 的 25 个 pending migrations（`20260813000000` 另需对 `auth.users` 的 SELECT；按 `supabase/preflight/expand-contract-rollback.md` 的 expand/contract 顺序）；封禁 `dev@newme.ae` 的 Auth 身份并吊销会话；落地 `/etc/newme/migration-db.url`；执行一次性控制面 bootstrap（`infra/release/control-plane-bootstrap.md`）。通用凭据轮换、仓库私有化、Git 历史清理与 origin 防火墙不属于本目标的完成条件。生产 migration history/schema 的只读 capture 与逐条 reconciliation 已完成，未伴随生产写入。

> **四审（e40 round 3）当前结论**：P1-11 生产 migration history/schema 的认证只读 capture、显式 reconciliation、baseline hash gate 与 fresh PostgreSQL 17 full replay 已完成，未写生产；P1-10 控制面 bootstrap/rollback 代码侧与隔离容器演练已完成，但 production bootstrap 尚未执行。两者不能用本地测试替代 exact-head CI、canonical deploy 与 postdeploy acceptance。
>
> **仍需登录态 UAT**：`on_lead_won()` 由 active 改 draft 后的 lead→合同链；合同详情页状态按钮（此前一直 405）；报价转换 + 两步审批；付款分配；KPI 周期替换（遗留重复键的前置检查会按设计中止部署）。

---

## M1 当前版本发布项 — 2026-07-19（SAM-6）
> 事实源：Linear milestone `M1 当前版本生产交付` 与 GitHub `main@43ec83432588909db1a064da4de2b4b029ff8f76`。STATUS 遵循本文件 Frozen v2：`REVIEW` 对应 Linear `In Review`，`TODO` 对应 Linear `Todo`。候选基线不等于最终生产放行；没有完整证据不得写成 `DONE`。

| TASK_ID | STATUS | M1 发布门禁 / 当前事实 |
|---|---|---|
| SAM-6 | REVIEW | 候选基线 `main@43ec83432588909db1a064da4de2b4b029ff8f76`；CI run `29664871138` 为同 SHA `success`；文档一致性由 SAM-41 收口；合并后的 main 仍待总控复核。 |
| SAM-7 | DONE | 部署前只读检查通过（M1 链）。 |
| SAM-8 | DONE | 冻结版本部署 + 健康验证（evidence 20260719-193837.json）。 |
| SAM-9 | DONE | 生产 P0 Smoke 14/14 + regression 22/22。 |
| SAM-11 | DONE | 机器项全验 + 工作簿导入写测试全 PASS（M1 RELEASED 2026-07-20）；差异 SAM-45 进 M2。 |
| SAM-12 | DONE | **RELEASED**：全链证据汇总 + 唯一结论出具（2026-07-20，森哥签收生效）。 |
| SAM-24 | DONE | DB 兼容/备份/回滚点验证通过（M1 链）。 |
| SAM-25 | DONE | 正向链路 + 负向 6/6 全 PASS，fixture 清理基线零漂移；差异 SAM-46 进 M2、SAM-48 热修已上线。 |
| SAM-26 | IN_PROGRESS | 角色权限矩阵机器验✓；移动端/视觉挂起人工（不拦发布，留 M2 观察）。 |
| SAM-27 | DONE | 集成/cron/可观测性验收✓；SAM-47 随热修 #1 关闭。 |
| SAM-28 | DONE | 业务签收（森哥 2026-07-20）+ 技术签收（机器全量验收）→ Done。 |
| SAM-43 | REVIEW | Linear 仍为 `In Review`；登录态视觉/交互 UAT 未完成，不得以 API/CI 证据替代。 |
| SAM-47 | DONE | 热修 #1：health 响应 memory 字段删除，生产实测无 memory（BUILD_ID `opQHvVVbt_cF_G80h-7is`）。 |
| SAM-48 | DONE | 热修 #1：报价引擎校验（未知设备 400 + 零总价双端 400），生产 API 验收 8/8（BUILD_ID `opQHvVVbt_cF_G80h-7is`）。 |

> SAM-41 只补文档事实，不改变发布、部署或 UAT 工作流；合并后仍需总控复核 `main`，再决定 SAM-6 是否推进。

---

## MoA Tier 1 — Technical Debt (IMMEDIATE, 1-2 weeks)
Source: MoA 4-round audit, 3-model unanimous sign-off, lines 478-500 + 559-600 + 643-675

### 1A. New Files (infrastructure)

| # | File | Operation | Verification | Status | Done Date |
|---|------|-----------|-------------|--------|-----------|
| T1-1 | src/lib/supabaseQuery.ts | CREATE | file exists + contains `useSupabaseQuery` + `AbortController` + retry logic (>=2 retries) + timeout (8s default) | ✅ | 2026-06-30 |
| T1-2 | src/components/DashboardErrorBoundary.tsx | CREATE | file exists + contains `errorId` + `Sentry` (or `sentry`) + fallback UI | ✅ | 2026-06-30 |
| T1-3 | src/shared/hooks/usePipelineDragDrop.ts | CREATE | file exists + contains `onDragStart` + `onDrop` + `draggingLeadId` | ✅ | 2026-06-30 |
| T1-4 | src/shared/hooks/useStageGuard.ts | CREATE | file exists + contains `stageGuard` or `validTransition` + STAGES definition | ✅ | 2026-06-30 |

### 1B. Modified Files (integration)

| # | File | Operation | Verification | Status | Done Date |
|---|------|-----------|-------------|--------|-----------|
| T1-5 | src/app/(dashboard)/layout.tsx | MODIFY | contains `ErrorBoundary` wrapping children | ✅ | 2026-06-30 |
| T1-6 | src/app/(dashboard)/leads/page.tsx | MODIFY | contains `usePipelineDragDrop` + `useStageGuard` + `useSupabaseQuery` + empty stages visible by default | ✅ | 2026-07-01 |
| T1-7 | src/app/(dashboard)/pipeline/page.tsx | MODIFY | contains `usePipelineDragDrop` (replaces inline drag) + `useSupabaseQuery` (replaces direct supabase calls) + `useStageGuard` | ✅ | 2026-07-01 |
| T1-8 | src/app/(dashboard)/leads/[id]/page.tsx | MODIFY | `maybeSingle` count >= 3 + contains `skeleton` or `Skeleton` or `loading` fallback + `useSupabaseQuery` | ✅ | 2026-07-01 |
| T1-9 | src/app/(dashboard)/products/page.tsx | MODIFY | uses `fetch('/api/products')` via API route (replaced client-side `useSupabaseQuery` + `createClient`, see P1-B Supabase removal) | ✅ | 2026-07-04 |
| T1-10 | src/app/globals.css | MODIFY | contains `error-boundary-fallback` class | ✅ | 2026-07-01 |

### 1C. Sentry Integration

| # | Requirement | Verification | Status | Done Date |
|---|-------------|-------------|--------|-----------|
| T1-11 | Sentry captureException in ErrorBoundary | DashboardErrorBoundary.tsx contains `Sentry.captureException` or `captureException` | ✅ | 2026-07-01 |
| T1-12 | Sentry error events actually received | Manual: trigger error → Sentry dashboard shows event | ✅ | 2026-07-01 |

**Tier 1 Progress: 12/12 (100%) ✅**

---

## P0 紧急性能修复（2026-07-01 立）

| # | Problem | Requirement | Verification | Status | Done Date |
|---|---------|-------------|-------------|--------|-----------|
| P0-1 | leads/[id] 加载 2.1 分钟 / 431 请求 | fetchData 的 8 个串行查询改并行 + 关键路径用 PostgREST JOIN | 详情页加载 < 5s，请求数 < 50 | ✅ | 2026-07-01 (编码 + migration + 161ms 验证) |

---

### MoA Tier 2 — UI Consistency (SHORT-TERM, 2-4 weeks)
Blocked by Tier 1 completion. Do NOT start until all Tier 1 = ✅.

|| # | Problem | Requirement | Verification | Status | Done Date |
||---|---------|-------------|-------------|--------|-----------|
|| T2-1 | P-2: Scroll behavior inconsistency | Unified scroll strategy across all dashboard pages | All pages use consistent overflow/scroll container | ✅ | 2026-07-01 |
|| T2-2 | P-3: Kanban stats scattered | Merge progress bar + numbers + percentage into single visual unit | Single stats component, not 3 separate elements | ✅ | 2026-07-01 |
|| T2-3 | P-5: Empty stage visibility | Default show empty stages + collapse toggle button | `showEmptyStages` default = true, toggle button exists | ✅ | 2026-07-01 |
|| T2-4 | 锚定功能卡片 (sticky headers/filter/action bar) — 2026-07-01 新立 | 长页面滚动时 filter/标题/搜索/操作栏跟随屏幕 | leads/page.tsx (DashboardScrollContainer 内 3 件套) + leads/[id] + payments + quotations + tasks (4 页 viewport 滚动模式) 全部锚定 | ✅ | 2026-07-01 (commits 1ac84ca + a606d9b + 0fe9543 + aa54565 + 7c7d74c) |

**Tier 2 Progress: 4/4 (100%) ✅**

---

### MoA Tier 3 — Architecture (LONG-TERM, 1-2 months)
Tier 2 unlocked (T2-1/2/3 ✅ 2026-07-01).

| # | Problem | Requirement | Verification | Status | Done Date |
|---|---------|-------------|-------------|--------|-----------|
| T3-1 | DashboardLayout unification | Full DashboardLayout refactor (方案A) | Single layout component, all pages conform | ✅ 2026-07-03 (24/24 pages) | |
| T3-2 | Performance monitoring + alerts | Lighthouse/Web Vitals baseline + alerting | Baseline recorded, alerts configured | ✅ | 2026-07-03 (web-vitals.ts + WebVitalsReporter.tsx + lighthouse-baseline.md) |
| T3-3 | Code debt elimination | Refactor large files (leads 1108行, leads/[id] 946行, pipeline 566→146行) | No single file > 500 lines, shared components extracted | ✅ | 2026-07-03 (pipeline: 5afce2f + ea791b1; leads: 15/15 steps done, page.tsx 351行) |
| T3-4 | Docs drift: coding_standards §4 contracts/payments stale | Refresh table schema section to match actual DB (contract_amount / sales_id / confirmed) | coding_standards.md §4 列与 DB service_role 查询结果一致 | ✅ | 2026-07-01 |

**Tier 3 Progress: 4/4 (100%) ✅**

---

### Pending Tasks (未启动)

| # | Task | Requirement | Verification | Status |
|---|------|-------------|-------------|--------|
| kanban-unify | 统一 kanban stage 定义 + fmtAED 到 shared/ | pipeline/leads 共享 KanbanShell + 共享 stage 常量 | shared/kanban/types.ts 存在 + leads/pipeline import from shared | ✅ 2026-07-06 |
| perf-1 | 全站性能优化 | 108 请求 → <50, 3.3MB → <1.5MB, 33.73s → <5s | Lighthouse + bundle analyzer report | 🔒 第一批完成，剩余冻结（见 SPEC.md §十一） |\n|  | ├─ xlsx lazy-load | `/leads` 首屏 -234KB | `import("xlsx")` 动态加载 | ✅ `c54d83b` |\n|  | ├─ Meta Pixel 条件加载 | 15 后台路径不加载 fbevents.js | 路由匹配验证 | ✅ `6dca992` `e7363fa` |\n|  | ├─ Bundle Analyzer 基线 | 全站客户端 JS map | ANALYZE=true 报告 | ✅ `e50a9c4` |\n|  | ├─ deploy.sh v4.0 隔离构建 | 生产 .next 零触碰 | swap 停机 <5s | ✅ `77563c8` 等 6 commits |\n|  | ├─ P0 防复发 guard | 阻止直接 build 覆盖生产 | guard-prod-build.sh | ✅ `d25faf3` |\n|  | └─ PostHog/Recharts/base-ui | 下一轮优化（解冻后） | — | 🔒 冻结 |
| hermes-ci | Hermes CI webhook 订阅 crm-ci | CI pipeline 跑通 + webhook 触发 | 等待 `ci` 成功、`crm-ci` workflow_run 成功及 Hermes webhook delivery 200 可审计证据 | ⚠️ REVIEW |
| moa-tier2-detail | MoA Tier 2 决策点 3+4 方案细化 | 10-12 人天方案文档 | `crm-v3/v3.1/moa-tier2-detail-20260701.md` 已细化决策点 3+4，2026-07-10 复核签收 | ✅ 2026-07-10 |

---

### MoA Tier 4 — Process Governance (新建 2026-07-01)
Tier 3 + P0 完成度不是前提——运维治理独立于产品进度。事由：2026-07-01 Sentry 131348591 流程违规补审。

| # | Problem | Requirement | Verification | Status | Done Date |
|---|---------|-------------|-------------|--------|-----------|
| T4-1 | hermes-rules.md §十 缺运维操作边界 | 立 §十 运维操作三档分级（🟢/🟡/🔴）+ OEEC 紧急例外 + 速查表 10 类 | 章节落地、3 档表完整、OEEC 条款存在、速查表覆盖 Sentry/服务/数据库/Secrets | ✅ | 2026-07-01 |
| T4-2 | Sentry issue 131348591 archived_forever 后未登记 ops-log + ChunkLoadError 紧急重建 | 在 HANDOFF-20260701.md 加 ## Ops Log 条目 + commit 留痕 | HANDOFF 含完整 6 字段（时间/命令/操作者/资源ID/缘由/审计报告路径） | ✅ | 2026-07-01 |
| T4-3 | deploy.sh Step 3 build guard 与服务启停冲突 | 重构 deploy.sh 让 build 步骤先自动停服务再 build 再起，或分离 build/deploy 步骤 | deploy.sh 完整跑通（5/5 步），build 不再被 guard 拦 | ✅ | 2026-07-01 (commit 5d7b60b, deploy.sh + package.json, nginx CSP 也改) |
| T4-4 | PostHog `eu-assets.i.posthog.com` 域名未白名单 CSP | CSP script-src/connect-src 加 eu-assets.i.posthog.com | 浏览器 console 不再报 CSP violation | ✅ | 2026-07-01 (nginx 改完 + reload, 生产 200 + CSP 头返回) |

**Tier 4 Progress: 4/4 (100%) ✅**

---

## Phase 1 — Business Features (25/25 ✅ COMPLETE)

All 25 items from Phase 1 business delivery are DONE. No action needed.
Includes: P0-1~P0-7, Sentry fix, RLS matrix (35 tables, 250 policies, 0 FOR ALL),
log_activity prefix, AI gateway v2.0-2.1, profiles.email migration, 3.3-3.7 dev,
integration tests, Codex review fixes, decision points 3-4, public→authenticated RLS fix.

---

## Freeze Rules (from MoA sign-off)

1. **禁止** 在任何页面直接调用 `supabase.from().select()` — 必须通过 `useSupabaseQuery`
2. **禁止** 在 leads/pipeline 实现新拖拽逻辑 — 必须复用 `usePipelineDragDrop`
3. **禁止** 移除或绕过全局 ErrorBoundary
4. **禁止** 在 Tier 1 完成前启动 Tier 2/Tier 3 工作
5. **禁止** 在 leads 详情页假设外键数据必然存在
6. **禁止** 引用未经 `supabase.from("table").select().limit(1)` 验证过的数据库列名。CC 子代理生成的任何 supabase 查询，必须在 commit 前用 service_role key 验证实际表结构

---

## How to Add New Tasks

1. Run an audit / plan
2. Add each file/action as a row in the table above
3. Define the **verification condition** (grep pattern, file existence, test pass)
4. Status: ❌ pending → ⚠️ partial → ✅ done
5. Fill in Done Date when ✅

## How to Remove Completed Tasks

After deployment + production verification, move completed rows to archive section below.

---

## Archive

### P1-B: Client Supabase Removal (analytics/ads/products) — 2026-07-04
| # | File | Change | Result |
|---|------|--------|--------|
| P1-B | src/app/(dashboard)/analytics, /ads, /products | 移除 client Supabase → server actions + API routes | analytics 995→771KB (-224KB), ads 961→738KB (-223KB), products 1066→842KB (-224KB) |

### P1-C: Dashboard Summary API Aggregation — 2026-07-04
| # | File | Change | Result |
|---|------|--------|--------|
| P1-C | src/app/api/dashboard/summary/route.ts | NEW — 聚合 14 条 server Supabase 查询，30s cache | dashboard 18 client Supabase REST calls → 1 fetch (573ms) |
| P1-C | src/app/(dashboard)/dashboard/page.tsx | −355 lines client Supabase reads, +30 lines fetch | 0 Supabase REST data calls on /dashboard Network panel |

验收: `/api/dashboard/summary` 573ms。Network 面板 0 `supabase.co/rest/v1/` 调用（仅 1 auth token）。BUILD_ID `arpeAWPUml4dotHYJ10KK`。

### P1-D: Leads List API Aggregation — 2026-07-04
|| # | File | Change | Result |
||---|------|--------|--------|
|| P1-D | src/app/api/leads/list/route.ts | NEW — 聚合 auth+profile+leads+salesUsers 4 queries | leads 4 client Supabase reads → 1 fetch |
|| P1-D | src/app/(dashboard)/leads/_hooks/useLeadsData.ts | Supabase reads → fetch('/api/leads/list') | 0 Supabase REST data calls on /leads Network panel |
|| P1-D | src/app/(dashboard)/leads/page.tsx | −createClient import, −supabase const | bulkTransfer → useLeadMutations integration |

验收: BUILD_ID `34myA0cSpjO3BQHGA3DTc`。smoke 14/14。

### P1-E: Analytics Summary API Aggregation — 2026-07-04
|| # | File | Change | Result |
||---|------|--------|--------|
|| P1-E | src/app/api/analytics/summary/route.ts | NEW — 聚合 7 条 server Supabase 查询，Promise.all 并行，30s cache | analytics 6 条分散 fetch → 1 条 /api/analytics/summary |
|| P1-E | src/app/(dashboard)/analytics/page.tsx | +AnalyticsContext, 单次 fetch 替代 6 条分散请求 | 0 client Supabase reads，AnalyticsContext 可供子组件未来迁移 |

验收: BUILD_ID `SKwOrxKMZl2AoWmEzyXS0`。smoke 14/14。5/5 页面 client Supabase reads 清零。

### P1-F: Workbench Query Parallelization — 2026-07-04
|| # | File | Change | Result |
||---|------|--------|--------|
|| P1-F | src/app/api/workbench/route.ts | 9 次串行 Supabase → 4 步（auth→profile→6并行→leadNames）+ 30s cache | -5 round-trips，缓存命中=0查询 |
|| P1-F | scripts/deploy.sh | GATE_RESULT_DIR /var/lib→~/.hermes | ubuntu 可写，不再 Permission denied |

验收: BUILD_ID `w_RxXx-k8y8aJ2dcuze9`。smoke PASS。Sentry ChunkLoadError 告警已上线(#696330)。

### 下一步摘要
- P2 reads all: ✅ 已上线 (6 页 reads → BFF API routes)
- P2 mutations low: ✅ 已上线 (team/payments/tasks server actions)
- P2 mutations core: ✅ 已上线 (pipeline/contracts server actions)
- P2 mutations settings: ✅ 已上线 (settings lead-assignment server actions)
- Post-audit patch: ✅ 已上线 (de3b52f: tasks + pipeline ownership + useSalesKpiData BFF)
- TRUE_CODEX_REAUDIT 全链: ✅ 已上线 (49cd03f: 一审→修复→二审→部署, BUILD_ID o1toe2b6XmKR_8Jdfx8oP)
- P2.5 Infra Hardening: ✅ 已上线 (11e3805 + 583ba89: 4 audit scripts + 2 release docs)
- P1/P2 Archive: FULL PASS 恢复 ✅

---

## ✅ Done — User-Reported Bugs (Lead Detail Page, 2026-07-06)

| ID | Symptom | Root Cause | Fix | Status |
|---|---|---|---|---|
| BUG-LD-1 | 修改区域 · 酋长国 (emirate) 字段不保存 | `renderInlineEdit` 无 onBlur → 点空白不退出 → 误以为"没保存"（实际 BUG-LD-3 体现） | page.tsx 加 `onBlur={() => setEditField(null)}` + Enter 也清空 + span 包住防止 click 冒泡 | ✅ 2026-07-06 02:57 (BUILD_ID kwrsLUQgv_irKkyEXa7d_) |
| BUG-LD-2 | 点击区域 · 左侧内容被遮盖 | sticky header `bg-background/95 backdrop-blur-sm` 不透明遮盖主内容 | page.tsx 改 `bg-background/70` 去掉 backdrop-blur | ✅ 2026-07-06 02:57 (BUILD_ID kwrsLUQgv_irKkyEXa7d_) |
| BUG-LD-3 | 点击区域进入输入状态，再次点击空白处不还原 | `renderInlineEdit` input 无 onBlur handler | page.tsx 加 `onBlur={() => setEditField(null)}` | ✅ 2026-07-06 02:57 (BUILD_ID kwrsLUQgv_irKkyEXa7d_) |
| BUG-LD-4 | admin 视角 · 转移销售下拉框被遮盖 | `LeadFoldingPanel.tsx:76` Card `overflow-hidden` 裁切 dropdown | 改 `overflow-visible` | ✅ 2026-07-06 02:57 (BUILD_ID kwrsLUQgv_irKkyEXa7d_) |

**部署链路**: commit 8057b80 → BUILD kwrsLUQgv_irKkyEXa7d_ → 14/14 smoke PASS → 0 journal errors → API /health version=kwrsLUQgv_ir.

Note: All four are independent of P3 PRD G1/G2/G3 (quality API, weekly-review, workbench). Batch into one fix after P3 PRD ships.

## 🔴 Open — Production (2026-07-05 23:55 CST)

| ID | Symptom | Root Cause | Fix | Status |
|---|---|---|---|---|
| ROOT_WHITEPAGE_FIX | `https://app.newme.ae/` 打开后白屏 1-3s 后跳转 | Next.js 16 App Router `page.tsx` 的 `redirect()` 被编译为 client-side navigation，触发 `BAILOUT_TO_CLIENT_SIDE_RENDERING`，body 内只有空模板 | 在 `src/proxy.ts` 顶部加 `if (pathname === "/") return NextResponse.redirect(/dashboard, 307)`，edge 层强制 HTTP 307，无需客户端 JS | ✅ | 2026-07-05 16:47 (BUILD_ID NCzYkIdYimkjk9_-xmx75) |

**Fix 链条 (3 commits)：**
- `62cf163` proxy.ts 加 pathname==="/" 307 → /dashboard
- `c6afe98` page.tsx force-dynamic + force-no-store 防止 prerender 缓存
- `d085078` proxy.ts config.matcher 加 "/"（**根因**：matcher 缺 / 导致 proxy 完全没注册到 middleware-manifest.json）
HOTFIX-2 | DEPLOYED | hermes | 2026-07-20 | SAM-45+SAM-46 closed; ca4cfdc(删quotes fallback+导入原始数据面板)+fd2b35e(insert schema对齐); BUILD_ID odB7DlnFp-Er6a5NbVmv0; CI 29705110814 green; K3审计 PASS_WITH_WARNINGS(Ship); 生产API验收通过+基线零漂移; SAM-49登记(hermes路由静默零总价姊妹项+K3 P2)
