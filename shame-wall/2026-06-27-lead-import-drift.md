# [deepseek-v4-pro] 2026-06-27 — 删库导入漂移

**LLM:** deepseek-v4-pro（session 20260627_153217，telegram）

## 失败

反复尝试删除 leads 并导入 Excel 数据集，途中遇 RLS/schema 错误。删除最终在报错的同时实际成功；随后用 service_role 脚本裸写导入，77 条记录缺少预期的分配/状态/import 元数据。

## 根因

缺少 UI 通道、多次上下文丢失、无完整契约检查的数据库回退写入。

## 整改控制

独立验证变更结果；使用预期导入契约；写入后对数量、归属、状态、元数据做对账。
