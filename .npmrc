# pnpm onlyBuiltDependencies — commented out (npm doesn't support this key)
# Uncomment if switching back to pnpm:
# onlyBuiltDependencies[]= @sentry/cli
# onlyBuiltDependencies[]= core-js
# onlyBuiltDependencies[]= sharp
# onlyBuiltDependencies[]= unrs-resolver
