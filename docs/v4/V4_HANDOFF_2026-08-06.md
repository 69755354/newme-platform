# NewMe V4 handoff — 2026-08-06

## Purpose and decision boundary

This handoff records what is verified for the V4 staging commercial slice and
what remains before any commercial or production claim. It is intentionally a
handoff record, not a production release approval. No raw customer data,
credentials, backup contents or runtime logs belong in this repository.

## Immutable repository and staging facts

| Item | Verified value |
| --- | --- |
| Current staging-isolation canonical | `1bdff64b2b5c057b02ba42b1ee3107912278a3ae` |
| Deployed staging release | `83c4b6f3a14bb248db263ba8d727e00f6c0b70fe` |
| Direct staging application rollback release | `683e841e7b5c684b5cebdb05dbc17b7ae35c6929` |
| Staging project | `bfsiibofuzoglziltgyd` (`newme-sam74-cleanroom-2`) |
| Deployment delta from `83c4...` to `1bd...` | documentation only; no runtime-code or migration delta |

The deployed release passed SHA-bound `sam78-staging-tenant-closure`,
`product-saas-final`, and `v4-staging-acceptance` controller UAT reports. Each
recorded `ok=true`, health `200`, and exact-ID cleanup with zero tracked
residue. The full evidence and its strict boundary are in
[`V4_STAGING_COMMERCIAL_ACCEPTANCE.md`](V4_STAGING_COMMERCIAL_ACCEPTANCE.md).

Production remains **NO-GO**. The complete blocker list is in
[`V4_PRODUCTION_GO_NO_GO.md`](V4_PRODUCTION_GO_NO_GO.md); staging evidence must
not be relabelled as production evidence.

## Linear delivery state at handoff

| Work item | State | Handoff meaning |
| --- | --- | --- |
| SAM-77 through SAM-84 | Done | scoped repository and staging acceptance recorded; not a production approval |
| SAM-85 | In Progress | isolated rehearsal environment provisioned; source/masking/reconciliation/destruction evidence not yet executed |
| SAM-86 | In Progress | release provenance and basic staging health exist; owned alerts, restore RPO/RTO, load/noisy-neighbor and credential/incident rehearsal remain |
| SAM-87 | Done | staged canary/rollback process artefacts are delivered; this does not supersede SAM-85/86 evidence requirements |
| SAM-88 | In Progress | no authorized real-estate and retail design-partner operations have been run |

## SAM-85 continuation boundary

An isolated, non-persistent Supabase development branch was attempted from the
staging parent project for this rehearsal:

| Item | Value |
| --- | --- |
| Branch name | `sam85-isolated-rehearsal-20260806` |
| Attempted isolated project ref | `eaumtykqdfuqdymcqylz` |
| Parent project ref | `bfsiibofuzoglziltgyd` |
| Data copied at provision | `false` |
| Terminal provider state | `MIGRATIONS_FAILED` |

The failed branch was deleted immediately after its terminal state, so it has
no continuing cost and must not be reused. It is not evidence of a migration
rehearsal. A replacement branch must not receive
production data, production credentials, or a route that can write to
production. Before application access, the next operator must establish all of
the following outside Git and retain only aggregate evidence:

1. a time-bounded approval and encrypted, authoritative **non-production**
   source snapshot reference with SHA-256;
2. clone-only credential references and proof that production credentials and
   write routes are denied;
3. masking/tokenization completed before application access and all five
   outbound channels (email, messaging, webhook, portal and payment) denied at
   configuration, network and runtime layers;
4. ordered forward and reverse migration assets, bounded backfill and
   aggregate quarantine/reconciliation results; and
5. verified destruction of the clone database, storage, credentials, logs,
   exports and access before the approved deadline.

Use the fail-closed validator already tracked in
[`scripts/v4-rehearsal-kit/`](../../scripts/v4-rehearsal-kit/) and the detailed
execution boundary in [`V4_09_MIGRATION_REHEARSAL_RUNBOOK.md`](V4_09_MIGRATION_REHEARSAL_RUNBOOK.md).
If any preflight condition is absent, do not import data; delete the isolated
branch to end cost exposure and record the non-execution outcome in Linear.

## First actions for the next worker

1. Read this file, `V4_STAGING_COMMERCIAL_ACCEPTANCE.md`, and
   `V4_PRODUCTION_GO_NO_GO.md` before changing a status, database or release.
2. Re-read the current Linear SAM-85, SAM-86 and SAM-88 acceptance criteria;
   keep them In Progress until their own evidence is present.
3. For SAM-85, validate an aggregate-only preflight document before any clone
   import. Never paste a source URL with credentials, data rows, tokens or
   personal data into Git, Linear or chat.
4. Maintain controller capacity discipline: preserve the active and direct
   rollback releases, preflight available disk before a build/deploy/UAT, and
   avoid broad Docker/release cleanup.
5. Treat a new production request as a distinct approval package. It needs a
   precise SHA, backup/PITR point, migration compatibility decision, owners,
   release criteria and rollback decision; it is outside this handoff.

## Evidence locations

* Git and CI provenance: `V4_EXTERNAL_AUDIT_INDEX.md`.
* Requirements-to-delivery mapping: `V4_REQUIREMENTS_TRACEABILITY.md`.
* Staging commercial evidence: `V4_STAGING_COMMERCIAL_ACCEPTANCE.md`.
* Production decision and blockers: `V4_PRODUCTION_GO_NO_GO.md`.
* SAM-85/SAM-86 aggregate-only validator: `scripts/v4-rehearsal-kit/`.
