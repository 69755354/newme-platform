# CRM Security Hardening — 2026-06-22

## Version: feat/crm-v2 @ c438db5

## Summary
Codex audit (209K tokens) discovered 13 P0/P1 security issues. 8 resolved, 5 assessed as non-issues.

## Resolved (8)

### P0 — Authorization Enforcement
| # | File | Issue | Fix |
|---|------|-------|-----|
| P0#1 | `api/activities/route.ts` | Any user could see all activities | Role check (admin/boss see all, sales see own leads only) + fixed table name `user_profiles`→`profiles` |
| P0#2 | `api/quotations/generate/route.ts` | Sales could quote on others' leads | Added `assigned_to` ownership check → 403 for non-owner |
| P0#3 | `api/cos/download-url/route.ts` | Any auth user could download any file | Requires `lead_id`, validates ownership, server-side resolves lead from COS key |
| P0#4 | `supabase/migrations/` | Notifications RLS with `WITH CHECK (true)` | Tightened to `WITH CHECK (user_id = auth.uid())` |
| P0#7 | `api/hermes/knx-design/route.ts` | Used `lead.assigned_to` without selecting it | Added `assigned_to` to SELECT |

### P1 — Input Validation & Resilience
| # | File | Issue | Fix |
|---|------|-------|-----|
| P1#8 | `lib/notifications.ts` | Notification failures silently lost | Writes errors to `logs/notification-errors.log` |
| P1#10 | `api/quotations/generate/route.ts` | Quote number race condition (SELECT MAX+1) | DB function `generate_quote_no()` with `pg_advisory_xact_lock(42)` |
| P1#11 | `lib/quotation-engine.ts` | `discount_rate` unbounded | Clamped 0–100 |

## Infrastructure Changes
- **Bearer token auth**: 4 API routes now accept `Authorization: Bearer <jwt>` as fallback (enables automated testing)
- **Regression test suite**: `scripts/regression-test.cjs` — Node.js, creates test users/leads, runs 14 test cases, 11/14 pass 5-run stable
- **DB functions**: `generate_quote_no(year)` — transaction-safe quote numbering

## Not Fixed / Non-Issues (5)
| # | Reason |
|---|--------|
| P0#5 | Grammar error in knx_circuit_engine.py — previously fixed |
| P0#6 | KNX sleep() — UI progress bar simulation, not security |
| P1#9 | /api/notify — each notification type already has auth checks |
| P1#12 | contracts/page — no independent contracts page exists |
| P1#13 | Script paths — Hermes skill layer, not CRM |

## Deployed
- Build: ✅ `npm run build` passes
- Service: ✅ `systemctl restart newme-platform.service`
- Migrations: ✅ `supabase db push` applied (20260622220000, 20260622220100)
- Push: ✅ `feat/crm-v2` @ c438db5
