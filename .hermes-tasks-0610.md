# NewMe CRM 任务进度 — 2026-06-10

## 状态：Batch 1 完成，Batch 2 待启动

### Batch 1 ✅ 全部完成

#### #1 Tanya(boss)密码查看 + Last Active ✅
- **密码查看 GET API**: 修复了 cookie 解析 bug（base64/raw JSON 两种格式），新增 `parseCookieValue()` helper，GET+PATCH 都修了
- **Last Active 追踪**: 在 `src/proxy.ts` 实现了 middleware 级别的追踪
  - 解析 auth cookie 获取 user ID
  - 5 分钟限流（内存 Map）
  - 跳过 API/_next/static 路径
  - fire-and-forget 写入 profiles.last_active_at
- 修改文件: `src/app/api/users/[id]/password/route.ts`, `src/proxy.ts`

#### #3 Leads 创建后不自动刷新 ✅
- **根因**: `QuickCreateLeadDialog` 的 `onCreated` 回调接的是 `router.refresh()`（只触发服务端重新验证，不会重跑客户端 fetchLeads）
- **修复**: 一行改动 `onCreated={() => router.refresh()}` → `onCreated={fetchLeads}`
- 修改文件: `src/app/(dashboard)/leads/page.tsx` L960

#### #4 通知中心规则 ✅
- 原有基础设施已较完整（notifications 表、API routes、NotificationBell 组件）
- **扩展**: 通知类型从 7 → 13 种（新增 lead_created, lead_stage_changed, quote_created, contract_created, payment_due, team_member_added）
- **新功能**: POST /api/notifications (admin/boss 直接创建)、lead_created 通知逻辑、6 种新类型的图标和翻译
- **已接线**: leads/new/page.tsx 和 QuickCreateLeadDialog.tsx 创建 lead 后触发 notify()
- 修改文件: 10 个文件（API routes、lib、components、translations、migration SQL）

---

### Batch 2 待执行（3 worker 并行）
- **Worker D**: #5a 产品导入
- **Worker E**: #5b 合同首期催收
- **Worker F**: #5c Leads follow-up 启动

### 关键路径
- 项目: `/home/ubuntu/newme-platform`
- Supabase: .env.local 中有 URL 和 keys
- Auth cookie: `sb-vfopmpxlhwzpxqegayew-auth-token`
- 角色体系: admin, boss, sales, designer, operator, finance
- 前端框架: Next.js App Router + Supabase JS client
