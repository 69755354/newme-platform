# 交接文档 — 2026-07-01

## 交接方视角（旧 LLM → 新 LLM）

### 我是谁、做了什么
我是 Hermes (deepseek-v4-pro)，在 2026-07-01 会话中接手了 CRM v3（newme-platform）项目。

### 核心教训：CC 子代理编造数据库列
Phase 2 时 CC 子代理在没查数据库实际结构的情况下，编造了 `leads.product_id`、`leads.created_by`、`leads.poor_reason` 等不存在的列名。
这导致了生产环境 leads/[id] 页面崩溃两天。**教训：永远先查实际表结构再写代码。查 leads 表结构用：**
```
supabase.from('leads').select().limit(1)  # 看出所有列
```

### 当前代码状态
- 项目：`/home/ubuntu/newme-platform`，分支 main
- Build ✅，部署 ✅，`app.newme.ae` 正常
- `check-taskboard.sh`：17 PASS / 0 FAIL / 1 WARN
- Tier 1 进度：11/12 (92%)

### 环境变量位置
- `.env.local` — 本地开发
- `.env.production` — 生产（但服务实际用 systemd 的 EnvironmentFile）
- Supabase URL / keys 都在 `.env.local`
- `COS_SECRET_ID` / `COS_SECRET_KEY` 在 `.env.local`
- COS bucket：`newme-1302961787`（新加坡）

### 坑清单
1. **context compaction 后方向漂移**：看到 handoff 摘要时，先读用户最后几条消息确认方向
2. **nginx CSP**：改了需要 `sudo nginx -t && sudo nginx -s reload`
3. **部署**：`npm run deploy` 调 `scripts/deploy.sh`，Step 0 有 taskboard gate
4. **Git commit**：需要 `[GLM-CP]` 前缀（v4 Code Gate）
5. **类型问题**：Next.js 16.2.6 + Turbopack，类型检查比之前严格

---

## 接收方视角（新 LLM 开局第一步）

### 第一步：了解当前状态
```bash
cd /home/ubuntu/newme-platform
bash scripts/check-taskboard.sh        # 确认任务状态
cat TASKBOARD.md                        # 看还有什么没做
git log --oneline -10                   # 看最近的 commit
```

### 第二步：确认生产是否正常
```bash
curl -sI https://app.newme.ae/login     # 应返回 200
curl -sI https://app.newme.ae/leads     # 应返回 200 (302 到登录页也正常)
```

### 第三步：读关键文件（按顺序）
1. `TASKBOARD.md` — 任务状态和 Freeze Rules
2. `crm-v3/session-summary-20260701.md` — 本次会话完整记录
3. `src/app/(dashboard)/leads/[id]/page.tsx` — 最复杂的页面，最易出 bug
4. `src/shared/hooks/usePipelineDragDrop.ts` — 拖拽逻辑，LeadBase 类型用了 `[key: string]: any`

### 第四步：优先做的事
1. **T1-12**：手动验证 Sentry（进 Sentry dashboard 查看是否有 error 事件）
2. **全局时区**：搜索 `toLocaleString` 或 `Date(` 找到剩余 12 处，替换为 `fmtDubai()`
3. **等用户说**：用户会告诉你接下来做什么

### 常见问题速查
| 症状 | 原因 | 修复 |
|------|------|------|
| leads 详情页 "加载失败" | 查询了不存在的列 | 查实际表结构，移除伪造列名 |
| 页面 300+ 次请求 | CSP 拦截 PostHog | 检查 nginx CSP 白名单 |
| `42703` 错误 | PostgreSQL 列不存在 | CC 编造的列名 |
| `PGRST200` 错误 | PostgREST 外键关系不存在 | FK 不存在或列不存在 |
| git commit 被拒 | 缺少 [GLM-CP] 前缀 | `git commit -m "[GLM-CP] ..."` |
| build 类型报错 | Next 16.2.6 类型更严 | 加 `?` 或 `| null` |

### 关键域名 / API
- 生产：`app.newme.ae`（不是 crm.newme.ae）
- Supabase：URL 在 `.env.local` 的 `NEXT_PUBLIC_SUPABASE_URL`
- PostHog：`eu.posthog.com`（EU cloud）
- COS：bucket `newme-1302961787`，region `ap-singapore`

### 系统服务
```bash
sudo systemctl status newme-platform    # 看 Next.js 服务状态
sudo journalctl -u newme-platform -f    # 看实时日志
```
