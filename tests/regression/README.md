# Repository-owned regression foundation

These tests are offline and deterministic. They do not connect to production Supabase, run migrations, deploy, or restart services.

`/home/ubuntu/.hermes/scripts/crm-regression.py` is an external source. If unavailable in a given environment, record `BLOCKED_EXTERNAL_SOURCE` and continue with the repository-owned tests here.
