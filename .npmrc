# Unreviewed dependency install scripts are a hard failure. Exact approvals and
# denials live in package.json#allowScripts and are checked before npm ci.
registry=https://registry.npmjs.org/
strict-allow-scripts=true
