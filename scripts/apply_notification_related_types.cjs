#!/usr/bin/env node
"use strict";

process.stderr.write(
  "retired utility: apply the reviewed versioned notification migrations through the protected database transition\n",
);
process.exitCode = 64;
