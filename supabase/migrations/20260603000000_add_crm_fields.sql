-- ================================================
-- NewMe CRM v2 - 添加销售归属和统计字段  (TOMBSTONE)
-- ================================================
-- This migration has never been applied to any database, including production,
-- and its statements now contradict the live schema. It is deliberately empty.
--
-- What was wrong
-- --------------
-- Line 30 of the original file read:
--     ALTER TABLE TABLE leads ADD COLUMN IF NOT EXISTS lost_reason_no_budget ...
-- `ALTER TABLE TABLE` is a syntax error. The Supabase CLI runs each migration
-- file in one transaction, so the error did not just skip that line — it rolled
-- back the entire file, every time it was attempted.
--
-- How we know it never applied
-- ----------------------------
--   * leads.follow_up_count was added on line 23, BEFORE the syntax error, and
--     does not exist in production (it is absent from the generated
--     src/types/database.ts). A pre-error statement that left no trace is only
--     possible if the whole transaction rolled back.
--   * The file's own second `CREATE TABLE IF NOT EXISTS business_events` declares
--     entity_type / entity_id / created_by. Production's business_events has
--     lead_id / event_type / event_data / description / user_id / created_at —
--     the shape from 20260602010000_crm_mvp_final.sql:7, which did run. The table
--     this file describes has never existed.
--   * create_business_event(uuid, text, uuid, text, jsonb, uuid), defined only
--     here, is absent from the production function list in the generated types
--     and has no caller anywhere in the repository.
--
-- Why it is emptied rather than repaired
-- --------------------------------------
-- Fixing the typo would have made the file live for the first time, months late,
-- and it would then have created a second, incompatible business_events
-- definition plus indexes on columns that do not exist in production. Every
-- object in the file that DOES exist in production is created by a migration that
-- actually ran:
--   * the leads columns — 20260602000000_crm_v2_columns.sql and
--     20260605000000_newme_crm_v22_complete.sql
--   * business_events — 20260602010000_crm_mvp_final.sql
--   * the sales_performance view — recreated by later migrations
-- The one exception, follow_up_count, does not exist in production and nothing in
-- the repository reads it.
--
-- Emptying the file makes the migration set replay to a schema that matches
-- production, which is the only thing a migration set is for. It also unblocks
-- the next `supabase db push`, which would otherwise still hit the syntax error.
--
-- Do not add statements here. Found by scripts/replay-migrations.sh; the original
-- content is in git history at 8b8b6cf7ad6.
-- ================================================

-- Intentionally no statements.
select 1 where false;
